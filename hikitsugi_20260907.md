# TASUKARU 開発の引き継ぎ（2026-09-07 時点）

このまま新しいチャットの最初に貼ってください。
私（HIRO）から改めて説明しなくても、続きから始められるように書いてあります。

---

## 0. まず守ってほしいこと（これが最優先）

### セキュリティ

- **キー・トークン・パスワード・接続文字列を、チャットにも画面にも出さないこと。**
  どうしても必要なときは、**必ず伏せ字にする**（末尾4文字だけ、`sk-…4a7f` のように）。
- **私に「キーを貼って」と頼まないこと。** 伏せた形で十分に判断できるように考えること。
- Supabase の Project ID、LINEのチャネル情報、Stripeの情報なども、
  **必要になったら「本番の方」「DEVの方」と呼ぶ**だけにして、値はチャットに書かない。
- スクリーンショットを私が貼るときに値が写り込んでいたら、**そこに触れず、伏せて扱うこと。**

### 作業の分担（これまでと同じ）

- **本番のSQLは、必ず私が実行する。** 読むだけのSQLでも、あなたが流さない。
  SQLはファイルで渡してくれれば、私が Supabase の SQL Editor で流して結果を貼る。
- **本番は読むだけ。** 確認のためにデータを作らない・変えない。
- **git は必ず私の手元。** コマンドを渡すだけにすること。
- **直しは `patch_*.py` で渡すこと。** marker・`.bak`・冪等（何度流しても同じ）・
  自己検査（assert）つき。私は `python3 patch_xxx.py` を流すだけ。
- **パッチは私の `~/dev/kaigo-ai-app/` に直接置いてくれる。** 私がダウンロードして移す手間はない。
- **コードはあなたが自分で読む。** 「grep して」「sed して」の往復は要らない。

### ★ファイルが必要なとき（これは新しい約束）

あなたの手元に無いファイルが必要になったら、
**VSCodeのターミナルで走らせて、ダウンロードフォルダに落とすコマンドを投げてください。**
私がそれを流して、**このチャットに添付します。**

例：

```
cp ~/dev/kaigo-ai-app/templates/renraku.html ~/Downloads/
```

「見せてください」と言うだけでなく、**コマンドの形で渡してくれると早い**です。

---

## 1. TASUKARU とは

介護事業所向けの統合SaaS。私（岸本洋幸）が1人で作って、1人で運用しています。
自分の事業所（機能訓練型デイサービス「ココカラプラス」／地域密着型通所介護）と
接骨院「もみの木接骨院」を運営しながら、B2Bの製品として育てています。
運営は合同会社LIFE PLUS。

### 作り

- Flask + Jinja2 + Supabase（service key）+ Google Cloud Run（asia-northeast1）
- リポジトリ `cocokaraplus-max/kaigo-ai-app` / 手元は `~/dev/kaigo-ai-app/`
- ブランチ: `tasukaru-dev`（DEV）→ `tasukaru`（本番）。マージは常に `--no-ff`、**最後に必ず `tasukaru-dev` へ戻る**
- **GitHubへpushすると Cloud Build が自動でデプロイ**（3〜4分）。手動デプロイは不要
- Supabase は DEV と本番の2つ（Project ID はチャットに書かない）
- 本番の施設コード: `cocokaraplus-5526` / DEVは `DEMO001`

### 知っておくと事故が減ること

- **Cloud Run は HTTP/1 だと 1リクエスト 32MiB まで。** 動画・写真・PDFを一緒に運ぶと当たる
- **列の型がばらばら。** `patient_profiles.id` は uuid、`vitals.patient_id` と
  `renraku_notes.patient_id` は text、**`line_friends.patient_id` だけ uuid**。
  古い `"37"` のような番号が残っていると uuid 側で落ちる
- **型を推測して書かない。** 分からなければ `information_schema` を1回読む（私が流す）
- **Supabase の SQL Editor は、最後のクエリの結果しか返さない。** 診断は1本にまとめること
- デプロイ後は Service Worker のキャッシュが残ることがある

---

## 2. 仕事の型（これで回してきました）

### パッチの作法

`patch_*.py` は必ずこの形。

- marker（例 `pay-verify-v1`）を全ファイルに残す
- `.bak` を取る
- 冪等（2回流しても「すでに適用済みです」）
- `swap(text, old, new, name)` で置き換え、**目印の数を assert で確かめる**
- 直したあとに**自己検査**を書く。「足したもの」だけでなく
  **「触っていないもの」が変わっていないこと**も見る

### ★assert の数え方（何度も転んだところ）

- **当ててから数える。頭で数えない。** 例外なし
- `def foo(...)` を探すと**定義の行にも当たる**。呼ぶ所だけ数えたいなら、呼ぶ形で書く
- HTMLの `id="x"` と JSの `getElementById('x')` は別々に数える
- **「触っていないこと」の検査は、数を書かず「前と後で同じ」で比べる**
  （`before.count(k) == after.count(k)`）。数を知らなくても書ける

### 関数の切り出し

「次の `@app.route` まで」で切ると、後ろに口を足しただけで切れ目が動く。
**字下げの無い行が来たら関数の終わり**で切る `_top_block()` を使うこと。
★これを別のパッチに写すとき `"\\n"` が `"\\\\n"` になっていたのに**落ちずに動いた**ことがある
（JSの `confirm('…\\nよろしいですか？')` の中の本物の `\\n` にたまたま当たっていた）。
**写したら1回は切れた中身を目で見ること。**

### 確かめ方（毎回これを通してから渡す）

- `python3 -m py_compile app.py`
- Jinja2 `Environment().parse()` でテンプレートが読めるか
- `<script>` を抜き出し（`src=` は除く）、`{{ }}` `{% %}` を置き換えて `node --check`
- **ハーネス（台）を1本書く。** 偽DOM・偽fetch・偽Supabase で動かす。
  いまある台（作業コピーに置いてある）:
  `h_line.py` 41 / `h_toggle.py` 150 / `h_shared.py` 49 / `h_video.py` 45 /
  `h_board.py` 33 / `h_vismedia.py` 43 / `h_payverify.py` 45 /
  `h_copy.js` 31 / `h_sharedui.js` 61 / `h_videoui.js` 57 / `h_boardui.js` 58 /
  `h_savebefore.js` 32 / `h_lineopen.js` 24
  **新しいパッチを当てたら、全部を流し直すこと。** 壊した所が出る

### 私に渡すときの形

1. パッチを `~/dev/kaigo-ai-app/` に置く
2. 流すコマンドを1行で渡す
3. コミット＆pushのコマンドを1行で渡す
4. **デプロイ後に私が何を見ればいいかを、具体的に書く**

---

## 3. 2026-09-07 にやったこと

### 本番に出したもの

| commit | 中身 |
|---|---|
| `2c05500` | 連絡帳4本（LINEの項目・コピー・トグル） |
| `e20827e` | 全利用者様の共通入力欄 |
| `fc16892` | 連絡帳と掲示板に動画（`video-srv-v1` / `video-renraku-ui-v1` / `board-video-v1`） |

動画まわりで決めたこと:

- 上限 **20MB**（Cloud Run の 32MiB から逆算）。**サーバーと画面の2か所に同じ数字がある**
- 掲示板は**選んだ時点で `/api/upload_video` に上げ、投稿にはURLだけ渡す**
  （投稿と一緒だと写真・PDFと合算で32MiBに当たる）
- LINEに送れるのは **mp4/m4v ＋ プレビュー画像があるもの**だけ。mov は保存も表示もするが送らない
- **現場のiPhoneは mp4 を作ると実機で確認済み。** だから ffmpeg での変換は入れないと決めた
- **掲示板は知らせ（LINE）に送らない。** 掲示板の札に「LINE」と書いてはいけない

### DEVに入っていて、本番へ出していないもの（DBの変更なし）

| marker | 中身 | 確認 |
|---|---|---|
| `renraku-save-before-v1` | 「LINEで送る」「印刷」の前に保存する | 実機で印刷OK |
| `renraku-line-vis-media-v1` | 表示項目のトグルを写真・動画にも効かせる | 写真は確認OK |
| `renraku-line-vis-media-v2` | 送るもの・送らないものを**両方**出す | ★未確認 |
| `pay-verify-v1` | 勤怠を社労士さんが検算できる形にする | ★未確認 |

覚えておいてほしい決め事:

- **印刷は「先に空のタブを開いてから保存する」。** 保存を待ってから `window.open` すると
  押した操作から離れて iPhone/Safari がポップアップとして止める。**素直に書き直すと壊れる**
- **「まとめて印刷」(`rkPrintAll`) は触らない。** 一覧の画面にあり書きかけが無い。
  しかも `RK_PATIENT` は一覧に戻っても残るので、保存すると開いていない利用者の連絡帳を書いてしまう
- **写真・動画は「保存した中身」から取る。** 画面から受け取らない。文章と同じ出どころにした
- **読めなかったときの倒し方が、文章とは逆。** 文章のトグルは**全表示**に倒す（消えて驚かせない）、
  写真・動画は**送らない**に倒す（送ってよいか分からないまま送るほうが取り返しがつかない）

### ★勤怠の件（今日いちばん大きかった話）

社労士さんから「何度計算しても合計が合わない」と言われた。
本番の実データを読んで、**計算そのものは合っていた**と分かった。合わないのは**見せ方**。

- **① 秒**: 出勤・退勤は「時:分」までしか出していないのに、実働は**秒まで見て**計算している。
  8月は打刻326件のうち289件(88.7%)に秒があり、6名合計 **57分**ずれていた
- **② 中抜け**: 一度退勤して戻った日。CSVには最初の出勤と最後の退勤しか出ず、
  間の空き時間はどこにも出ない。**打刻異常の印も付かない。**
  宇佐美友理さん 8/21 で **165分（2時間45分）**
- 打刻がそろっていない日は8月**ゼロ**だった

直したこと（`pay-verify-v1`）:

- **途中退出(分)** と **在席(分)** の列を右端に足した
  → ★**在席 − 休憩 = 実働** が【いつでもぴったり】成り立つ。合計行でも成り立つ
- 出勤・退勤を**秒まで**表示（`09:00` → `09:00:50`）
- 合計行に「打刻異常 N日（実働は合計に入っていません）」

★**実働の数字は1つも変えていない。** でたらめな打刻3000通りで前と後が同じになること、
300か月分で合計行の等式が外れないことを機械で確かめた。

★**1分未満の扱いは未決。社労士さん待ち。**
いまは区間ごとに切り捨て（＝すでに1分未満は捨てている）。
「打刻の秒を先に捨てる」形にすると検算と一致するが**支払う時間が増える**（8月で+57分）し、
**中抜けの問題は解決しない**。労働時間の端数処理は賃金に関わる論点なので、
**こちらで決めず、社労士さんの指示どおりに直す。**
社労士さんへの説明文（`kintai_setsumei_202608.md`）の末尾でその質問をしている。

---

## 4. すぐ次にやること

1. DEVで**連絡帳②**を確認（動画をオフにしたとき、
   「文章と写真 1枚を一緒に送ります。」と「動画は送りません。」が**両方**出るか）
2. DEVで**勤怠のCSV**を確認（右端が 途中退出(分)/在席(分) か、
   出勤・退勤に秒が出ているか、在席−休憩=実働 か）
3. 両方よければ**本番へ4本まとめてマージ**（DBの変更なし）
4. 本番で**8月分のシンプル勤怠を出し直して**、説明文と一緒に社労士さんへ送る

---

## 5. 返事待ち・手つかず

**返事待ち**

- 社労士さん … ①1分未満の扱い ②出勤・退勤に秒が入っても取り込みに影響しないか
- LINEのトークが上に来ない件 … トーク画面に文章そのものは出ているか（未確認）

**手つかず**

- 動画: `.mov` が出たときの案内（iPhone 設定→カメラ→フォーマット→互換性優先）
- `line_friends.patient_id` だけ uuid、連絡帳側は text。移行するか
- Google Workspace の解約そのもの（私が「サブスクリプションを解約」を押す段階）
- Google Play 開発者アカウントの持ち主（未確認）
- Apple: `DEVELOPMENT_TEAM` の差し替え → App ID登録＋Push有効化 → APNs鍵 → 実機 → Unlisted/Custom App
- 介護保険課への事前相談3件／様式④付表第2号(3) の残り空欄
- 送迎: 出発・帰着のガードの403／曜日配車の保存が「全消し→入れ直し」／
  `_soge_merge_day` の `stop_by_key`／`/api/soge/run/replan` の `facility_code` 絞り

---

## 6. 最後に

**README.md（`~/dev/kaigo-ai-app/README.md`）に全部書いてあります。**
8200行あるので頭から読む必要はありませんが、**末尾の 2026-09-04 以降**は目を通してください。
特に各日の「★この日の失敗（次に読む自分へ）」は、同じ穴に落ちないために書いたものです。

作業を始める前に、まず私に**何をするか**を1〜2行で言ってください。
それから読み始めてくれれば大丈夫です。
