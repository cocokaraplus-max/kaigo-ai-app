# 引き継ぎ 2026-09-01（次のチャットへ）

**このファイルを最初に、最後まで読んでください。** HIROさんに、もう一度説明させないためのものです。
前回（`引き継ぎ_2026-08-28.md`）を引き継いだうえで、**09-01 のぶんを全面的に書き直し**ています。
このファイルだけで足りるように書いてあります。古いほうは、必要になったときだけ見てください。

---

## 0. まず守ること — セキュリティ（★破らない）

**キー・トークン・パスワード・接続文字列を、チャットにも画面にも出さない。**

- `.env` / 環境変数 / Supabaseのキー / APIキー を `cat` `echo` `printenv` **しない**
- ログやエラーを貼るときも、キーが混ざっていないか**貼る前に自分で見る**
- どうしても触れる必要があるときは、**必ず伏せる（スクランブルをかける）**
  - `SUPABASE_KEY=（伏せ字）`
  - どうしても同一性を示す必要があるときだけ、**末尾4文字**を `…a1b2` の形で
  - 先頭は出さない。先頭はキーの種類が分かってしまう
- スクリーンショットも、キーが写る画面は撮らない・貼らない
- **このファイルにも、実物のキーは1つも書いていない。** 次のチャットも、この状態を保つこと

**本番のSupabaseに流すSQLは、必ずHIROさんの手元で実行してもらう。**
読むだけのSQLでも、こちらからは流さない。

**本番（`tasukaru`）は読むだけ。** 確認のためにデータを1件も作らない・変えない。
画面を触ったら、必ず元の状態に戻してから終わる。

### SQLを渡すときの決まり

- **1回につき1文だけ**渡す。SupabaseのSQLエディタは**最後の文の結果しか返さない**
- **そのまま流せる形**で渡す。`<ここに施設コード>` のような穴を空けない。実際の値を入れて渡す
- 「これはSQLです」「これはターミナルです」と**必ず書く**。混ざると事故になる

---

## 1. ファイルの受け渡し（★ここが今回いちばん変わった）

新しいチャットでは、**HIROさんのフォルダにこちらから直接触れないことがある**。
そのときは、次の形で進める。

### こちらがファイルを見たいとき

**VS Code のターミナルにそのまま貼れるコマンドを、こちらから出す。**
HIROさんはそれを流して、`~/Downloads` に出たファイルを**このチャットに添付する**。

```bash
# 例：見たいファイルをダウンロードフォルダへ出す
cd ~/dev/kaigo-ai-app && cp -v templates/soge_run.html templates/soge_week.html ~/Downloads/ && open ~/Downloads
```

**app.py は大きいので、まるごと出さない。** 見たい範囲だけ抜く。

```bash
# 例：app.py の 28200〜28400 行だけ抜き出す
cd ~/dev/kaigo-ai-app && sed -n '28200,28400p' app.py > ~/Downloads/app_抜粋.txt && open ~/Downloads

# 例：関数の場所を先に探す（行番号だけ返してもらう）
cd ~/dev/kaigo-ai-app && grep -n "def soge_materialize_day\|def _soge_merge_day" app.py
```

★**まず `grep -n` で行番号を出してもらい、それから範囲を抜く。** これが速い。

### こちらがファイルを渡すとき

パッチ（`patch_*.py`）などは、こちらからチャットに出す。
HIROさんが `~/dev/kaigo-ai-app/` に置いて、ターミナルで流す。

### git と SQL は必ずHIROさんの手元

こちらはコマンドを渡すだけ。**流すのはHIROさん。**
コマンドは `&&` で1本につないで渡す（1行ずつだと貼り間違いが起きる）。

---

## 2. 相手のこと

- **HIROさん（ZIMAX）**。TASUKARU をひとりで作って、ひとりで運用している
- 同時に**デイサービスの経営者**。現場を毎日見ている。だから指摘が具体的で速い
- 本番は**86事業所**が使っている。壊すと現場が止まる
- 判断が速い。案を出せばすぐ選ぶ。**迷わせない形で出すこと**（案A/案Bとモック画像）

### 話し方

- **日本語**。専門用語を避け、現場の言葉で書く
- コードの中のコメントも**日本語**
- 長い説明より、**画像1枚**のほうが早い。モックを作って見せる
- **できないこと・間違えたことは、はっきり言う。** ごまかさない
  - 実際、HIROさんは「これ本当に作り直されないの？」と文を読んで**嘘に気づいた**
  - こちらの間違いを黙っていると、現場が困る

---

## 3. 環境

### リポジトリ

```
~/dev/kaigo-ai-app        （HIROさんのMac）
github.com/cocokaraplus-max/kaigo-ai-app
```

### ブランチ（★この2本だけ）

| ブランチ | 何 | 施設コード |
|---|---|---|
| `tasukaru-dev` | 検証。ここで先に確かめる | DEMO001 |
| `tasukaru` | **本番**。86事業所が使う | cocokaraplus-5526 ほか |

### 出す手順（★必ずこの順）

```bash
# ① dev に出す
cd ~/dev/kaigo-ai-app
git switch tasukaru-dev
python3 patch_xxx.py
python3 -m py_compile app.py        # app.py を触ったときだけ
git add -A && git commit -m "marker: 何をしたか"
git push

# ② DEMO001 で確かめてもらう（デプロイは自動。数分かかる）

# ③ 本番へ
git switch tasukaru
git merge --no-ff tasukaru-dev -m "本番反映: marker"
git push
git switch tasukaru-dev            # ★必ず dev に戻して終わる
```

- **デプロイは自動**（Cloud Build → Cloud Run / asia-northeast1）。push すれば数分で入る
- `--no-ff` を必ず付ける。履歴が追えなくなる
- **最後は必ず `git switch tasukaru-dev`。** 本番ブランチに居たまま次の作業を始めると事故る

### 構成

- Flask + Jinja2 / Supabase（service key・RLSは通さない）/ Cloud Run
- 画面のテンプレートは `templates/` の下
- ロジックはほぼ `app.py`（約37,000行）

### 送迎まわりの画面（今回いちばん触った）

| パス | 画面 | ファイル |
|---|---|---|
| `/soge` | 配車編集（いつもの配車＝曜日ごと） | `templates/soge_week.html` |
| `/soge/date` | 配車編集（今日だけの配車＝日付ごと） | 同上（`sg_mode='date'`） |
| `/soge/run` | 運行画面（運転中／一覧） | `templates/soge_run.html` |
| `/soge/print` | 記録表（月の帳票） | `templates/soge_print.html` |

### 時刻

- 表示も記録も**日本時間**。DBは UTC で持つ
- 変換は `_soge_jst()` を使う。手で9時間足さない

---

## 4. パッチの作法（★この形を崩さない）

直しは**すべて `patch_*.py`（Pythonスクリプト）**で渡す。
HIROさんが手元で流す。ファイルを直接書き換えて渡さない。

### 必ず入れるもの

1. **marker**（`soge-day-merge-v1` のような印）。コードの中に必ず残す
   - あとで「この直しが入っているか」を1行で確かめられる
2. **`.bak` を取る**（marker がまだ無いときだけ）
3. **何度流しても同じ結果になる**（idempotent）。2回流しても壊れない
4. **最後に `assert` で自己検査**。当たっていなければその場で止める
5. **理由を日本語でコードに書く**。「なぜこうしたか」「前に何が起きたか」
6. うまくいかなかった手当ては**残さずに消す**

### `swap()` と `add_before()`（毎回この2つを使う）

```python
def swap(f, old, new, label):
    """★新しい形が古い形の一部なら【古い形】を先に、
       そうでなければ【新しい形】を先に見る。
       ここを逆にすると、2回流したときに二重に入る（実際にやった）。"""

def add_before(f, anchor, block, mark, label):
    """anchor の【前】に足す。関数を足すときは必ずこちら。
       ★うしろに足すと、目印が「function ○○() {」のとき
         中身がその関数の【中】に入り、外から呼べなくなる。
         node --check は通ってしまうので気づけない（実際にやった）。"""
```

### assert の書き方（★ここでいちばん失敗している）

**「その文字列が入っているか」で数えてはいけない。** 自分が書いた説明コメントにも
同じ文字が出るので、必ず引っかかる。**この失敗を10回以上している。**

```python
def count_line(text, line):
    """その行が【まるごと一致する】数を数える。"""
    return text.split("\n").count(line)
```

- 行まるごとで数える（`count_line`）
- または**コードの形**で見る（`d.source_data ` と `d.source_data_eval` は別物）
- `def` の行も数に入ることを忘れない（関数名で数えると必ず+1される）

---

## 5. 確かめ方（★これが品質の中身）

出す前に、必ずここまでやる。

| 対象 | やること |
|---|---|
| `app.py` | `python3 -m py_compile app.py` と `pyflakes`（**基準は 89 件**。増やさない） |
| テンプレートのJS | `<script>` を抜き出して `node --check` |
| テンプレート全体 | `jinja2.Environment().parse()` |
| サーバの関数 | **本物の app.py から AST で切り出して**、作り物のSupabaseで動かす |
| 画面 | **本物のテンプレートを Flask で `http://` に出して、Chromium で実際に押す** |

★**「読んで正しそう」で出さない。押して測る。** 今日の見つかった不具合は、
どちらも**実際に押して**見つかった。

### 作り物データ（fixture）でやりがちな間違い（全部やった）

- `patient_profiles` に `facility_code` を入れ忘れ → 絞り込みに1件も当たらず、
  「分からない人は出す」の既定に落ちて、**確かめたつもりで何も測っていなかった**
- `/api/soge/staff` は**文字列の配列**を返す。オブジェクトの配列にすると `[object Object]`
- 立ち寄りの `id` を重複させた → `findStop()` が**別人**を拾い、⇄ のパネルに違う名前が出た
- 画面が見ているのは `j.day`（`j` の直下ではない）。取り違えると注意書きが1つも出ず、
  「直っていない」と誤判定する

### 手元のテスト（`/home/claude/work/`）

作業ディレクトリに、本物のテンプレートを出す小さなFlaskサーバ（`srv9.py`）と、
Playwright のテストが一式ある。**次のチャットでは作り直しになる**ので、
必要なら同じ形で作ること（本物のテンプレート＋作り物データ＋実際に押す）。

---

## 6. 2026-09-01 に出したもの（すべて本番稼働中）

本番は `110edd4` まで。dev（`baf3821`）と揃っている。

### 送迎 — 記録が残らなかった件の対処

| marker | 何を直したか |
|---|---|
| `soge-tap-retry-v1` | **押しても何も起きない**を止めた。`fetch` の例外を外に出さず、赤い帯で必ず知らせる。圏外の打刻は端末に貯めて、電波が戻ったら送り直す（48時間・最大200件）。未送信には「未送信」の印 |
| `soge-day-guard-v1` | ① 日付の配車を第N週で絞っていたのをやめた（**9/1が第1週なので、第2〜5週の人が全員落ちていた**）② 状態が読めないときに作り直さない（読めない＝無い、にしない）③ 消すときにログを出す |
| `soge-day-merge-v1` | **走り出したあとでも当日の配車を直せる**。作り直しではなく**差分だけ当てる**。増えた人は足す／外れた人は消す／**打刻・欠席が付いていたら残す**／車を移された人は**行ごと移す（到着時刻を持ったまま）**。出発・帰着・備考・臨時便は触らない |
| `soge-plan-label-v1` | 先の人の時刻に「予定」と書いた。書いていなかったので**打刻済みに見えていた** |

### 送迎 — 運行画面の作り替え

| marker | 何を直したか |
|---|---|
| `soge-drive-v5` | 運転中の板：先の人を灰色に。住所の色は落とさない（コントラスト） |
| `soge-undo-v1` | 押し間違いを戻す。直後の帯（確認なし）＋「済んだ人」一覧（確認あり）。出発・帰着も戻せる |
| `soge-driver-change-v1` | 土壇場で運転手を替えられる。「未設定」は選ばせない（嘘になる） |
| `soge-back-v1` | **運転席で順番を入れ替える。**「後にする」を押すと次の人が前に出る。**回した人の板にも小さい「後に」**を置いた（これが無いと2回目で行ったり来たりになる） |
| `soge-done-road-v1` | 送り終わった人を道の上に灰色で残す。時刻に必ず**「済」**と書く |
| `soge-run-list-v1` | 「車両から」「便から」を無くし、**運転中／一覧**の2つに。消えたもの（確定・作り直し・臨時便・車が未定の人）に置き場所を作ってから消した |
| `soge-run-acc-v1` | 運転中の車えらびを**縦のアコーディオン**に。閉じた車にも「次は誰か」を1行 |
| `soge-run-pick-v1` | はじめは全部閉じる／一覧は車を選んでから。**一覧の ⇄ と ＋利用者 が効かなかったのを修正** |
| `soge-trip-pick-v1` | 一覧を **便 → 車 → 一覧** の順に。便は横3つ |
| `soge-time-edit-v1` | 記録表・運行画面の時刻が、**軽く触れただけで確定するのを止めた**。空のまま確定して実績が消えるのも止めた |
| `soge-print-driver-v1` | 記録表に運転手の列。TOPへ戻るリンク |

### 送迎 — 配車編集

| marker | 何を直したか |
|---|---|
| `soge-week-head-v1` | 上を「切り替え（いつもの配車／今日だけの配車）」と「操作（組み直す／ゼロから／保存）」の2段に。言葉を7か所そろえた |
| `soge-week-gap-v1` | 操作の段と曜日の段がくっついていた（**実測0px**）のを直した |
| `soge-week-preview-v1` | **見るだけの配車プレビュー**（便で見る／車で見る）。直せるものは1つも出さない |
| `soge-week-pvmx-v1` | 「車で見る」を**表**にして、迎え・中間・送りの頭をそろえた。**空欄はそのまま残す**（詰めると「無い」が見えなくなる） |
| `soge-week-warn-v1` | 「当日の運行表は作り直されません」が**嘘になっていた**のを直した |

### 評価・モニタリング

| marker | 何を直したか |
|---|---|
| `mon-no-greeting-v1` | モニタリングのAI文から「今月の訓練状況をお伝えします」などの前置き・結びを消す |
| `a2-voice-v1` / `a2-len-v1` | 評価担当ページの聞き取りメモに音声文字起こし。文字数を150〜500（50刻み） |
| `a2-section-delete-v1` | 評価担当ページ／機能訓練指導員ページに、それぞれの削除ボタン |
| `a2-seq-swap-v1` | **1と2を入れ替え**（聞き取りが先、目標があと）。順番が自動入力を殺していた |

---

## 7. 失敗の記録（★ここをいちばんよく読むこと）

**同じ失敗を繰り返さないために書いてある。** 抽象論ではなく、実際に起きたこと。

### A. 作りの失敗

| 何が起きたか | なぜ | どうする |
|---|---|---|
| **画面が真っ白（`escA is not defined`）** | 関数を「`function foo(t) {`」の**うしろ**に足したので、その関数の**中**に入った。`node --check` は通る | **必ず `add_before` で前に足す。** 足したあと、ひとつ上が `}` `;` `*/` で終わっているか assert |
| **2回流すと二重に入る** | `swap()` で「新しい形が古い形を含む」ときに、新しい形を先に見ていた | 含む向きで分岐する。今の `swap()` はそうなっている |
| **assert が自分のコメントに当たる（10回以上）** | 「その文字列が入っているか」で数えた。説明コメントに同じ字が出る | `count_line`（行まるごと）か、**コードの形**で見る。`def` の行も数に入る |
| **一覧の ⇄ と ＋利用者 が押しても何も起きない** | パネルの置き先を `.sr-trip` から探していた。一覧の行は別の枠なので `null` になり `appendChild` で落ちていた。**落ちても画面に何も出ない** | 置き先は**必ずフォールバックを付ける**（最後は `btn.parentNode`）。作ったら**必ず押して確かめる** |
| **名前が1文字ずつ縦に割れた** | 列数を**画面の幅**で決めた。本文の幅は `--page-max-width`（既定480px）で絞られていて、画面1200pxでも本文は480pxだった | **入れ物の幅**で決める。`.sg-wrap` に `container-type: inline-size` があるので `@container` が使える。または `auto-fit + minmax` |
| **段がくっついた（実測0px）** | ボタンを親の外に出したとき、親が持っていた `margin-bottom` を付け直し忘れた | 要素を移したら、**移す前に効いていた余白を必ず付け直す**。測って確かめる |

### B. 言葉の失敗

| 何が起きたか | どうする |
|---|---|
| **「当日の運行表は作り直されません」が嘘になった** | サーバ側の言い方は直したのに、**画面の「保存する前に出る注意書き」を直し忘れた**。`soge-day-merge-v1` で反映するようにしたのに、画面は「されません」のまま。**HIROさんが文を読んで気づいた**。→ **動きを変えたら、それを説明している文を全部探す** |
| **同じものを2つの名前で呼んでいた** | 「この日だけの配車」「いつもの曜日の配車」が7か所に散っていた。1か所だけ直すと、いちばん人を迷わせる。→ **言葉を変えるときは全部まとめて** |
| **便の名前が「送り便」と2つ並んだ** | 便名を**立ち寄りの種類**から決めていたので、中間便に送りしか無いと「送り便」になる。→ 施設が付けた `trip_name` を先に使う |
| **帯の文が「…」で切れた** | 「○○さんを後にしました」が幅に入らなかった。→ **帯の文は短く。名前を先に置く** |

### C. 見立ての失敗（★2回、はっきり外した）

1. **「Supabaseの1000行制限で切れている」** と言った。
   HIROさんにSQLを流してもらったら **0件**。外れ。
   → 「私の見立ては外れでした」と、はっきり言った。**言い切る前に測る**

2. **スクリーンショットの灰色の板の時刻を「打刻済み」と読んだ。**
   実際は**予定時刻**だった。HIROさんに「この画面、記録が消えてるね」と正された。
   → 画面の色や数字から**推測しない**。データで確かめる

### D. 自分が作り込んだ穴

- `soge-undo-v1` と `soge-driver-change-v1` に、`soge-tap-retry-v1` と**同じ「catch が無い」穴**を作っていた。押しても何も起きない。
  → 新しく `fetch` を書くときは、**必ず既存の送り口（`srSend`）を通す**。生の `fetch` を書かない

### E. 進め方で決めたこと

- **大きい直しは2本に分ける。** 1本が大きすぎると、戻すときに困る
- **消す前に、そこにしか無いものを数える。** `soge-run-list-v1` では、消す前に3つ（確定・臨時便・車が未定の人）を見つけて、置き場所を作ってから消した
- **既存のテストは、動きを変えたら必ず通し直す。** 落ちたら「仕様が変わった」のか「壊した」のかを見分ける

---

## 8. 積み残し（優先度順ではない）

| 件 | 状態 |
|---|---|
| **9/1 10:06 の作り直しが何だったか** | 未解決。Cloud Run のログで `soge day state error` が 2026-09-01 01:06 UTC 前後に出ていないか見てもらう。打刻が消えたのか、そもそも無かったのかが決まる |
| 配車編集の「確定を解除する」ボタン | いまは運行画面の「一覧」にしか無い。配車編集の注意書きの中にも置くか、HIROさん判断待ち |
| 「車で見る」で人数の多い便 | 13名などで縦に長くなる。折りたたむかどうか、実データを見てから |
| 書類出力（担当者会議用）の挨拶文 | モニタリングと同じように消すか未定 |
| 席が足りないまま保存できる | 未着手 |
| 端末を全部消すと承認ログインが使えなくなる | 未着手 |
| LINE送信の成功もログに残す | 未着手 |
| `my_tasks_overdue` の JSON エラー | 未着手 |
| 旧方式のログインを閉じる／塩なし SHA-256 | 未着手 |
| px直書きが残り約80か所 | 未着手 |
| `/api/evaluation/ingest_file` の音声が「評価確定時に削除」されているか | 未確認 |

---

## 9. 次のチャットの最初の一手

1. このファイルを読む
2. HIROさんに、**いま困っていること**を聞く（明日の実運行の感想が来るはず）
3. 直す前に、**そこにしか無いものが無いか**を確かめる
4. 作ったら、**必ず自分で押して測る**
5. dev → DEMO001で確認 → 本番。**最後は `git switch tasukaru-dev`**

### 明日いちばん知りたいこと（HIROさんに聞く）

- **「後にする」**が運転席で使えたか（押す場所が2か所あるのが迷わないか）
- **当日の配車変更**が、打刻を壊さずに届いたか
- **アコーディオンが閉じて始まる**のが、運転手にとって遠回りでないか
  （もし「自分の車はいつも開いていてほしい」なら、前回開いた車を覚える形にできる）
