# TASUKARU 引き継ぎ（2026-08-21 作成 / 次のチャットのAIへ）

このファイルを**いちばん最初に、最後まで読むこと。**
ユーザー（岸本洋幸さん・HIRO）に、同じ説明を二度させないためのものです。

---

## 0. 30秒でわかる前提

- **TASUKARU** は、通所介護（デイサービス）の**本番稼働中**の業務システム。
  介護記録・送迎・勤怠・請求・評価などを1つでまかなっている。
- **本番で毎日使われている。** 壊すと現場の業務が止まる。実在の高齢者の記録が入っている。
- **ユーザーはエンジニアではない。** コードは読まない。ターミナルにコマンドを貼ることはできる。
- 主要ファイル：`app.py`（約32,000行のFlask）、`templates/*.html`（Jinja2）、
  `*_integration.py`（機能ごとのモジュール）。DBは Supabase（Postgres）。ホストは Cloud Run。

---

## 1. ★セキュリティ（これだけは絶対）

### キーは絶対に出さない

**APIキー・トークン・シークレット・パスワード・`.p8` の中身を、
チャットにもコードにも README にも、一切出力しない。**

対象（例）：
`GEMINI_API_KEY` / `ANTHROPIC_API_KEY` / `SUPABASE_SERVICE_KEY` / `SUPABASE_KEY` /
`STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` / `LINE_CHANNEL_SECRET` /
`LINE_CHANNEL_TOKEN` / `CRON_TOKEN` / `GOOGLE_MAPS_API_KEY` / Apple の `.p8` / 各種パスワード

- **どうしても言及が必要なときは、必ずスクランブル（伏せ字）にする。**
  例：`AIza••••••••3f2a` / `sk_live_••••••••` / `-----BEGIN PRIVATE KEY----- ••••••••`
- キーの置き場所は **Cloud Run の環境変数と Supabase 側だけ**。コードにベタ書きしない。
- **ユーザーがうっかりキーを貼ってしまったら、その場で指摘して再発行（ローテーション）を勧める。**
  そして以後、そのキーを会話に繰り返さない。

### 個人情報

- 介護記録は**要配慮個人情報**。SQLの結果やCSVを扱うとき、
  **利用者名・住所・病名などを不必要にチャットへ広げない。**
- 診断SQLは、**名前を出さずに済む形**（件数・フラグ・日付だけ）で書けないかを先に考える。
  例：`select is_discontinued, discontinued_date, count(*) ... group by 1,2`
- 画面に出す情報も同じ。**利用者に渡すタブレットから他の利用者の名前が見えないこと**は
  設計上の絶対条件（後述の `_kiosk_guard`）。

---

## 2. ファイルの受け渡し（★環境によって2通りある）

### 方法A：デバイスブリッジが使えるとき（推奨）

`device_stage_files` / `device_commit_files` が使えるなら、AIが直接読み書きできる。

- **ローカルパスは `/Users/ZIMAX/dev/kaigo-ai-app`。**
  古い `"/Users/ZIMAX 1/dev/..."` はもう存在しない。
- 読むとき：`device_stage_files` でコンテナへ取り込む
- 書き戻すとき：`SendUserFile` → `device_commit_files`
- **`device_bash`（Mac側のシェル）は起動しない。** git はユーザーに実行してもらう。

### 方法B：ブリッジが使えないとき

**AIが「ダウンロードフォルダにコピーするコマンド」を出す → ユーザーがチャットに添付する。**
これがユーザーの希望する手順。遠慮せずコマンドを投げてよい。

```bash
# 例：見たいファイルをダウンロードフォルダへコピーする
cd "/Users/ZIMAX/dev/kaigo-ai-app"
cp app.py templates/soge_run.html ~/Downloads/
```

- ユーザーは **VSCodeのターミナル**にこれを貼って実行し、
  `~/Downloads` にできたファイルを**このチャットに添付**してくれる。
- 修正版を返すときは、AIがファイルを作って `SendUserFile` で渡し、
  ユーザーが元の場所へ置き換える手順（`cp ~/Downloads/xxx ./templates/`）を案内する。
- **どちらの方法でも、書き戻したあとは必ず `git add` → `git commit` をしてもらう。**

---

## 3. 開発の流れ（毎回これ）

| ブランチ | 用途 |
|---|---|
| `tasukaru-dev` | 開発。ここに push すると DEV にデプロイされる |
| `tasukaru` | **本番**。ここに merge → push すると本番にデプロイされる |

### 通常の1サイクル

```bash
cd "/Users/ZIMAX/dev/kaigo-ai-app"
rm -f .git/index.lock .git/HEAD.lock
git add <触ったファイル>
git commit -m "<マーカー>: <何をしたか>"
git push origin tasukaru-dev
```

### 本番へ出すとき

```bash
cd "/Users/ZIMAX/dev/kaigo-ai-app"
git checkout tasukaru
git pull origin tasukaru
git merge tasukaru-dev
git push origin tasukaru
git checkout tasukaru-dev
```

- **最後の `git checkout tasukaru-dev` まで必ず実行してもらう。**
  本番ブランチに居たまま作業を続けると、開発中のコミットが本番に直接乗る（過去に2回発生）。
- **Cloud Run のデプロイは4〜6分。** 完了はマーカー文字列や画面で判定する。
- git が `index.lock` で止まったら、`rm -f .git/index.lock .git/HEAD.lock` を先頭に付ける。

### ★DDL（テーブル・列の追加）があるとき

**必ず「SQL を先、デプロイを後」。** 逆にすると保存が全部落ちる。

- **本番の施設コードは `cocokaraplus-5526`**（`cocokaraplus` ではない）
- Supabase の実行時に出るダイアログは、**緑の「Run and enable RLS」**を選ぶ。
  黄色の「Run without RLS」は選ばない（2026年8月にRLS対応を済ませてある）。
- DDLは `db/*.sql` にファイルとして残す。すべて冪等（`if not exists`）で書く。

### コミットメッセージ

- **短くする。** 長いと壊れて `Co-ACo-ACo-A...` のようになったことがある。
- 形式：`<マーカー>: <日本語で何をしたか>`（例：`soge-lock-v1: 運行表の確定`）
- 末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` を付ける。

---

## 4. 進め方のルール（ユーザーへの接し方）

- **一度に1タスク。5ステップ以内。具体的な操作手順で。**
  「〜してください」ではなく、貼れるコマンドと押すボタン名を出す。
- **推測で断定しない。** DBの実データ・スクリーンショット・ログで**裏を取ってから**結論を言う。
  「たぶん〜だと思います」で進めない。分からないなら調べる手順を出す。
- 調べた範囲を限定したなら、**結論にもその範囲を明示する。**
- **自分の間違いは、その場ではっきり認めて直す。** 言い訳しない。
  （このセッションでも、私の実装の穴を2つ、自分で見つけて直した。それでよい）
- コードのコメントは**日本語で、「なぜそうしたか」**を書く。
  次に読む人が同じ失敗をしないための情報を優先する。
- **README を育てる。** 大きめの作業をしたら、必ず節を足す。
  ユーザーからも「READMEに記録しておこう」と頼まれる。

---

## 5. ★触ると壊れる／漏れる場所（3つ）

### (1) 利用者セルフ評価の `_kiosk_guard()`（`self_eval_integration.py`）

利用者にタブレットを渡している間、**他の画面へ行けないようにするサーバ側のガード**。
`@app.before_request` で全リクエストを止めている。

- **許可URLは【完全一致】のリストで持つこと。**
  前方一致にすると `/self-eval`（職員の一覧＝**利用者名がずらりと並ぶ**）まで通る。
  実際に一度そうなった。DEVで実測して見つけた。
- 消さない。緩めない。詳細は README の `self-eval-impl-2026-08-20` の「3.」。

### (2) カレンダー通知の予約（`cal-notify-v1`・`templates/base.html`）

**`base.html`（全ページ共通）から動かさないこと。**
TOPページだけに置くと「TOPを開かないと通知が入らない」に逆戻りする。
（「今日の予定」の**帯の表示**だけはTOP限定にしてある。予約は全ページのまま）

### (3) 手で入れた休みに、勤務予定から触らない

`staff_leave_days.source` が `'shift_plan'` 以外（＝手入力）の行は、勤務予定の同期で上書きしない。
詳細は README の `session-68-2026-08-20` の「3.」。

---

## 6. 2026-08-21 にやったこと（このセッション）

**すべて本番反映済み。ユーザーが本番で動作確認して「全部OK」と確認済み。**

### 利用者セルフ評価（タブレットで本人に答えてもらう機能）

| マーカー | 内容 |
|---|---|
| `self-eval-tts-v1` | Google Cloud Text-to-Speech で読み上げ。失敗時はブラウザ読み上げに自動で戻る |
| `self-eval-pen-v1` | **ペンでの手書き入力**。canvas → PNG → Storage。AIで文字起こし（職員が直して保存） |
| `self-eval-pen-v2` | 手書きの紙を画面いっぱいに広げる |
| `self-eval-fit-v1` | 実機の表示崩れを修正（中央ぞろえで上側に戻れなくなる問題） |
| `self-eval-interview-v1` | **職員が聞き取って入力するモード**。タブレットを渡せない方のため |
| `self-eval-status-v1` | ICFの状態欄（達成／一部達成／未達成）を点数から自動入力 |
| `self-eval-ui-v3` | 「作成中」のグルグル表示、項目ごとの色分け |

### 送迎

| マーカー | 内容 |
|---|---|
| `soge-view-v1` | 運行画面の見せ方を「車両から／便から」で選べるように |
| `soge-lock-v1` | **運行表の「確定」**。未確定なら配車表から作り直す／確定したら固定 |
| `soge-leave-v1` | 休み連絡の方を運行表で「お休み」と表示（消さない） |
| `soge-leave-plan-v1` | **休みの人を抜いて予定時刻を計算**＋あとから引き直すボタン |
| `soge-week-tab-v1` | 配車画面を便ごとのタブに（縦に長すぎた） |
| `soge-week-wide-v1` | パソコンでは車を横に並べる |
| `soge-week-autoscroll-v1` | スマホで、つまんだまま画面端に寄せると自動で送る |
| `patient-active-v1` の適用 | 利用中止した方を送迎の各所から除外（過去日は残す） |

### そのほか

- `patient-list-split-v1` … 管理画面の利用者一覧を「来ている方」と「中止した方」に分離
- ハイムリック図（`static/kyukyu/09_heimlich.jpg`）の写り込みを除去

### ★このセッションで見つけて直した「危ない穴」2つ

どちらも私（AI）の実装の抜けで、**本番に出す前に自分で見つけて直した**。
同じことをしないよう、README に理由ごと書いてある。

1. **過去の運行表が、開いただけで書き換わる穴**（`soge-lock-v1` の節）
   「未確定なら作り直す」を素直に書いたため、打刻の無い過去日が
   今の配車表で上書きされる状態だった。→ **過ぎた日は絶対に作り直さない**を追加。
2. **配車画面に、中止した方が残り続ける穴**（同節）
   運行画面にしか除外を入れていなかった。保存済みの週次表（`_soge_saved_week`）は
   「保存した時の顔ぶれ」がそのまま残るので、**そこにも入れる必要があった**。

---

## 7. 次にやること

### ★最優先（2026-08-21 の最後に現場から報告あり・未着手）

0. **送迎ルートの自動設定が、効率の良い順番になっていない。**
   ユーザーの言葉：「ルートの自動設定がうまく機能してないね。効率の良いルート設定ができていない。」
   **まだ原因調査もしていない。ここから始めること。**

   関係するのは `app.py` の以下（`soge-time-v1` / `soge-routeopt-v1`）：
   - `soge_build_week()` … その曜日の送迎表を自動生成する入口
   - `_soge_split_by_time()` … 席数ではなく「戻るまでの時間」で台数を決める
   - `_soge_drive_minutes()` … Routes API で走行時間を出す（結果は `soge_route_time` にキャッシュ）
   - `_soge_planned_times()` … 走行時間を距離比で按分して各家の到着予定を出す
   - `_soge_facility_latlng()` / `soge_geo_map()` … 施設と利用者の座標

   **調べる順番の提案**（推測で断定しないこと）：
   1. 実際に出た順番を1便ぶん、地図に載せて**どう非効率なのか**をユーザーに具体的に聞く
      （遠回り？ 行ったり来たり？ 台数の割り振り？ 時刻がずれる？ — どれかで原因が全く違う）
   2. `soge_geo_map()` で**座標が取れていない利用者が居ないか**を数える。
      座標が無い人は距離順に並べられず、順番が崩れる
   3. 立ち寄り順が「施設からの距離順」で決まっているのか、
      Routes API の最適化（`optimizeWaypointOrder`）を使っているのかをコードで確認する
   4. `soge_route_time` の**キャッシュが古いまま**使われていないか

1. **iPhone実機でのカレンダー通知の鳴動確認**（`cal-notify-v1`）
   → 2026-08-21 時点で「全部OK」と報告あり。**念のため最初に状況を確認すること。**
   まだなら：カレンダー画面の「予定の通知」パネル → 通知ON → 音を選ぶ → 15秒テスト →
   画面をロックして待つ。サイレントスイッチと集中モードの確認を先に。

2. **`CalendarBarConnect` の `gridRect` 宣言順バグ**（`templates/calendar.html`・未修正）
   `__calendarBarConnectRun()` の中で `var gridRect` の宣言が使用箇所より**後ろ**にある。
   var の巻き上げで毎回 TypeError になり、**複数日にまたがる予定の連結バーがずっと出ていない。**
   直すと今より見やすくなるが、今まで無くても回っていた機能。ユーザーに確認してから。

3. **聞き取りモードでの音声入力**（未着手）
   職員が話した内容をそのまま文字にする。セルフ評価の `self-eval-interview-v1` の続き。

### SESSION_67 から引き継いだ運用側の宿題

`SESSION_67_HANDOFF.md` の「3. 次にやること → 残タスク」の1〜9。
OCR実書類テスト、**Stripe Webhook の本番登録**、`CRON_TOKEN`、`PLAN_ENFORCE`、
Apple/APNs、Android内部テスト など。

### 作らないと決めたもの

- **利用者セルフ評価の「印刷」**（本人・家族に見せる形）
  設計時は第3段として置いていたが、**2026-08-21 に現場から不要と回答があった。**
  作らないこと。必要になったら、何のために出すのかを聞くところから始める。

---

## 8. READMEのどこを読むか

`README.md` は長いので、必要な節だけ読めばよい。マーカーで検索できる。

| 知りたいこと | 検索するマーカー |
|---|---|
| 利用者セルフ評価の設計思想 | `self-eval-design-2026-08-20` |
| セルフ評価の実装と落とし穴（**必読**） | `self-eval-impl-2026-08-20`（特に「10.」「11.」） |
| 送迎の確定・休み・利用中止 | `soge-lock-v1` |
| 送迎の見せ方・タブ・ドラッグ | `soge-view-v1` / `soge-week-tab-v1` |
| 勤務予定とカレンダーの連動 | `session-68-2026-08-20` |
| カレンダー通知の仕組み | `cal-notify-v1` |
| 利用者一覧の分離 | `patient-list-split-v1` |

---

## 9. 最初のひとことの例

> `README.md` と `SESSION_69_HANDOFF.md` を読みました。TASUKARUの開発を続けます。
> 前回（8/21）は、利用者セルフ評価の手書き入力と聞き取りモード、
> 送迎の運行表の確定・休み連絡の反映・利用中止の除外まで本番に入れて、
> 動作確認まで済んでいます。
> 今日は何から進めましょうか。残っている宿題は、
> iPhone実機の通知確認、カレンダーの連結バーの不具合、決済まわりの設定です。

**ユーザーに「前回何をしたか」を説明させないこと。** それがこのファイルの目的です。
