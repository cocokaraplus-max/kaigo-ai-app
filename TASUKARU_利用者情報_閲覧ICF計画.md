# TASUKARU 利用者情報：閲覧ページ／ICF運用／重複確認／削除 計画書

作成: 2026-07-22 / 目的: 利用者情報の「見る」タブから、**ICF・担当者会議記録・アセスメントシート**を閲覧でき、
会議記録の重複を防ぎ、不要な記録を削除できるようにする。

---

## 0. 現状の把握（データの持ち方）
- **会議記録（担当者会議・議事録）** = `meetings` テーブル。ICF付箋 = `meeting_icf_links`。
  - 保存 `/api/meeting/save`（**毎回INSERT** ＝「同じ日の記録が何個もできる」原因）。一覧 `/api/meeting/list`、読込 `/api/meeting/get`、PDF `/api/meeting/pdf`。
- **アセスメントシート** = `patient_evaluations` テーブル。保存 `/api/save_patient_evaluation`（**upsert＝上書き型**なので重複しない）。取得 `/api/get_patient_evaluations`。
- **利用者情報ページ** = `patient_info.html`（`/patient-info`）。「**見る／入力**」タブ（patient-hub-v1）があり、**ICF付箋ボードは既にここにある**。
- 会議記録・ICFは録音→文字起こし→議事録生成→ICF分類（`admin_meetings.html`, pro機能）で作られる。

## 1. 置き場所（決定事項）
- 利用者情報の「**見る**」タブの中に、**3つの閲覧ブロック（またはサブタブ）**を並べる：
  1. **ICF**（既存のICFをここに集約）
  2. **担当者会議記録（議事録）**
  3. **アセスメントシート**
- それぞれ「**作成日（日付）が新しい順に並ぶ一覧**」→タップで中身を**閲覧のみ**表示。編集ボタン類は「見る」では出さない。

---

## 2. A. 閲覧ページ（見るタブ・閲覧のみ）
- **UI**：「見る」タブ内に3ブロック。各ブロックに日付リスト（例：2026/07/22、2026/06/10 …）。タップで詳細を読み取り専用で展開/表示。
- **データ取得（既存APIを流用）**：
  - 議事録一覧/詳細 = `/api/meeting/list?patient_id=…` → `/api/meeting/get?meeting_id=…`。
  - アセスメント = `/api/get_patient_evaluations`（作成日で並べる）。
  - ICF = 対象利用者のICF（付箋）を日付付きで表示（下記Bと連動：どの会議・いつのICFかを保持）。
- **閲覧のみの担保**：「見る」タブでは保存/追加/削除ボタンを非表示。編集は「入力」タブ側でのみ。
- PDF出力（`/api/meeting/pdf`）への導線を各記録に付けるかは実装時に判断（閲覧＋PDFはあってよい）。

## 3. B. ICF運用（積み上げ式）
- **狙い**：担会のたびにICFをゼロから作り直すのではなく、**既存のICFを引き継いで“足す”**。
  - 担会を開くと、その利用者の**既存ICF項目を読み込んで表示**。
  - **記入済み（内容がある）項目はスルー**（上書きしない・重複させない）。空いている観点にだけ新規に追加。
  - **利用者情報から手動追加**も可能（「入力」タブでICFに項目を足す）。
- **設計課題（実装時に確定）**：
  - ICFを「利用者単位の最新状態（1本を更新）」で持つか、「会議ごとのスナップショット＋利用者ビューで統合」で持つか。
  - 現状 `meeting_icf_links`（会議単位）と、`patient_info` のICFボード（利用者単位）の関係を精査し、**重複防止のキー（ICFコード/観点単位）**で“記入済みスルー”を実装。
  - → ここはBに入る前に、現物のICFデータを1〜2件見て方式を確定する。

## 4. C. 会議記録の重複確認（上書き / 新規）
- **現状**：`/api/meeting/save` が毎回INSERT → 同じ日の記録が増殖。
- **変更**：保存操作時に「**同じ利用者・同じ日付の記録が既にある**」かを判定し、
  - あれば「**上書きしますか？ 新しい記録として保存しますか？**」を確認（モーダル）。
  - 「上書き」= 既存 `meeting_id` をUPDATE、「新規」= これまで通りINSERT。
- **実装**：保存前に同日既存を照会（`/api/meeting/list` か専用チェック）→ ユーザー選択 → `save` に `mode`（overwrite+meeting_id / new）を渡す。サーバ側でUPDATE/INSERTを分岐。

## 5. D. 一覧から削除
- **追加**：`/api/meeting/delete`（`meeting_id`指定。`meetings` と `meeting_icf_links` を併せて削除＝カスケード）。
- **UI**：会議記録の一覧（「見る」タブ／`admin_meetings` 一覧）に**削除ボタン**。押下時に「本当に削除しますか？」を確認。
- 権限：管理者のみに絞るか、記録者本人も可にするかは実装時に確認。

---

## 6. 実装順（安全に段階リリース）
1. **C（重複確認）＋ D（削除）**：自己完結・即効。まず「増えすぎ・消せない」を解消。
2. **A（見るタブの閲覧）**：議事録・アセス・ICFを日付一覧で閲覧のみ。
3. **B（ICF積み上げ運用）**：最も複雑。実データを見て方式を確定してから実装。

各ステップはこれまで同様、DEVへpush→端末/ブラウザで確認→本番へcherry-pick（本番/DEV分岐のため“まとめマージはしない”／§0-3b運用）。

## 7. 実装前に確定したい細部（Bを中心に）
- ICFは「利用者単位で1本を更新（積み上げ）」で良いか。会議ごとの履歴も残すか。
- 「記入済みスルー」の判定単位（ICFコード単位 / 観点＝b/s/d/e単位）。
- 削除の権限（管理者のみ / 記録者本人も可）。
- 「見る」タブでのPDF出力導線の要否。

---
※この計画に沿って、まずは C＋D から着手予定。決定・変更が出たらこの計画書とREADME/HANDOFFに反映する。
