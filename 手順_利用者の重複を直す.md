# 手順：利用者が二重に登録されたときの直し方

`patients-dup-guard-v1` のLINE通知が届いたときに使う。
**HIROさんが通知をチャットに貼れば、ここから始められる。**

初出：2026-08-28（cocokaraplus-5526 小倉愈さん id 82 / 85）

---

## 0. まず、やってはいけないこと

- **すぐ消さない。** 今日の事故では、**正しい値（火曜・午後）は消そうとしていた側**に入っていた
- **どちらが正しいかを自分で決めない。** 曜日・午前午後・番号は、その事業所しか知らない
- **条件で消さない。** 必ず主キー（id）で名指しする

---

## 1. 通知の読み方

通知は2種類ある。**やることが違う。**

### A「利用者の登録を見送りました」

```
※すでに登録済みかを確かめられませんでした（通信エラー等）。
　二重登録を避けるため作成していません。
```

→ **データは壊れていない。** もう一度操作してもらえば済む。調査は不要。

### B「二重登録を止めました」／「二重に登録されています」

→ 名前か番号が食い違っている。**以下へ進む。**
通知から次の2つを控える。

- `事業所:` … 例 `cocokaraplus-5526`
- `id=` … 例 `82` と `85`

---

## 2. 調べる（selectだけ。まだ何も消さない）

`FCODE` `ID1` `ID2` を通知の値に置き換えて、**1文ずつ**流す。
★SupabaseのSQLエディタは**最後の1文の結果しか返さない**。まとめて貼らないこと。

### ① 2つの行を丸ごと見る

```sql
select * from patients
where facility_code = 'FCODE' and id in (ID1, ID2)
order by id;
```

### ② プロフィールが何件あるか（根っこの確認）

```sql
select id, user_name, patient_number, created_at
from patient_profiles
where facility_code = 'FCODE' and user_name like '%（通知の名前）%';
```

- **1件なら** … patients が二重になっただけ。3へ進む
- **2件以上なら** … 根っこはプロフィール側。patients だけ直しても再発する。
  目標・評価・家族情報がどちらに入っているかを先に調べること

### ③ どのテーブルにぶら下がっているか

★`patient_id` は **text型** に整数が文字として入っている。型では区別できない。
uuid型の列だけは `'82'` を入れられないので対象外。

```sql
with t as (
  select 'patient_visit_days' as テーブル, patient_id from patient_visit_days
  union all select 'vitals',                  patient_id from vitals
  union all select 'body_weights',            patient_id from body_weights
  union all select 'fitness_tests',           patient_id from fitness_tests
  union all select 'life_check_appointments', patient_id from life_check_appointments
  union all select 'life_function_checks',    patient_id from life_function_checks
  union all select 'renraku_notes',           patient_id from renraku_notes
  union all select 'renraku_patient_settings',patient_id from renraku_patient_settings
  union all select 'visit_records',           patient_id from visit_records
  union all select 'vital_daily_excludes',    patient_id from vital_daily_excludes
  union all select 'vital_daily_includes',    patient_id from vital_daily_includes
  union all select 'vital_recheck_schedules', patient_id from vital_recheck_schedules
)
select テーブル, patient_id, count(*) from t
where patient_id in ('ID1','ID2') group by テーブル, patient_id order by 1,2;
```

### ④ 出てきたテーブルの中身を見る

```sql
select * from （③で出たテーブル）
where patient_id in ('ID1','ID2') order by patient_id;
```

---

## 3. 事業所に確認する（★ここは飛ばせない）

④で**中身が食い違っていたら**、その事業所に聞く。

> 「◯◯さんのご利用は、△曜日の午前と午後どちらでしょうか」

**中身が完全に同じなら**確認は要らない。どちらを消しても同じ。

---

## 4. 直す（退避つき・1回で流す）

`STAMP` は日付（例 `20260828`）。`KEEP` は残すid、`DROP` は消すid。
★**残すのは古いほう**（プロフィールと同時に作られた側）が基本。

```sql
begin;

-- ⓪ 退避（あとから戻せるようにする）
create table if not exists _backup_dup_STAMP_patients   as select * from patients where 1=0;
create table if not exists _backup_dup_STAMP_visit_days as select * from patient_visit_days where 1=0;
insert into _backup_dup_STAMP_patients   select * from patients where id in (KEEP, DROP);
insert into _backup_dup_STAMP_visit_days select * from patient_visit_days where patient_id in ('KEEP','DROP');

-- ① 正しい値を「残す側」へ写す（★消す前にやる）
update patient_visit_days set ampm_per_day = '{"曜日":"AM または PM"}'
where id = '（残す側の行のid）';

-- ② 消す側のぶら下がりを消す
delete from patient_visit_days where id = '（消す側の行のid）';

-- ③ 消す側の利用者行を消す
delete from patients where id = DROP and facility_code = 'FCODE';

commit;
```

★③で出たテーブルが `patient_visit_days` 以外にもあったら、
**そのぶんの退避と付け替えを①②に足すこと。** 消すのは最後。

---

## 5. 確かめる

```sql
select p.id, '[' || p.user_name || ']' as 名前, length(p.user_name) as 文字数,
       v.weekdays, v.ampm_per_day
from patients p
left join patient_visit_days v on v.patient_id = p.id::text
where p.facility_code = 'FCODE' and p.user_name like '%（名前）%'
order by p.id;
```

- 行が**1つだけ**になっていること
- 中身が**確認した内容**になっていること
- 文字数が名前の長さと合っていること（見えない空白が無いか）

退避表は、しばらく置いてから消す。

---

## 6. なぜ起きるのか（原因を見失わないために）

`patients` と `patient_profiles` は **氏名で結ばれている**。idで結ばれていない。
だから氏名が一時的にでも食い違うと橋が切れ、新しい行ができる。

`patients-dup-guard-v1` で**カルテ番号でも照合する**ようにしたので、
氏名だけ食い違うケースは止まる。それでも通知が来たということは、
**氏名も番号も両方食い違っている**ということ。そこを事業所に確認する。

★根本的には `patients` に `patient_profile_id` を持たせて
**idで結ぶ**のが正しい。まだやっていない。
