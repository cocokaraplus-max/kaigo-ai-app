# TASUKARU 開発 引き継ぎメモ（2026-06-05 セッション末 v2）

このメモは `HANDOFF_2026-06-05.md`（v1, コミット `170bbde`）の続き。v1 の §0〜§2（基本情報・作法・ブランチ取り違えの教訓）は引き続き有効。本 v2 では、v1 作成後に積んだ追加の本番反映（8コミット）と、新たな教訓・繰り越しタスクを記録する。次セッションは v1 → v2 → README の順で読めば現状を把握できる。

---

## 0. まず読むもの
- `HANDOFF_2026-06-05.md`（v1）… 基本情報・作法・§2-6 ブランチ取り違えの教訓。
- このメモ（v2）… v1 以降の追加修正・新教訓・繰り越し（生活機能チェック、開発者MENU 2機能）。
- `README_tasukaru_dev.md`。

作法・本番反映手順・セキュリティ・冪等パッチの考え方は v1 §2 を踏襲。要点の再掲: コミット前に必ず `git branch --show-current` で `tasukaru-dev` を確認。本番マージ後は必ず `git checkout tasukaru-dev` で戻る。

---

## 1. v1 以降に本番反映した追加修正（時系列・dev上のハッシュ）

すべて DEV 確認 → 本番マージ済み（休み連絡のみ実データ依存のため現場確認に回す前提で反映）。

1. `7652096` **体力・体重記録: 保存後の次アクションボタン**（fitness.html）
   - 保存成功時に「✅保存しました」＋2ボタン（別の利用者を入力／同じ利用者で続ける）を表示。
   - 入力欄クリアは既存処理を活用。利用者リセットは `resetFitPatient()` 新設（FIT_SELECTED=null・hidden/input空・hint非表示・保存ボタンdisabled・履歴を初期表示に戻す・検索欄focus）。`showSaveNextActions(msgId)` で innerHTML にボタン描画（setMsg は textContent なので別関数化）。

2. `33b7541` **本日の記録: 編集パネルを個別レコード単位に**（vitals.html）
   - 症状: 既存測定を修正保存すると `invalid input syntax for type uuid: "undefined"`。
   - 原因: 編集パネルが参照する `cachedData.items` に 10分枠の**合成レコード**（`groupVitalWindows` の merged）を入れていた。合成レコードは `id` を持たず（個別idは `_ids` 配列）、`v.id=undefined` → `update_vital` に id:undefined。
   - 修正: 3051行 `cachedData[p.pid] = { name, items: (p._rawItems || p.items) }` に変更。`_rawItems`（グループ化前の生レコード、idあり）を編集パネルに使う。表示テーブルは枠版を別途使うので ●バッジ・履歴モーダルは無傷。修正・削除・追加すべて実機OK。

3. `c72573f` **TOP左上の目標期限確認: モーダル重複解消＋早期return修正**（base.html）
   - 症状: 目標変更しても「変更なし」、期限切れも表示されない。
   - 原因1: `goal-check-modal` / `goal-check-content` が base.html 内に**2つ重複定義**（コメントごとコピペ）。id重複で getElementById が1つ目を掴む一方、空の2つ目が上に重なり中身が見えない。
   - 原因2: `openGoalCheckModal` 冒頭で `patients.length===0` だと return → 目標変更だけある月に何も出ない。
   - 修正: 重複モーダル(2個目)削除＋判定を `patients も goal_changes も両方0` に変更。
   - 注意: API `/api/goal_check`（app.py 6551〜）・データは正常だった。表示側のバグ。Chrome連携で `/api/goal_check` を叩いて「APIは正常・表示で落ちている」と切り分けたのが早かった。

4. `203abb7` **目標変更のゴミ入力足切り**（app.py `/api/goal_check`）
   - 症状: 変更していない人に「あ」だけの目標変更が表示。
   - 原因: `new_goal = tg or sg or lg; if new_goal:` が1文字でも True。本番の `patient_evaluations` にテスト「あ」が残存（DEVには無く再現せず＝本番データ差）。
   - 修正: 各値 `.strip()` し `if new_goal and len(new_goal) >= 2:`。表示時の足切り（元データは消さない）。マーカー `goal-minlen-fix`。

5. `9516a05` **目標確認ボタンの色分け＋跳ねアニメ**（base.html）
   - 期限切れ→赤(#e53935)／期限間近→黄(#f57c00)／目標変更→緑(#34a853)。優先度 赤>黄>緑。該当時 `.goal-bounce`（`@keyframes goalBounce` 1.6s 無限ループ）でアイコンが跳ね続ける。`openGoalCheckModal` 冒頭で `.goal-bounce` 除去（開いたら止まる）。`closeGoalCheckModal` の重複定義(2つ)も1つに解消。マーカー `goal-bounce-css`/`goal-bounce-js`/`goal-bounce-stop`。
   - Chrome計測で `has_bounce_class:true`/`animationName:goalBounce`/`icon_color:rgb(229,57,53)` を確認、開くと false 化も確認済み。

6. `7848894` **休み連絡編集: 理由だけ変更でも content 再生成＋カレンダー同期**（app.py `/api/update_record`）
   - 症状: ケース記録から休みの理由を変更しても保存しても元に戻る／カレンダーも変わらない。
   - 原因: content 再生成・カレンダー同期が `if new_leave_start:`（日付が送られた時のみ）の中だけにあった。理由だけ変更だと `new_leave_start` が無く、`leave_reason` カラムは更新されるが content（表示本文）が古いまま＝戻って見える。
   - 修正: 休み連絡の編集（`leave_reason`/`leave_reporter_type` 送信時）で日付未送信なら、既存レコードの `leave_date_start/end` を select して `new_leave_start` を補完 → 既存の再生成・同期ロジックが動く。マーカー `leave-edit-reason-sync`。
   - 既知の残: カレンダーイベントの **memo**（本文）同期は既存コードに無い（event_date/end_date/title のみ同期）。理由変更を memo にも反映したいなら別途追加が必要。現場確認の結果次第。

7. `0346539` **TOP自分のタスク: クリックで該当タスクへ遷移＋ハイライト**（top.html, tasks.html）
   - TOPタスク行に `onclick="location.href='/tasks?focus={{ t.id }}'"`。tasks.html の `loadTasks` で `renderTasks` 後に URL の `focus` を読み、`.task-card[data-id="<id>"]` へ `scrollIntoView`＋`.focus-highlight`（`@keyframes taskFocusGlow` 2s×2、4.2秒後にクラス除去）。タスクカードは元々 `data-id` 保持。マーカー `task-focus-glow`/`task-focus-js`/`task-row-clickable`。

8. `54ef6f6` **TOP自分のタスク: 誕生日カード風の見やすい表示**（app.py, top.html）
   - app.py /top（777〜）で各 my_task に `priority_label`(高/中/低)・`due_urgent`(7日以内)・`overdue`(期限切れ) を付与。マーカー `top-task-meta`。
   - top.html: タスク行を `.top-task-card` のカード風に。優先度バッジ `.top-task-prio.prio-high/medium/low`(赤#ea4335/黄#f9a825/緑#34a853)、期限 `.top-task-due`(近い=橙 due-urgent、過ぎた=赤 due-overdue「期限切れ」)。クリック遷移＋ハイライト(7)は維持。マーカー `top-task-card`/`top-task-css`。

---

## 2. 今回得た教訓（v1 §2 に追加）

- **Chrome連携での原因切り分けが強力**: 「APIは正常だが表示で落ちている」のような問題は、DEVタブで `fetch('/api/...')` を叩いて生レスポンスを見れば一発。`getComputedStyle`/`classList.contains`/`getBoundingClientRect` で見た目の状態も計測できる。認証・保存系の操作はユーザーに任せ、Claudeは読み取り計測のみ。
- **DEVと本番のデータ差に注意**: 「あ」のゴミ入力は本番のみ存在しDEVで再現しなかった。データ起因のバグはコード側の足切り（防御的バリデーション）で恒久対策するのが堅実。
- **id重複は表示を静かに壊す**: 同一idの要素が2つあると getElementById は1つ目だけ返す。後ろの空要素が重なって「中身が出ない」症状に。`grep -c 'id="..."'` で重複検知が有効。base.html はコピペ起因の重複が複数あった（goal-check-modal、closeGoalCheckModal）。
- **作成パスと編集パスのロジック非対称**: insert（新規）には content 再生成・カレンダー同期・全フィールドがあるのに、update（編集）に欠けている、という非対称が複数のバグ源（休み連絡、本日の記録）。編集系を直すときは「作成系と同じ処理が編集系にもあるか」を必ず確認。
- **副作用を if 条件のスコープに閉じ込めない**: 休み連絡バグは「content再生成とカレンダー同期が `if new_leave_start:` の中だけ」だったのが原因。複数のトリガー（日付変更・理由変更）がある処理は、条件を広げるか前段で値を補完する。
- **冪等マーカーは挿入文字列に必ず含める**（v1 既出だが再発: `/* below-gap-fix */` 問題）。今回の各パッチはマーカーを挿入文字列内の実在文字列にして回避。
- **アンカーは `cat -et` / `sed -n | cat -et` で不可視文字確認**: closeGoalCheckModal 重複解消で、2関数の間に空行1行があってアンカー不一致 → `cat -et` で `$`（空行）を見て解決。
- **10分枠の合成レコードは id を持たない**: `groupVitalWindows` の merged は `_ids` 配列に個別idを退避。編集・削除など id が要る処理では生レコード（`_rawItems`）を使う。
- **Cloud Run デプロイ中は一時的にサイトが無応答**: デプロイ直後に「サイトが開かない」と見えても数十秒〜数分で復帰。慌てず待ってからハードリロード。py_compile が通っていれば構文起因ではない。

---

## 3. 繰り越しタスク（次にやる新規開発）

### 3-1.【最優先・本題】生活機能チェックシート（別紙様式3-2）のペーパーレス化
- 厚労省 別紙様式3-2 = 通所介護の個別機能訓練加算で使用。居宅訪問でADL/IADL等を確認し、目標設定・個別機能訓練計画の作成前と、その後3か月ごとに1回以上の居宅訪問で記録。
- 構成: 基本情報（氏名・生年月日・性別・評価日・評価者・職種・要介護度・障害高齢者/認知症高齢者の日常生活自立度）＋ ADL 10項目（Barthel Index形式・点数つき: 食事/移乗/整容/トイレ/入浴/歩行/階段/更衣/排便/排尿、各レベル＋課題有無）＋ IADL 3項目（調理/洗濯/掃除）＋ 基本動作 5項目（寝返り/起き上がり/座位/立ち上がり/立位）。
- **ユーザーが絞ったフェーズ1**:
  1. 入力をスマホで楽に（タップ選択式・前回値プリフィル・スコア自動合計）
  2. 在宅アセスメント（居宅訪問）の**1か月前アラート**（3か月ごとの訪問を忘れない）
- 設計方針: バイタル/体力測定と同じ「入力→蓄積→履歴→書類出力」パターン。Supabase に専用テーブル（例 `life_function_checks`）、app.py に保存/取得API、専用入力ページ新設。1か月前アラートは既存の再検査リマインダー基盤（`recheck_schedule`、vitals.html 2583付近）が流用候補。
- ステップ案: A) データ設計＋入力画面モック → B) API＋入力画面 → C) 1か月前アラート → D) 書類出力(様式3-2)。フェーズ2で興味関心チェック/個別機能訓練計画との連動。
- 着手前の確認コマンド（未取得）: `grep -rln "assessment\|生活機能\|居宅訪問\|個別機能訓練\|興味.*関心" templates/*.html app.py | grep -v bak`、既存アラート/期限/スケジュール基盤の確認。

### 3-2. 開発者MENUからアップデート情報を更新できるように
- 現状の「更新ログモーダル」（top.html 72行付近）は内容がハードコード。これを開発者MENU（`/dev`）から編集・追加できるようにする。
- 想定: Supabase に更新情報テーブル（例 `app_updates`: title, body, published_at）、`/dev` に投稿フォーム、TOPの更新ログモーダルがテーブルから読む。
- 未着手。設計から。

### 3-3. 全施設へのポップアップメッセージを開発者MENUから投稿
- 開発者MENUから投稿すると、各施設がアプリを開いた時にポップアップでメッセージ表示。
- 想定: Supabase にお知らせテーブル（例 `global_notices`: message, active, created_at, 表示期間など）、`/dev` に投稿UI、base.html 等で全ページ共通に「未読のアクティブなお知らせがあればポップアップ」。既読管理（ユーザー/施設ごとに一度閉じたら出さない）をどうするか要設計。
- 3-2 と「開発者MENUから投稿 → 各施設で表示」という構造が共通。まとめて設計すると効率的。
- 未着手。設計から。

---

## 4. その他の既存残タスク（v1 §4 から継続）
- 測定UI第3段（進捗バー・済/未の色分け・カード下段の表示切替）。
- 満足度の「None」表示（print_preview 1656/1660、`ev.get('satisfaction') or '未記入'`）。優先度低・見送り中。
- 写真の横並び（print_preview。グラフresponsive化で連動改善したか実機未確認）。
- ガイド目次(toc)に10分判定セクション `s-vitals-window` リンク追加（任意）。
- 無音しきい値 `BULK_VOICE_RMS_THRESHOLD = 0.05`（vitals.html）の現場フィードバック反映。
- 決済(Stripe)・LINE連携の残（README §5-4）。掲示板「さらに読み込む」、utils の整理。
- base.html のコピペ重複が他にも無いか一度棚卸し（goal-check 系で2種の重複が見つかった前例あり）。

---

## 5. 次セッションの最初の一手（推奨）
1. v1・v2・README を読む。
2. `git status --short && git branch --show-current`（`tasukaru-dev`・クリーン確認）。
3. 本題の 3-1（生活機能チェック）に着手するなら、まず §3-1 の確認コマンドで既存資産（assessment 機能・recheck_schedule の通知基盤）を把握 → データ設計＋入力画面モックから。
4. パッチは grep/sed で現物確認 → 冪等パッチ → サンドボックス検証（py_compile / node --check / 適用後構文 / 冪等性 / div balance・Jinja for-endfor/if-endif 対応）→ DEV反映 → 実機確認 → 本番反映。
5. **本番反映後は必ず `git checkout tasukaru-dev`**（v1 §2-6）。
