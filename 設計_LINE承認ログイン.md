# 設計：LINE承認ログイン（共有PC向け）

marker: `line-approve-login-v1`
作成 2026-08-28 ／ **まだ1行も作っていません。読んで決めてから作ります。**

---

## 1. 何を作るか

共有PCで、**名前を選んで「ログイン」を押すと、本人のLINEに承認ボタンが届く。**
本人が押せばPCが開き、**30分さわらなければ自動で降りる。**

パスワードを毎回打たない。覚え書きも要らない。それでも**本人しか入れない。**

---

## 2. 使う人から見た流れ

```
共有PC                                     本人のスマホ（LINE）
──────────────────────────────────────────────────────────────
[ 山田 太郎 ▼ ]  ← 名前を選ぶ
[    ログイン   ]
       ↓
┌─────────────────────┐              ┌──────────────────────────┐
│ 承認をお待ちください   │              │ 【TASUKARU】ログイン承認  │
│                     │   ──────→   │ 事務所PC（受付）          │
│   確認番号           │              │ 8月28日 14:32            │
│    4 7 2 9          │              │                          │
│                     │              │  確認番号  4 7 2 9        │
│ スマホのLINEを       │              │                          │
│ ご確認ください        │              │ ［ 承認する ］            │
│                     │              │ ［ 心当たりがない ］       │
│      (2分30秒)      │              └──────────────────────────┘
└─────────────────────┘                        ↓
       ↑                                「承認する」を押す
       └────────────────────────────────────────┘
       ↓
   ログイン完了 → いつもの画面
       ↓
   30分さわらなければ自動ログアウト
```

### ★確認番号を両方に出すのが肝

画面とLINEに**同じ4桁**を出す。番号が合っていることを見てから押してもらう。

これが無いと、**別の誰かが自分の名前でログインしようとしたときに、うっかり承認してしまう**。
番号が違えば「心当たりがない」を押す。押されたら、その要求は即座に無効になる。

---

## 3. 安全のための決めごと

| 決めごと | 理由 |
|---|---|
| **承認は3分で切れる** | 放置された承認リンクが後から使われないように |
| **1回きり** | 使ったら即無効 |
| **リンクを開いただけでは通らない** | 開いた先で**もう一度「承認する」**を押す（POST）。誤タップやプレビュー取得で通らないように |
| **PC側とLINE側で違う鍵を持つ** | LINEに送る鍵（approve_token）では**ログインできない**。PCが持つ鍵（poll_token）でしかセッションは作れない。メッセージを転送されても乗っ取られない |
| **職員名の一覧は、許可された端末にだけ返す** | ★URLを知っている人が誰でも職員名を並べられる状態を作らない |
| **連投を止める** | 同じ人への承認要求は30秒に1回、1時間に10回まで。LINEを埋め尽くす嫌がらせを防ぐ |
| **失敗の記録** | 既存の `login_attempts` と同じ形で数える |

---

## 4. 端末の登録（最初の1回だけ）

**打刻端末（`timecard_devices`）とまったく同じやり方**にする。職員も見慣れている。

1. その端末で開くと、印（トークン）ができる → `localStorage`
2. 「この端末を使えるようにしてください」と申請
3. **管理者が管理画面で許可**する（名前を付けられる：「事務所PC（受付）」など）
4. 許可された端末だけが、職員名の一覧を出せる／承認ログインを使える

管理者は**一覧を見て、いつでも取り消せる**。端末を入れ替えたとき・失くしたときはここで切る。

### ★ログインしたあとに「この端末はどちらですか」と聞く

初めてその端末でログインした直後に、1回だけ出す。

```
┌──────────────────────────────────────┐
│ このパソコンはどちらですか？            │
│                                      │
│ ○ みんなで使う（共有）                 │
│    30分さわらないと自動でログアウト     │
│    します。次の人が安心して使えます。    │
│                                      │
│ ○ 自分だけが使う（専用）               │
│    自動ログアウトしません。              │
│    そのまま使い続けられます。            │
│                                      │
│ ※あとから変えられます                  │
│              [ 決定 ]                 │
└──────────────────────────────────────┘
```

- **既定は「共有」。** 閉じても・後回しにしても**共有のまま**＝30分で落ちる側に倒す。
  ★逆にすると、設定し忘れた端末が入りっぱなしになる。**安全な側を既定にする。**
- あとから変えられる（設定画面 と 管理者の端末一覧、どちらからでも）
- 管理者の端末一覧に**「共有／専用」が出る**ので、事務所のPCが勝手に「専用」に
  なっていないかを見て、戻せる

| 種別 | 自動ログアウト | セッション | 想定 |
|---|---|---|---|
| **共有** | **30分** | 30分 | 事務所PC・受付・共用タブレット |
| **専用** | **しない** | 30日（今まで通り） | 個人のPC・自分のタブレット |

★どちらでも、**ログインのたびにLINEの承認は要る**。
専用PCはセッションが30日もつので、承認を求められるのは月に1回くらい。
これが「専用PCでは無制限に使える」の中身。

---

## 5. 30分で自動ログアウト

### ★ここに落とし穴がある（調べて分かったこと）

`base.html` に、**全画面で30秒ごとに走る通信**がある。

```js
setInterval(checkUnreadMessages, 30000);   // → /api/board/unread_count
```

未読バッジの更新。**画面を開いているだけで通信が飛ぶ。**

もし「通信が来たら操作中」と作ると、**誰も触っていないPCが永遠にログインしたまま**になる。
しかも**エラーが出ない**ので、動いているつもりで放置される。

### だから、こう作る

> **既定は「操作していない」。**
> 本当に人が触ったとき（クリック・キー入力・タップ・スクロール）だけ、
> 画面から「操作しました」と伝える（`/api/session/touch`・最大1分に1回）。

こうすれば、**あとから定期通信が増えても壊れない。** 逆に作ると、増えるたびに黙って壊れる。

### 動き

| 何分 | 何が起きるか |
|---|---|
| 0〜29分 | ふつうに使える |
| 29分 | 「まもなくログアウトします」＋［まだ使っています］ボタン |
| 30分 | ログアウト。名前選択の画面に戻る |

- **「共有」と設定された端末だけ**30分。**「専用」の端末は自動ログアウトしない**（30日）
  （そうしないと自分の端末でも30分で落ちて、使い物にならない）
- 画面側だけで測ると**閉じた端末が落ちない**ので、サーバ側にも最終操作時刻を持つ
- ★種別は**サーバ側（`login_devices.is_shared`）で判定する**。
  画面側の値だけで決めると、ブラウザをいじれば自動ログアウトを外せてしまう

### ★一時退避保存（HIROさん判断で作る）

30分で落ちるとき、**記録を書きかけだと消える。** 現場でいちばん怒られるやつです。

**作り方**

- 入力欄をさわっている間、**その端末のブラウザに書きかけを退避**しておく（数秒おき）
- 自動ログアウトの直前にも、もう一度退避する
- 同じ人が入り直したら「**書きかけが残っています。戻しますか？**」と出す
- ★**別の人が入ったときには出さない。** 共有PCなので、名前が違えば見せない
- 戻したら退避は消す。7日たっても戻されなければ捨てる

**どこに退避するか → サーバ（HIROさん判断）**

表 `draft_autosaves` を1本作る。どの端末からでも戻せる。

★サーバに置くぶん、気をつけることが増える。

| 気をつけること | なぜ |
|---|---|
| **必ず `staff_name` で絞る** | 共有PCなので、間違えると**他人の書きかけが見えてしまう** |
| **7日で捨てる掃除を入れる** | 書きかけの記録がサーバに溜まりっぱなしにならないように |
| **戻したら消す** | 古い書きかけが後から出てこないように |
| 中身は利用者の記録そのもの | 通常の記録と同じ慎重さで扱う |

★これは自動ログアウトと**同時に作る**。片方だけ出すと事故になる。

---

## 6. DDL（★コードより先に流す）

**別ファイル `db/ddl_line_approve_login_v1.sql` に出してあります。**
（設計書に二重に書くと、片方だけ直して食い違うので、実体は1か所に置く）

表は3本。

| 表 | 何のため |
|---|---|
| `login_devices` | 使える端末の登録。共有／専用、管理者の許可、取り消し |
| `login_requests` | 承認待ち。★鍵を2本に分ける（LINEに送る鍵ではログインできない） |
| `draft_autosaves` | 書きかけの一時退避。★`staff_name` で必ず絞る |

★緊急ログインのON/OFFは、既存の `admin_settings` に持つ（`key = 'emergency_password_login'`）。
**表は増やさない。**

★`login_requests` と `draft_autosaves` は放っておくと溜まる。**掃除の処理を別途入れる。**

★確かめていないこと：このプロジェクトが他の表で RLS を使っているかどうか。
使っているなら、この3本にも同じポリシーが要る。

---

## 7. 画面とAPI

| URL | 何をするか | 守り |
|---|---|---|
| `GET /shared-login` | 名前を選ぶ画面 | 許可された端末のみ。未登録なら申請画面 |
| `POST /api/shared-login/device/request` | 端末の登録申請 | なし（申請するだけ。使えるのは承認後） |
| `GET /api/shared-login/names` | 職員名の一覧 | **許可された端末のみ** |
| `POST /api/shared-login/request` | 承認要求を作り、LINEへ送る | 許可された端末のみ・連投制限 |
| `GET /api/shared-login/poll` | 承認されたか確認（PCが数秒おきに聞く） | `poll_token` |
| `GET /login/approve?t=…` | LINEから開く承認ページ（★まだ承認しない） | `approve_token` |
| `POST /login/approve` | ここで初めて承認 or 拒否 | `approve_token` |
| `POST /api/session/touch` | 「人が操作した」と伝える | ログイン中のみ |
| `POST /api/shared-login/device/kind` | この端末を「共有／専用」に設定 | ログイン中のみ。**サーバ側に保存** |
| `GET /admin/login-devices` ほか | 端末の一覧・許可・取り消し・共有／専用の変更 | 管理者のみ |

★`GET /login/approve` は**画面を出すだけ**。承認は `POST` で。
これでリンクのプレビュー取得や誤タップでは通らない。

---

## 8. LINEに送るメッセージ

いまLINEには**文字しか送っていない**（`line_send_message` はテキスト専用）。
ボタン付き（Flex Message）を送る関数を**新しく1つ足す**。既存の送信は触らない。

```
┌────────────────────────────┐
│ TASUKARU ログイン承認        │
├────────────────────────────┤
│ 端末   事務所PC（受付）      │
│ 日時   8月28日 14:32        │
│                            │
│      確認番号               │
│      4 7 2 9               │
│                            │
│ 画面の番号と同じことを       │
│ 確かめてから押してください    │
├────────────────────────────┤
│ [    承認する    ]          │
│ [ 心当たりがない ]          │
└────────────────────────────┘
```

---

## 9. ★危ないところ（業務が止まる経路）

| もし起きたら | どうなる | 備え |
|---|---|---|
| LINE未連携の職員がいる | **その人は入れない** | 事前に0人であることを数える。移行期間中は旧方式も残す |
| LINEの送信が失敗する | 承認が来ない＝入れない | 画面に「送れませんでした」と出し、**旧方式（パスワード）への入口を残す** |
| スマホを忘れた・電池切れ | 入れない | 同上。旧方式が逃げ道 |
| 通信障害でLINEが遅い | 待たされる | 3分で切れるので、押し直せばやり直せる |
| 30分ログアウトで書きかけが消える | 記録が飛ぶ | §5の下書き保存を**同時に**作る |
| 定期通信を「操作」と数えてしまう | **自動ログアウトが1度も動かない** | §5のとおり「既定は操作していない」で作る |

★**旧方式は残す。ただし普段は閉じる**（§12・案C）。
そして**閉じたときに詰まないよう、逃げ道を3本**用意する（§12-2）。

---

## 10. 作る順番（提案）

出す単位を分ける。**1つ出すごとにDEVで確かめてから次へ。**

| # | 出すもの | 出しても壊れないか |
|---|---|---|
| 1 | **DDL 3本**（表を作るだけ） | コードは何も使わない。安全 |
| 2 | **端末の登録と管理画面** | 既存のログインに触らない。安全 |
| 3 | **承認ログインの本体**（`/shared-login`） | **新しいURL**。既存の `/login` に触らない。安全 |
| 4 | **30分自動ログアウト＋一時退避保存** | ★ここだけ既存画面に触る。慎重に |
| 5 | **旧方式を閉じる（緊急スイッチ・自動開放）** | ★最後。ここを先にやると詰む |

★1〜3は、既存のログインを**1行も変えずに**作れます。
4だけが `base.html` に触るので、そこは分けて出したい。

---

## 11. 決まったこと（2026-08-28・HIROさん判断）

| # | 決定 |
|---|---|
| 1 | **旧方式（施設コード＋パスワード）は残す。** LINEが止まった日に事業所全体が入れなくなるのを避ける |
| 2 | **最初の端末登録は管理者の承認が要る。** 個人の端末も同じ |
| 3 | **共有端末の自動ログアウトは30分** |
| 4 | **一時退避保存をつくる**（書きかけを失わない） |
| 5 | **個人の端末を許可するかどうかは、端末登録のときの管理者の判断による** |

---

## 12. 旧方式は「普段」どうするか（★要判断）

残すのは決まった。問題は**普段どれくらい見えているか**。ここが安全の要になる。

★**旧方式が普段からふつうに使えると、LINE承認をつけた意味がほとんど無くなる。**
パスワードを知っている人は、承認を通らずに入れてしまうから。

### 案

| 案 | 普段の見え方 | 安全度 | 現場の楽さ |
|---|---|---|---|
| **A. 小さなリンクで残す** | 画面の下に「うまくいかないときはこちら」 | △ | ◎ |
| **B. Aに加えて、使われたらLINEに知らせる** | 同上＋本人へ「パスワードでログインされました」 | ○ | ◎ |
| **C. 管理者が緊急時にONにする（既定OFF）** | 普段は入口が無い。管理者が開けて初めて使える | ◎ | △ |

### 決定：C（HIROさん判断）

普段は入口を出さない。管理者が開けたときだけ使える。

### 12-2. ★Cには「詰む道」がある。逃げ道を3本用意する

調べたところ、**開発者用の入口も含めて、すべての入口が `@login_required` の内側**にある。

```python
@app.route('/dev_login')
@login_required          # ← ここ
```

つまりCのままだと：

```
LINEが止まる → 誰もログインできない → 旧方式を開ける画面にも入れない → 詰み
```

**旧方式が要るのはまさにLINEが止まった日**なのに、その日に開けられない。だから逃げ道を3本置く。

| # | 逃げ道 | ログイン要る？ | 速さ | いつ |
|---|---|---|---|---|
| 1 | **開発者MENUのスイッチ** | 要る | 即時 | 普段の運用。「今日は開けておく」 |
| 2 | **LINE送信が続けて失敗したら自動でON** | 不要（自動） | 自動 | LINEが止まった日 |
| 3 | **SupabaseのSQLエディタで1行流す** | **不要** | 約10秒 | 誰も入れないとき |

★スイッチの実体は**DB（既存の `admin_settings`）に置く**。
だから3番が成り立つ。環境変数だとデプロイに3〜6分かかるうえ、
アプリの外から変えるのに手間がかかる。**DBに置けば、Supabaseから直接開けられる。**

**緊急時に流すSQL（手元に控えておく）**

```sql
-- 開ける
update admin_settings set value = 'on'
 where facility_code = 'cocokaraplus-5526' and key = 'emergency_password_login';

insert into admin_settings (facility_code, key, value)
select 'cocokaraplus-5526', 'emergency_password_login', 'on'
 where not exists (
   select 1 from admin_settings
    where facility_code = 'cocokaraplus-5526' and key = 'emergency_password_login');

-- 閉じる
update admin_settings set value = 'off'
 where facility_code = 'cocokaraplus-5526' and key = 'emergency_password_login';
```

★2本に分かれているのは、`admin_settings` に `(facility_code, key)` のユニーク制約が無いため。
既存のコード（`api_update_hist_limit` など）も同じ書き方をしている。

### 12-3. 開発者MENUに足すもの

```
┌────────────────────────────────────────────┐
│ 緊急ログイン（施設コード＋パスワード）          │
│                                            │
│   状態    ● 閉じています                    │
│                                            │
│   [ 24時間だけ開ける ]                       │
│                                            │
│ ※開けると全管理者のLINEに通知します           │
│ ※24時間で自動的に閉じます                    │
│ ※LINE送信が続けて失敗したときは自動で開きます   │
└────────────────────────────────────────────┘
```

- 開けたら**全管理者のLINEに通知**（勝手に開けられたら気づける）
- **24時間で自動的に閉じる**（開けっ放し防止）
- 施設ごとに持つので、他の事業所には影響しない

★旧方式は、開いている間は**登録された端末かどうかに関係なく使える**ようにしておく。
「端末が壊れて新しいPCから入りたい」が、いちばん困る場面だから。

---

## 13. ガイド・手引書に載せる文章（★画面ができたときに入れる）

★**いま `guide_integration.py` に入れないこと。**
`check_guide.py` は app.py のルートと突き合わせるので、**まだ無い画面の案内を入れるとズレとして報告される**。
画面ができた回で、コードと一緒に入れる。

### 13-1. `guide_integration.py` の SCREENS に足すもの

```python
    # line-approve-login-v1
    "/admin/login-devices": {
        "title": "ログインできる端末の管理",
        "what": "どのパソコン・タブレットからログインできるかを決める画面です。"
                "新しい端末は、ここで許可するまで使えません。"
                "★職員の名前の一覧は、許可した端末にしか出ません。",
        "flow": [
            "職員が端末から「使えるようにしてください」と申請する",
            "この画面に申請が出るので、どの端末か確かめる",
            "分かる名前を付けて許可する（例：事務所PC（受付））",
            "使わなくなった端末・失くした端末は取り消す",
        ],
        "fields": {
            "ld-label": {
                "t": "端末の名前",
                "d": "あとで見て分かる名前を付けてください。"
                     "「事務所PC（受付）」「送迎車のタブレット」など。"
                     "★取り消すときに、どれを消すか迷わなくなります。",
            },
            "ld-kind": {
                "t": "共有／専用",
                "d": "みんなで使う端末は【共有】。30分さわらないと自動でログアウトします。"
                     "自分だけが使う端末は【専用】。自動ログアウトしません。"
                     "★事務所のパソコンが【専用】になっていたら、【共有】に戻してください。"
                     "誰かが入りっぱなしになります。",
            },
            "ld-revoke": {
                "t": "取り消す",
                "d": "★端末を失くしたとき・盗まれたときは、すぐここで取り消してください。"
                     "取り消すと、その端末からはログインできなくなります。",
            },
        },
        "checks": ["login_device_pending"],   # ← 承認待ちの端末があれば知らせる（新しく作る）
    },
```

★`checks` の `login_device_pending` は**新しく作る判定**。
「承認待ちの端末が n 台あります」を出す。既存の12種類と同じ形で足す。軽い問い合わせ1回で済む。

### 13-2. 手引書（`templates/manual.html`）の「ログイン」の節

いまの節（`id="s-login"`）は **施設コード＋パスワード**で書かれている。書き換える。

**新しい流れ図**

```
名前を選ぶ  →  ログインを押す  →  LINEの承認を押す  →  ログイン完了
```

**入れる文章**

- 「共有のパソコンでは、**パスワードは要りません**。自分の名前を選んで［ログイン］を押すと、
  **あなたのLINEに承認のお知らせ**が届きます。」
- 「★**画面の4桁とLINEの4桁が同じことを必ず確かめてから**、［承認する］を押してください。
  番号が違うときは、**あなた以外の誰かがあなたの名前でログインしようとしています**。
  ［心当たりがない］を押してください。」
- 「みんなで使うパソコンは、**30分さわらないと自動でログアウト**します。次の人が安心して使えます。」
- 「書きかけの記録は**自動で預かります**。入り直すと『書きかけが残っています。戻しますか？』と出ます。」
- 「自分だけが使うパソコンは【専用】にしておくと、自動ログアウトしません。」
- 「スマホが手元にない・LINEが届かないときは、**施設の管理者に連絡してください**。」

★**旧方式（施設コード＋パスワード）は手引書に書かない。**
普段は閉じていて、管理者が開けたときだけ使えるものなので、
書くと「使えるはずなのに使えない」と現場が混乱する。
管理者向けの説明にだけ書く。

### 13-3. ★ついでに見つけた、手引書の間違い

いまの手引書の流れ図に **「名前入力（任意）」** という段がある。

```
施設コード入力 → パスワード入力 → 名前入力（任意） → ログイン完了！
```

だが `app.py` の `login()` は **`login_name` を1度も読んでいない**。
`saved_login_name` も画面に渡していない。**この欄は何もしていない。**

手引書が、動かない欄を説明している状態。
ログインを作り直す回に、**欄ごと消す**のが筋。
