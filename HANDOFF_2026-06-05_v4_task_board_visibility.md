# TASUKARU 開発 引き継ぎメモ（2026-06-05 v4：タスク／掲示板の公開範囲仕様）

このメモは `HANDOFF_2026-06-05_v2.md`（v2, print系・追加修正・繰り越し）の続き。
v2 §3-2 で「現場からの『特定スタッフだけタスク完了ボタンが押せない』報告」を原因特定のみで繰り越していた件を、本セッションで仕様確定・修正・本番反映まで完了した記録。生活機能チェック（v3_lifecheck_design）は本セッションでは未着手で繰り越し継続。

読む順序の目安: v1 → v2 → v3(lifecheck設計) → 本v4 → README。

---

## 0. 一行サマリ

タスクと掲示板の「誰に見せる／誰が操作できる」を **一貫ルール「特定の人を選ばなければ全員、選んだらその人だけ。管理者も例外なし」** に統一した。タスク権限の不整合バグ修正＋管理者除外＋掲示板のメンション強制限定。全て本番反映済み。

---

## 1. 確定した仕様（HIRO と合意済み）

### 共通の思想
- **特定の対象を指定しない** → 全員に見える・全員が操作できる（従来の「全体」と同じ結果）
- **特定の対象を指定する** → 指定された人＋作成者/投稿者だけに見える・操作できる
- **管理者も例外なし** … 担当/メンションされていなければ、管理者でも見えない・操作できない（プライバシー優先）

### タスク
| 操作 | できる人 |
|---|---|
| 表示 | 作成者 or 担当者（担当者リストが空＝全員向けなら全員） |
| 更新（完了など） | 作成者 or 担当者（空配列＝全員）。**管理者は権限から除外** |
| 削除 | 作成者 or 担当者（空配列＝全員）。**管理者は権限から除外** |

- 既存の `assigned_to=[]`（空配列）タスクは「全員向け」として自動的に救済される（移行作業不要）。
- 保存側（`assign_type=="all"` で空配列）はそのまま。表示側ももともと「空＝全員」で正しかった。

### 掲示板
| 投稿 | 見える人 |
|---|---|
| メンションあり | メンションされた人＋投稿者のみ（管理者もメンションされなければ見えない） |
| メンションなし | 全員 |

- 表示側は元々 `is_private=True` なら mention_names＋投稿者に絞っており管理者特別扱いなし＝新仕様と整合。
- 投稿時に **メンションが1人以上いれば強制的に `is_private=True`** にすることで「メンション＝限定」を仕組みで保証。
- 掲示板コメント（`/api/board/add_comment`）は `is_private` を持たず親投稿に従うため変更なし。

### 削除権限を「作成者＋担当者（管理者除外）」にした経緯
管理者を表示から除外する以上、「見えないものを削除する権限だけ管理者に残す」のは矛盾（操作する手段がない）。検討した3案:
- A: 作成者のみ削除可 … 作成者が退職・不在だと誰も消せず詰む
- **B: 作成者＋担当者が削除可（採用）** … 「見える人は消せる」で一貫。作成者不在でも担当者が消せる
- C: 作成者＋管理者 … 管理者は見えないのに権限だけ持つ＝破綻。不採用

---

## 2. 実際に直した箇所（app.py）とコミット

DEVブランチ `tasukaru-dev` 上のハッシュ。いずれも本番 `tasukaru` へマージ済み。

### コミット `(1) 4e15c59`（dev） → 本番 `54cffbd`
**タスク更新の権限を表示側と整合**（`/api/tasks/update`, 7823付近）
- 症状: 表示側は「空配列＝全員に見せる」なのに、権限側は `is_assignee = (my_name in assigned)` のみで空配列だと作成者/管理者以外を 403。結果「全員に見えるのに担当外は完了ボタンが無反応」（tasks.html の `toggleDone` の `catch(e){}` が 403 を握りつぶす）。
- 修正: `is_assignee = (my_name in assigned) or (not assigned)`（空配列＝全員が担当扱い）。マーカー `task-perm-empty-allowall`。
- パッチ: `patch_task_perm_fix.py`

### コミット `(2) bd7312b`（dev） → 本番 `11bba13`
**管理者除外＋削除の担当者対応＋掲示板メンション強制限定**（3箇所）
- **タスク更新**（7825付近）: 判定から `is_admin` を除外 → `if not (is_creator or is_assignee):`。未使用になった `is_admin = is_admin_user(...)` 行も削除。マーカー `task-update-no-admin`。
- **タスク削除**（`/api/tasks/delete`, 7846付近）:
  - `select("created_by")` → `select("created_by,assigned_to")`（担当者判定に必要。元は取得していなかった）。マーカー `task-del-select-assigned`。
  - 判定を `is_creator or is_admin` → `is_creator or is_assignee`（担当者込み・管理者除外）。`_assigned_del` を新設。エラーメッセージも「作成者または担当者のみ削除できます」に。マーカー `task-del-no-admin`。
- **掲示板投稿**（`api_board_create`, 7279付近）: `is_private` 決定の直後に `if mentions: is_private = True`。マーカー `board-mention-force-private`。
- パッチ: `patch_mention_visibility.py`

---

## 3. 触っていない箇所と理由（重要）

- **タスク表示側**（app.py 780 TOP / 7700-7711 `/api/tasks/list`）: 元々 `created_by==my_name or my_name in assigned_to or not assigned_to` で管理者特別扱いなし。新仕様と整合のため変更不要。
- **掲示板表示側**（7115-7117 一覧 / 7556-7558 未読カウント / 7618 既読化）: 3箇所とも `is_private なら mention_names＋投稿者のみ` で一貫・管理者特別扱いなし。変更不要。
- **掲示板の編集/削除権限**（7310, 7351, 7443, 7470 等）: `is_board_editor_user` を使う別系統（閲覧範囲ではなく編集削除権限）。今回のスコープ外。
- **カレンダーの is_private**（3400-3407）: 別機能（`calendars` テーブル、category=="PRIVATE"）。掲示板と無関係。
- **フロント** `templates/board.html` (2431) は `currentScope==='mention' ? '1':'0'` で既にメンションスコープと is_private を連動済み。サーバ側強制限定はそれをバックアップする二重の保証。
- **フロント** `templates/tasks.html` の作成送信（688-689）はロジック正常（全員=[]+all／特定=selectedAssign+specific）。`selectedAssign` の組み立て（657-700）も正常。フロント修正は不要だった。

---

## 4. 既知の小さな残課題

- `api_tasks_delete` の docstring が `"""タスク削除（作成者のみ）"""` のまま。実態は「作成者＋担当者」。コメントのみで動作影響なし。次に触るとき修正推奨。
- DEV実機確認は実施せず現場確認に回した（前回の休み連絡修正と同じ運用判断）。サンドボックスで構文・冪等性・挙動（管理者でも担当外なら拒否、メンション強制限定、空配列=全員許可）は検証済み。
- 現場確認の観点: ①以前ボタンが押せなかったスタッフで全員向けタスクのボタンが押せるか ②担当指定タスク／メンション投稿が対象外（管理者含む）に見えないか ③無指定は従来どおり全員に見えるか。

---

## 5. 次にやること（繰り越し継続）

- **本題（最優先）**: 生活機能チェック（様式3-2）フェーズ1 の残り。DB・保存/取得API（`/api/save_life_check`, `/api/life_check_history`）は適用済み（コミット前の可能性）。残りは ①`/api/life_check_assist`（AI判定アシスト、`LIFE_ASSIST_PROMPT`、app.py 4829-4874 のSDK呼び出しを雛形）②`/life_check` route と `life_check.html`（fitness.html のUIパターン流用）③保存時の check_date+3か月アラート登録（`life_check_schedules`、vital_recheck_schedules=app.py 2655-2759 のパターン流用、TOP表示は新規）。設計詳細は v3_lifecheck_design 参照。DDLは DEV Supabase に適用済み・**本番未適用**。
- 開発者MENUからの更新情報投稿（v2 §3-2）、全施設ポップアップ（v2 §3-3）。
- Stripe/LINE 残（README §5）。

---

_最終更新: 2026-06-05（タスク／掲示板の公開範囲を「メンション/担当ベース＋管理者除外」に統一。dev 4e15c59・bd7312b → 本番 54cffbd・11bba13。パッチ patch_task_perm_fix.py / patch_mention_visibility.py）_
