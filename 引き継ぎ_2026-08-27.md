# 引き継ぎ 2026-08-27（次のチャットへ）

このファイルを最初に読んでください。**HIROさんに、もう一度説明させないため**のものです。

---

## 0. まず守ること — セキュリティ

★**キー・トークン・パスワード・接続文字列を、チャットに出さないこと。**

- `.env`・環境変数・Supabaseのキー・APIキーを **`cat` / `echo` / `printenv` しない**
- ログやエラーを貼るときも、キーが混ざっていないか**貼る前に見る**
- どうしても触れる必要があるときは**必ず伏せる**
  例：`SUPABASE_KEY=（伏せ字）` / `キーの末尾4文字だけ …XXXX`
  ★このファイルにも、実物のキーは1つも書いていない
- スクリーンショットを撮るときも、キーが写る画面は避ける
- **本番のSupabaseに書き込むSQLは、HIROさんに実行してもらう。** こちらから流さない

★**本番（`tasukaru`）では読むだけ。** 確認のためにデータを1件も作らない・変えない。
　画面を触ったら、必ず元の状態に戻してから終わる。

---

## 1. ファイルの受け渡し方（★これが一番大事）

**ファイルを読みたいときは、こちらから「VSCodeのターミナルに貼るコマンド」を出す。
HIROさんがそれを実行して、Downloadsに出てきたファイルをチャットに添付する。**

こちらから勝手にファイルを取りに行かない。必ずコマンドを渡して、添付を待つ。

### 出すコマンドの形

```bash
# 1ファイル
cp /Users/ZIMAX/dev/kaigo-ai-app/guide_integration.py ~/Downloads/

# 複数まとめて（zipにする。大きいファイルが混ざるとき）
cd /Users/ZIMAX/dev/kaigo-ai-app && zip -j ~/Downloads/tasukaru_files.zip \
  app.py evaluation_helper.py templates/base.html

# 大きすぎるファイルは、要る所だけ抜き出す
sed -n '1,600p' /Users/ZIMAX/dev/kaigo-ai-app/app.py > ~/Downloads/app_1-600.txt

# ある文字列の周りだけ見たい
grep -n "探したい文字列" -B5 -A30 /Users/ZIMAX/dev/kaigo-ai-app/app.py > ~/Downloads/抜粋.txt

# ファイルの一覧だけ欲しい
ls -la /Users/ZIMAX/dev/kaigo-ai-app/templates/ > ~/Downloads/templates一覧.txt
```

★`app.py` は **1.6MB・33,000行以上**ある。丸ごと出させない。**要る所だけ**。
★`templates/vitals.html`（256KB）、`README.md`（500KB超）も同じ。

### 直したファイルを返すとき

こちらが直したファイルはチャットに添付して渡す。HIROさんが所定の場所に置いてから
`git add` → `git commit` → `git push` する。**コマンドは毎回こちらが書いて渡す。**

---

## 2. 相手のこと

**HIROさん（ZIMAXさん）。TASUKARU の作者で、運営者で、たった一人の開発者。**
通所介護（デイサービス）の事業所を運営しながら、自分で作っている。

- **専門の技術者ではない。** 用語をそのまま使わず、何が起きているかを日本語で説明する
- **判断は速い。** 選択肢と、それぞれの危なさを出せば、その場で決めてくれる
- **こちらの間違いを責めない。** だからこそ**間違いは自分から先に言う**
- 「〜だと思います」より **「測ったらこうでした」** を喜ぶ

### 話し方

- **日本語。です・ます。** 短く、具体的に
- **専門用語を使うときは、その場で言い換える**（例：「合成click＝ブラウザが勝手に送ってくるクリック」）
- ★**「たぶん」「〜のはずです」で終わらせない。** 確かめてから言う。
  確かめていないなら **「これは確かめていません」と明記する**
- 表・箇条書きを使って、目で追えるようにする

---

## 3. 2つのアプリ（★混同しないこと）

| | **TASUKARU** | **FUKUSHI QUEST** |
|---|---|---|
| 場所 | `/Users/ZIMAX/dev/kaigo-ai-app` | `/Users/ZIMAX/dev/fukushi-quest` |
| 何のため | 通所介護の**日々の業務** | 2026/12/13 のイベント用 |
| 作り | Flask（`app.py`）＋Jinja2テンプレート | HTML/CSS/JSだけ |
| 公開先 | Google Cloud Run（asia-northeast1） | GitHub Pages |
| ブランチ | `tasukaru-dev` → `tasukaru` の2段 | `main` の1本 |
| 止まると | **職員が記録を書けない。業務が止まる** | 当日以外は誰も困らない |

★**この重さの違いが、進め方の違い。TASUKARU は慎重に。**

---

## 4. TASUKARU の環境

### デプロイは自動

```
git push origin tasukaru-dev  → DEV が自動ビルド&デプロイ（3〜6分）
git push origin tasukaru      → 本番 が自動ビルド&デプロイ（3〜6分）
```

Cloud Build のトリガーが2つ稼働中。**手動デプロイは不要。**
HIROさんが「デプロイ完了」と言ったら反映されている。

### URL

```
DEV   https://tasukaru-dev-191764727533.asia-northeast1.run.app
本番  https://tasukaru-191764727533.asia-northeast1.run.app
```

### Supabase（キーは絶対に出さない）

| | 事業所コード | 利用者数 |
|---|---|---|
| DEV | `DEMO001` | 53人 |
| 本番 | `cocokaraplus-5526` | 86人（利用中78人） |

★**どちらのプロジェクトで流したSQLか、必ず見分けてから信じる。**
　列の数だけでは見分けがつかない。**利用者数と `facility_code` を同時に数える。**

### 本番へ出す手順

```bash
cd /Users/ZIMAX/dev/kaigo-ai-app
git switch tasukaru
git merge --no-ff tasukaru-dev -m "（何を出すか）"
git push origin tasukaru
git switch tasukaru-dev        # ★戻るのを忘れない
```

★リポジトリ直下に **`tasukaru` という0バイトのファイル**がある。
　`git` がブランチ名と取り違えて `fatal: ambiguous argument` を出すことがある。
　`git switch` を使うか `--` で区切る。（消すのが残件）

### 時刻

Cloud Run は **UTC**。日付を扱うコードは必ず `tokyo_tz` を通す。
これで一度事故が起きている（セルフ評価の対象月がUTCで決まっていた）。

---

## 5. 守っている決まり（★破らないこと）

1. **DDLはコードより先。** 本番に列が無いのに新しいコードを出すと、
   職員が保存した瞬間に失敗する。しかも手がかりが出ない
2. **`.py` の書き換えは、何度流しても同じ結果になるスクリプトで。**
   `assert count == 1` で、置き換える場所が1か所であることを必ず確かめる
3. **印（marker）を付ける。** `guide-v1` `eval-two-entrances-v1` のように。
   後から `grep` で追えるようにする
4. **理由をコードに書く。** 「なぜこうしたか」「なぜこうしてはいけないか」。
   このリポジトリは元からそう書かれている。合わせる
5. **外れた対策は消す。** 残すと「あれが効いている」と誤解される

---

## 6. 確認の作法（★これが品質の中身）

こちらの言う「直りました」は、**測った結果**でなければならない。

```
Python      python3 -m py_compile   ＋  python3 -m pyflakes
            ★py_compile は通るのに未定義の名前を見逃す。pyflakes まで走らせる
JavaScript  node --check
            ★テンプレートの {{ }} は null に置き換えてから
実際に動かす Chromium（headless）または Claude-in-Chrome で本物の画面
見た目の数字 推測しない。ブラウザで測る（要素の位置・大きさ）
DBの列名    近くのコードから推測しない。information_schema を読む
            ★コメントも信じない。実際に読んでいるコードを追う
```

### ★やりがちな間違い（今日4回やった）

**ソースの中の文字列の出現回数を数えて「直った」「壊れている」と判断すること。**

- 自分が書いたコメントの中の文字列まで数えてしまう
- `if`/`else` に分かれて書かれているだけのものを「id重複」と誤報した

→ **コメントを外してから数える。`ast` で実際の値を取る。実行して確かめる。**

---

## 7. 今日までに作ったもの

### `guide-v1` 画面の案内役（本番稼働中）

**79画面あるのに手引書は22項目**という問題への答え。

```
guide_integration.py の SCREENS（43画面ぶんの文章）
  ├─ ① 入力欄の横の「?」（吹き出し）
  ├─ ② 右上の「?」のカード（この画面は何をする所か＋いまの状況）
  └─ ③ 手引書に足すときのもとネタ
```

★**テンプレート79枚には手を入れていない。** 触ったのは `base.html` 1枚だけ。
　文章を直すときは **`guide_integration.py` だけ**を直す。

- キーは**URLのパスまるごと**（`/input` `/admin/jisseki`）。
  ★先頭1語にすると `/admin` の下が全部おなじになる（一度これで失敗した）
- 探し方は「完全一致 → いちばん長い前方一致」
- `_NO_GUIDE`（`/self-eval/run`・`/self-eval/locked`）＝
  **利用者にタブレットを渡す画面。案内を出してはいけない**
- ★このモジュールは**読むだけ**。`insert/update/delete/upsert/rpc` を書かない
- 「いまの状況」の判定は12種類。**どの画面でも走るので、軽い問い合わせだけ**

**点検スクリプト `check_guide.py`** — 案内文と画面のズレを見つける。
画面を足したとき・欄のidを変えたとき・URLを変えたとき・**本番へ出す前**に走らせる。

```bash
python3 check_guide.py          # 点検
python3 check_guide.py --new    # 案内がまだ無い画面
python3 check_guide.py --quiet  # 問題があるときだけ（終了コード 0/1）
```

★**文章の中身が古いかどうかは機械には分からない。** そこは人が読むしかない。

### `eval-two-entrances-v1` 評価の入口を担当ごとに分けた

`/assessment2`。評価担当者と機能訓練指導員で入口を分けた。

- ★**2人が同時に入る**ので、ロックを**入口ごと**に持たせた
- ★**古い `editing_by` は消さない。** 旧画面がまだ動いている
- 材料の欄を2つに（`source_data` ＝機能訓練指導員／`source_data_eval` ＝評価担当者）。
  同じ列だと**同時に書くと片方が消える**
- ★**旧 `/assessment` はそのまま残してある**

### その他

- `dead-caserecords-v1` … `/case_records`・`/history` の500を解消（`/monitoring` へ転送）
- `shift-line-hit-v2` … カレンダーのマス、上半分＝勤務予定／下半分＝予定
- `drawer-tap-v2` … Androidで取っ手のタップが効かない件
- `static/tap.html` … 端末のタップを診断するページ（次も使える）

---

## 8. ★今日いちばんの学び

**手元で再現できない不具合は、推測を重ねずに【実機から事実を取る道具】を作る。**

Androidのタップは**2回とも見当違いの直し**をした。3回目に診断ページを作って
記録を取ったら、**1回で原因が確定した**。

```
実機の記録：
  A     0ms  pointerdown   捕捉=できた
  A    72ms  pointerup     ずれ 0px  move 0回      ← ブレは無かった（見立ては外れ）
  A    74ms  判定 → 開く                          ← 取っ手は正しい
  A    96ms  click が来た                          ← ★犯人
```

開いた直後にブラウザが送る**合成click**が、全画面を覆ったマスクに当たって
`close()` を呼んでいた。**開いた瞬間に自分で閉じていた。**

★**2回外したら、直し方ではなく調べ方を変えること。**

### 他の学び

- **列がどのテーブルにあるかを、コメントで判断しない。**
  `evaluation_helper.py` には「patients.care_level」とあるが、実際は `patient_profiles`
- **「必ず踏む」と言う前に、導線まで調べる。** `/case_records` の500を大げさに報告した
- **見た目に関わる数字は実測してから決める。** 推測ならカレンダーの上下を逆にしていた
- **職員が見ている環境は Chrome とは限らない。**
  UAを見て初めて `Line/26.13.0/IAB`（LINEのアプリ内ブラウザ）と分かった
- **「直した」と言うなら、届いたことまで確かめる。**
  作業用コピーだけ直して送り忘れ、`git status` に出なかったことがある

---

## 9. 残っていること

### TASUKARU

**すぐできる**

- 案内文をHIROさんに読んでもらう → 直すのは `guide_integration.py` の文章だけ
- 介護区分が空の2人（横地美恵子、嶺村竹男）を埋める

**中くらい**

- `/timecard` に案内が届かない。`timecard.html` が `base.html` を継承していない
  → `templates/_guide.html` に切り出して両方から読み込む（B案）
- 案内がまだ無い画面3つ（`/admin/timecard/report`・`/patient-info/import`・`/photo/admin`）
- 月を切り替えても評価の入力が残る（引き継ぐ／消す の確認が要る）
- 同姓同名の利用者が評価でぶつかる（`user_name` をキーにしているため）
- `app.py` の pyflakes 未定義名 16件（`supabase` `my_name` `_fc` `_mn`）
- `/api/update_patient_care_level` が介護度を壊す（要介護4 → 要介護1）
- 書式の選択（機能訓練加算なしの事業所は「今月の評価」1欄でよい）
- 評価画面の音声入力（`/api/evaluation/ingest_file` の録音機を移植する）
- リポジトリ直下の0バイトファイル `tasukaru` を消す

**やらないと決めたこと**

- `/care-manager-report`（`docs/CARE_MANAGER_REPORT_DESIGN.md` の③）
  → HIROさん判断：**復活させない。いまの書類出力で印刷できればよい**

### FUKUSHI QUEST（★期限あり。2026/12/13）

- **許諾確認4件**（326さん／かすこんねぅ／ダンス部／プラタナス）
- ゲスト紹介の写真が透けた `326.png` のまま
- 協賛企業名・キッチンカー名・プラタナスのInstagram URL
- 会場用 `?q=` QRコード 7種類
- 本番前の本部リセット確認

---

## 10. 直近の状態

```
本番マージ    d181d27（2026-08-27）
              ★戻すときは git revert -m 1 d181d27 → push
tasukaru-dev  506776f
本番の確認    全画面200・案内役が実データで動作・旧評価画面は無傷
```

---

**最初にやること：** このファイルを読んだうえで、HIROさんに
「今日は何から始めますか」と聞いてください。**説明を求めないこと。**
ここに書いていないことが必要になったら、そのとき聞けば十分です。
