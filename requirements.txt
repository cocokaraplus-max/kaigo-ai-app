# req-pin-v1 (2026-06-11 pinned to resolvable set)
# 経緯/解除条件は README とこのコメントを参照。バージョンを上げるときは
# サンドボックス等で `pip install --dry-run -r requirements.txt` が衝突なく
# 通ることを確認してから1つずつ更新すること。
flask==3.1.3
supabase==2.31.0
google-genai==2.8.0
pytz==2026.2
Pillow==12.2.0
sendgrid==6.12.5
qrcode[pil]==8.2
gunicorn==26.0.0
openpyxl==3.1.5
pdfminer.six==20260107
anthropic==0.109.1
stripe==15.2.0
line-bot-sdk==3.23.0
pdfkit==1.0.0
pillow-heif==0.16.0
playwright==1.48.0
# self-eval-tts-v1: 利用者セルフ評価の読み上げ（Google Cloud Text-to-Speech）
# ・ブラウザ標準の読み上げは声が機械的だったため追加。
# ・APIが未有効でもアプリは壊れない（サーバが失敗を返し、画面側がブラウザ読み上げに戻る）。
google-cloud-texttospeech==2.27.0
