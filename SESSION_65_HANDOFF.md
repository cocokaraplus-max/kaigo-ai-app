# SESSION 65 HANDOFF — 音声入力のハルシネーション対策／同音語対策／利用者名の漢字変換

作成: 2026-08-17 / ブランチ: tasukaru-dev / 本セッション分は **DEV・本番とも反映済み（完了）**
本番tip: `a47ce04` / DEV tip: `ab7151d` / 本番とDEVは**コード完全一致**（差分ファイル0）

---

## ★最重要ルール（ユーザー明示・厳守）

### 1. セキュリティ（キー類を絶対に露出しない）
- APIキー・トークン・パスワード・SECRET_KEY・Supabaseキー等の**秘密情報は、出力にもファイルにも絶対に生で書かない／貼らない**。
- 表示が必要な場合は**必ずスクランブル（マスク）**する。例: `sk-ant-****…****`。
- 秘密情報の入力・貼り付けが必要な操作は、ユーザー本人に依頼する（Claudeが代行しない）。

### 2. git はユーザーが Mac のターミナルで実行
- Claudeはコマンドを提示するだけ。各git操作の前に `rm -f .git/index.lock` を入れる。
- 読み取り専用git（`git log` / `git show` / `git rev-list` / `git merge-base` / `git merge-tree` / `git diff --name-only`）はdevice上でも安全。
- リポ直下に空ファイル `tasukaru-dev` があり参照が曖昧になる。ブランチ指定は `refs/heads/tasukaru-dev` を使う。
- `static/kyukyu/09_heimlich.jpg` が常に modified。ブランチ切替時は `git stash push -- static/kyukyu/09_heimlich.jpg` → 戻ったら `git stash pop`。
- **device_bash は rm 不可**。不要ファイルは `_to_delete/` へ `mv`。

---

## 発端（現場からの報告）

ケース記録の入力で、**午後のサービスでもなく手打ちもしていないのに**、内容に
「午後の活動では…」というハルシネーションが出た。

---

## 調査でわかったこと

昨年の `halluc-guard-v1` は **議事録とAI要約だけ**を対策しており、
**入り口である音声入力そのものは無防備**だった。音声→テキストの入口は8つあり、状態は次の通りだった。

| # | 機能 | 無音を弾く | 創作禁止 | temperature | 修正前の評価 |
|---|---|---|---|---|---|
| 1 | ケース記録入力の音声入力 `/api/transcribe` | なし | **なし**（逆に「介護記録として」と指示） | なし | **危険＝原因** |
| 2 | 翻訳入力 `/api/translate/voice` | なし | なし | なし | 危険 |
| 3 | 勉強会・職員会議 `/api/staff_minutes/transcribe` | あり | 弱い | なし | 要注意 |
| 4 | アセスメント音声 `/api/assessment/voice` | あり | あり | なし | 軽微 |
| 5 | バイタル音声 `/api/vital_voice_parse` | あり | 弱い | なし | 軽微 |
| 6 | 体温まとめ音声 | なし | あり（模範的） | なし | 軽微 |
| 7 | 担当者会議 `/api/meeting/transcribe` | – | あり | **0.1** | 対策済み(v1) |
| 8 | 未保存録音の復元 `/api/meeting/recover_transcribe` | – | あり | なし（v1の漏れ） | 軽微 |

**根本原因**：1のプロンプトが「以下の音声を**介護記録として文章に起こしてください**」と、
AIに「介護記録を書く」役割を与えていた。無音・短い・雑音だけの音声でも、
AIは役割を果たそうとして「午後の活動では…」のような定型的な介護記録文を創作していた。

---

## 対策1: ハルシネーション封じ（`halluc-guard-v2`）

- 1〜8 **すべてに `generation_config={"temperature": 0.1}`** を設定。
- 1・2・6 に **`len(audio_bytes) < 2048` なら AI に送らない**ガードを追加。
- 1 のプロンプトを「記録を書く」から「**聞こえた言葉だけを書き起こす**」へ全面書き換え。
- 聞き取れないときは AI に **`[NO_SPEECH]`** を返させ、サーバーで検出してテキスト欄に何も入れない。
- `input.html` に `no_retry` フラグ対応を追加（無音時に無駄な再試行をしない）。

### ⚠️ ハマりどころ（重要）
`utils.py` の `FastGeminiModel.generate_content` は **応答が空文字だと例外を投げ、
全フォールバックモデルで再試行する**。そのため「無音なら空文字を返せ」という指示は逆効果になる。
→ **合図トークン `[NO_SPEECH]` を返させる方式**にした。プレーンテキストを返す3本
（`/api/transcribe`・`/api/translate/voice`・`/api/staff_minutes/transcribe`）が対象。
JSONを返す系（バイタル・体温・アセスメント）は transcript を空にするだけでよいので対象外。

---

## 対策2: 聞き取り精度の回復（`halluc-guard-v3` → `asr-homophone-v1`）

対策1で介護の文脈を丸ごと外した結果、**「入浴を介助しました」→「ニューヨークを解除しました」**という
聞き取り精度の低下が発生した。介護の文脈には2つの役割があった。

- **聞き取りの手がかり**（同音語の判別に必要）
- **文章を書く役割の付与**（創作の原因＝有害）

この2つを分離し、前者だけを戻した。プロンプトに次を入れている。

- **同音異義語の対応表**（14項目）
  - かいじょ → 介助（「解除」ではない）※センサー・アラーム等の機器の話のときだけ「解除」
  - にゅうよく → 入浴（「ニューヨーク」ではない）
  - いじょう → 体を移す話なら「移乗」／様子の話なら「異常」／数量の話なら「以上」
  - せいしき→清拭 / こうい→更衣 / ごえん→誤嚥 / けいちょう→傾聴 / そうげい→送迎 / ほこう→歩行 / てんとう→転倒 ほか
- **文脈から判断するルール**（「同じ文にケア動作があれば、その文の『かいじょ』は必ず『介助』」）
- **誤変換の実例**（×「ニューヨークを解除しました」→ ○「入浴を介助しました」など4例）
- **歯止め**：「※上記はすべて『聞き取りのヒント』であって『書くべき内容』ではありません。
  聞こえていない語や名前を、ここから持ってきては絶対にいけません。」

---

## 対策3: 利用者名の漢字変換（二段構え）

### 一段目: 録音時のヒント（`asr-name-hint-v1` → `v2`）
- 入力画面が**選択中の利用者**を `/api/transcribe` に同送する（`input.html`）。
- サーバーはクライアントの値を信用せず、**施設の名簿（`patient_profiles`）に実在する氏名だけ**を採用。
  照合は `session["f_code"]`（ログイン時にサーバーが決めた値）＋ `user_name` の完全一致。
  → 細工したリクエストで偽名や他施設の名前をAIに注入できない。
- AIに渡すのは**その1人分だけ**（施設全員の名簿は送らない）。
- `v2`で、**姓・名それぞれの読み**を明示するようにした。
  例：「介護現場では下の名前で呼ぶことが多いため、『たかはし』と聞こえたら『高橋』、
  『みさき』と聞こえたら『美咲』と書いてください。」

### 二段目: 保存時の自動修正（`asr-name-kanji-v1` → `v5`）
録音の順番は自由。保存時に、本文中の読み仮名のままの利用者名を名簿の漢字表記へ直す。

- **場所**：`/input` の POST、`if m:` の直後（`app.py` ≈3651行）。insert は同関数内 ≈3768行。
- **読みの取得**：`input_view` 冒頭の `patients = get_patients(...)` から引くので **DB問い合わせは増えない**。
- **照合方式**：敬称（さん・サン・さま・様・ちゃん・くん・君・氏）の**出現位置ごとに、その直前の仮名だけ**を見る。
  一般語の巻き添え（「みずのみを介助」→「水野み」）を防ぐため、原則として敬称が続くときだけ置換する。
  姓名そろった4文字以上の読みは敬称なしでも置換。
- **呼ばれ方3通りに対応**：フルネーム／姓だけ／**下の名前だけ**（介護現場では下の名前で呼ぶことが多い）。
  ふりがなが「たかはしみさき」と姓名つづきで登録されていても、**前半一致なら姓・後半一致なら名**と判定する。
- 姓名の間の空白（「たかはし みさき様」「タカハシ　ミサキ様」）にも対応。
- 失敗しても本文はそのまま返す（**保存は絶対に止めない**）。

### ⚠️ ハマりどころ（重要）
- **「様子」の「様」を敬称と誤認**していた。正規表現に `様(?!子)` `氏(?!名)` `君(?!主)` の除外を入れた。
  誤認すると手前の文字をまるごと飲み込み、照合が全滅する（これが「変換されない」の直接原因だった）。
- 一つの正規表現で「仮名の塊＋敬称」を貪欲マッチすると、**敬称をまたいで飲み込む**
  （「ますこさんとはなこさん」で1人目を取り逃す）。→ **敬態の位置ごとに後方へ走査**する方式にした。

---

## 変更ファイルとマーカー

| ファイル | 内容 | マーカー |
|---|---|---|
| `app.py` | 音声8APIの対策、プロンプト、名前ヒント、保存時変換、ヘルパー関数 | `halluc-guard-v2` `halluc-guard-v3` `asr-name-hint-v1/v2` `asr-name-kanji-v1〜v5` |
| `templates/input.html` | 選択中利用者の同送、`no_retry` 対応 | `asr-name-hint-v1` `halluc-guard-v2` |

主な関数：`_kana_to_hira` / `_kana_to_kata` / `_is_kana_ch` / `_NAME_HONOR_RE` / `_fix_name_kanji`
（いずれも `@app.route('/input')` の直前に定義）

---

## テスト方法（次回も使える）

flask 未導入の device 上でも、**app.py から関数だけを取り出して単体テスト**できる。

```python
import io
src = io.open("app.py", encoding="utf-8").read()
ns = {}
exec(src[src.index("import re as _re_mod"):src.index("@app.route('/input', methods=['GET', 'POST'])")], ns)
fix = ns["_fix_name_kanji"]
print(fix("みさきさんの入浴を介助しました。", "高橋 美咲", "たかはし みさき"))
# → 美咲さんの入浴を介助しました。
```

最終的に20件のケース（呼び方3通り×仮名/カナ、様子・氏名・おかあさん・ケアマネさん・みずのみ の巻き添え防止、複数回出現、空入力）が全通過。
`python3 -m py_compile app.py` と jinja2 の `env.parse` でも検証している。

---

## 現場での確認結果（DEV→本番）
- 「みさきさんの入浴を介助しました。」→ **美咲さん／入浴／介助 すべて正しく変換**（確認済み）
- 無音録音 → テキスト欄は空のまま「音声を聞き取れませんでした」
- 短い発話（「転倒はありません。」）→ 言っていない文が足されない

---

## 残タスク / 次の候補

### 音声まわり（必要になったら）
- **モデルの見直し**：`utils.py` の `FALLBACK_MODELS` は `gemini-2.5-flash` が先頭（速度優先）。
  音声の聞き取りは上位モデルが明確に強いので、**ケース記録の文字起こしだけ上位モデルを使う**調整が可能
  （応答は数秒遅くなる）。`FastGeminiModel` にモデル指定の引数を足す形になる。
- 施設独自の用語（レク名・行事名・職員名）を聞き取りヒントに足す案。

### TASUKARU 本体（商用展開まわり／HIROさんの管理画面操作が中心）
1. **Stripe Webhook の本番登録** — コード（`/api/stripe/webhook`）は完成済み。Stripe管理画面での登録と `STRIPE_WEBHOOK_SECRET` 設定が未
2. **Cloud Scheduler の `CRON_TOKEN`** — 契約満了通知の定期実行。コードは完成済み、設定が未
3. **`PLAN_ENFORCE = True` への切替判断** — 現在 `False`（`app.py` ≈2987行）
4. **商用展開前のセキュリティ点検** — Stripe まわり、Flask `SECRET_KEY` の運用、Supabase Security Advisor の残項目

### ネイティブアプリ配布（`TASUKARU_配布ロードマップ.md` 参照）
5. iPhone 実機インストール（無料・すぐ試せる）
6. Android：Play Console の状態確認 → `npx cap add android` → 内部テスト（費用ゼロ）
7. iOS：Apple Developer 登録（$99/年）→ 非公開App配信
8. `disaster.html` の「同期について」の文言が古いまま（同期は実装済み）

### 細かい確認・整理
9. 勤務予定の本人制限を、一般職員アカウントで実挙動確認（コードのみ確認済み・SESSION 64 由来）
10. リポジトリ整理（空ファイル `tasukaru-dev`、`_to_delete/`、HANDOFF ファイル25個超）

### 完了扱いにしてよいもの
- `TASUKARU_利用者情報_閲覧ICF計画.md` の A（見るタブ閲覧）・B（ICF積み上げ）・C（重複確認）・D（削除）は**すべて実装済み**を確認（2026-08-17）。

---

## 今セッションのコミット（新しい順）

- 本番(tasukaru): `a47ce04` merge(prod) / `4af3290` merge(prod)
- DEV(tasukaru-dev):
  - `ab7151d` 下の名前で呼ばれた場合の漢字変換（asr-name-kanji-v5・hint-v2）
  - `a4647b3` 同音異義語の対応表＋「様子」誤認の修正
  - `ca53d0d` 保存時に読み仮名を漢字へ自動修正（asr-name-kanji-v1）
  - `00403ec` 利用者名の聞き取りヒント（名簿照合つき／asr-name-hint-v1）
  - `649bf18` 聞き取り精度の回復（halluc-guard-v3）
  - `207d3bb` 音声APIのハルシネーション対策を全体へ拡大（halluc-guard-v2）

## バックアップ（device 上・gitignore対象外なので必要なら整理）
`app.py.bak_hallucguard2_20260817` / `app.py.bak_hg3_20260817` /
`app.py.bak_namehint_20260817` / `app.py.bak_namekanji_20260817` / `app.py.bak_homophone_20260817` /
`templates/input.html.bak_hallucguard2_20260817` / `templates/input.html.bak_namehint_20260817`
