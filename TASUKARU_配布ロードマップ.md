# TASUKARU ネイティブアプリ配布ロードマップ

作成: 2026-07-22 / 目的: TASUKARUを **ストアに公開せず**、実施設のスタッフのスマホに **ネイティブアプリ**として配る。
ネイティブで配ると、ブラウザ版では動かない **通知（再検査アラーム）・オフライン災害モード** も使える。

---

## 0. 前提（現状）
- Capacitorアプリ（殻）は完成済み。iOSシミュレータで起動OK。中身はDEV/PRODのWebをそのまま表示するので、**料金設定・契約書印刷など既存Web機能はアプリ内でもそのまま動く**。
- ネイティブ機能（アラーム＝`app-native-recheck-v1`／オフライン災害＝`app-disaster-*`）は **アプリ内でのみ有効**。ブラウザ/PWAでは鳴らない・動かない。
- Bundle ID `jp.lifeplus.tasukaru` / App名 TASUKARU。
- **Apple Developer Program：未登録**（iOS配布に必須・$99/年）。
- **Google Play：過去に登録済み（$25は支払い済み・追加費用なし）**。Androidはビルドが未着手（`npx cap add android` 未実施）。

## 1. 費用の整理（ここが結論）
- **Apple＝$99/年**：アカウント単位・**アプリ本数無制限**。非公開App配信でも公開App配信でも同額。将来別アプリを公開しても追加年会費なし。
- **Google＝$25 一度きり（支払い済み）**：アカウント単位・アプリ本数無制限。**追加費用は発生しない**。
- ストア手数料(15〜30%)は「ストア決済で有料販売した時だけ」。TASUKARUは無料アプリ＋Stripe課金なので**発生しない**。

## 2. iOSトラック（本命＝非公開App配信 / Unlisted App Distribution）
利用者はApp Storeと同じ1タップ導入、TestFlight不要・ホーム追加不要・90日期限なし。検索に出ないので実質「ストアに載せない」。
- [ ] **(HIRO)** Apple Developer Program に登録（$99/年）。法人＝合同会社LIFE PLUS で登録する場合は D-U-N-S番号が必要（取得無料・数日）。
- [ ] **(HIRO)** App Store Connect で新規アプリ作成（Bundle ID `jp.lifeplus.tasukaru`）。
- [ ] **(Claude)** iOSアプリのアイコン一式（1024pxから全サイズ）・表示名・起動画面をストア審査向けに整備 ※`tasukaru-app` 接続済み。
- [ ] **(HIRO)** Xcodeでアーカイブ→App Store Connectへアップロード（署名は自動でOK）。
- [ ] **(HIRO)** アプリ情報・スクショ等を入力し、**通常のApp Store審査**を1回通す（数日〜2週間目安）。※WebViewラッパー単体は 4.2 でリジェクトされがちだが、TASUKARUはネイティブ通知・オフライン等の実機能があるのでかわしやすい。
- [ ] **(HIRO)** 承認後、「非公開配信(Unlisted)」をリクエスト → 直リンク発行 → QR/LINEで配布。
- 先に無料で試すなら：MacにiPhoneをつなぎ **Xcodeから実機インストール**（署名は無料Apple IDで可・7日で失効＝週1入れ直し）。動作確認・アラーム確認に最適。

## 3. Androidトラック（既存Googleアカウントを使う）
- [ ] **(HIRO)** 既存の Google Play デベロッパーアカウントの**状態を確認**（ログインできるか／有効か／作成時期・種別）。
  - 2023/11以降に作った“個人”アカウントは **本番公開に「12人×14日クローズドテスト」** が必要。**それ以前のアカウント or 法人アカウントは対象外**のことが多い。
  - スタッフ配布だけなら **内部テスト（最大100人）** を使えば、このゲートと無関係に**すぐ配れる**（招待リンク/メール）。
- [ ] **(HIRO)** Android Studio 導入 → `npx cap add android`（`tasukaru-app` 側）。
- [ ] **(Claude)** Androidアプリアイコン一式・表示名・設定を整備。
- [ ] **(HIRO)** リリース用ビルド（AAB/APK）→ Play Console にアップロード → 内部テスト（or クローズド/公開）で配布。
  - 手っ取り早い代替：APKを直接配布（提供元不明を許可）→ QRでダウンロード導入も可。

## 4. 私(Claude)が手伝えること / HIROさんがやること
- **Claude**：iOS/Androidのアイコン・起動画面・表示名の整備、`capacitor.config` 調整、fastlane/GitHub Actions等でのビルド自動化スクリプト作成、審査対策の助言、手順の逐次伴走。
- **HIRO**：各種アカウント登録・支払い、Xcode/Android Studio操作、ストアへのアップロードと審査提出（＝アカウント・実機・署名が絡む操作）。

## 5. おすすめの順番
1. **(無料・今すぐ)** HIROさんのiPhoneにXcodeで実機インストール → 本当にアプリとして動く・アラームが鳴るのを確認。
2. **Android**：既存Googleアカウントの状態確認 → Android Studio導入 → ビルド → 内部テストでスタッフ数名に配って検証（費用ゼロで実配布まで試せる）。
3. **iOS**：Apple Developer登録($99/年) → 非公開App配信の審査 → 直リンク/QRでスタッフ配布。
4. 将来、必要なら公開App（App Store掲載）も同じアカウントで追加費用なしで出せる。

---
※このロードマップは進捗に応じてチェックを埋め、決定事項はREADME/HANDOFFにも反映すること。
