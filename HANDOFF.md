# TASUKARU 開発 引き継ぎメモ（休み連絡 複数日対応 + Q&Aガイド）

最終更新：2026-05-29

このファイルは、次のチャット（次のセッション）が作業を止めずに続けられるようにするための引き継ぎ書です。
上から順に読めば、今どこまで終わっていて、次に何をすればよいかが分かります。

---

## 0. 最初に必ず読むこと（セキュリティの約束）

このプロジェクトは介護施設の実データ（利用者情報など）を扱う本番システムです。次のことを必ず守ってください。

- **APIキー・トークン・パスワード・接続文字列を、チャットや出力に絶対に出さない。**
  - Supabase の Settings（API キー、Service Role Key、DB パスワード、接続URL）画面は**開かない／表示しない／コピーしない**。
  - `setenv.sh` などの環境変数ファイル、`.env`、`requirements.txt` 以外の秘密情報を画面に出さない。
  - もし会話・スクリーンショット・ログにキーらしき文字列が映りそうなら、**マスキング（スクランブル）して伏せる**こと。例：`SUPABASE_KEY=eyJh****（以下伏せる）`。
  - 万一キーが露出したら、作業を止めて「キーが露出した可能性があるので、ローテーション（再発行）を検討してください」とユーザーに伝える。
- **Supabase で触ってよいのは SQL Editor と Table Editor のデータ／構造だけ。** プロジェクト設定・認証情報には触らない。
- **本番DB・本番ブランチへの操作は、必ず実行直前にユーザーへ最終確認を取ってから行う。** 「これは本番です」と明示してから実行する。
- 破壊的操作（DELETE、DROP、巻き戻し等）は、まず影響範囲を SELECT で確認 → 件数確認 → 実行、の順で慎重に。
- ブラウザ操作（Chrome連携）で、外部サイトに利用者の個人情報やシステム情報を入力・送信しない。

---

## 1. プロジェクト基本情報

| 項目 | 値 |
|---|---|
| アプリ名 | TASUKARU（介護記録アプリ） |
| GitHub | `cocokaraplus-max/kaigo-ai-app` |
| ローカル | `/Users/ZIMAX 1/dev/kaigo-ai-app/` |
| dev URL | https://tasukaru-dev-191764727533.asia-northeast1.run.app |
| 本番 URL | https://tasukaru-191764727533.asia-northeast1.run.app |
| 作業ブランチ | `tasukaru-dev`（dev）→ 確認後 `tasukaru`（本番）へマージ |
| デプロイ | GitHub連携でブランチへ push すると Cloud Run に自動デプロイ（数分かかる）。反映確認は `gcloud run revisions list --service=tasukaru-dev --region=asia-northeast1 --project=tasukaru-production` |
| インフラ | Google Cloud Run（東京 asia-northeast1）/ プロジェクト `tasukaru-production` |

### Supabase（データベース）
| 環境 | プロジェクト名 | プロジェクトID | 備考 |
|---|---|---|---|
| 本番 | `kaigo-ai-app` | `abvglnkwtdeoaazyqwyd` | 本番アプリが参照 |
| dev | `tasukaru-dev` | `otjevnmoycnvaxeltrtj` | dev アプリが参照 |

> ※ ID はプロジェクト識別用（URLに出る公開情報）であり、APIキーではない。キーは別物なので絶対に扱わないこと。

### ローカル環境の注意
- VSCode で `stripe` `pdfkit` の import 警告が出るが、これは**ローカルに未インストールなだけ**で、コンテナ側は `requirements.txt` から入るため実害なし。消したいなら `pip3 install stripe pdfkit`（venvを使っているなら有効化してから）。
- ローカルに `fix_*.py` という過去の作業スクリプトが多数 untracked で残っているが、今回の件とは無関係。放置でよい。

---

## 2. このセッションで完成・本番反映した内容（休み連絡 複数日対応）

### 仕様（確定済み・この方針を維持すること）
- **入力の起点は「記録入力 → 休み連絡」に一本化する（片方向設計）。** カレンダーから直接お休みを作ってもケース記録は作らない。
  - 理由：カレンダー起点の双方向化（＝カレンダーから作ってもケース記録を作る）は、利用者特定UIや多数の仕様判断が必要で複雑。今回は見送り、代わりに Q&Aガイドで運用を案内する方針にした。
- **休み連絡で複数日（飛び日）を指定できる。** 例：6/2・6/5・6/10。
  - ケース記録は**1件にまとめて**「6月2日・6月5日・6月10日はお休みと家族から連絡がありました。理由：…」と列挙。
  - カレンダーには**各日に1個ずつ**「○○様 お休み」イベントを作成（飛び日なので個別）。
  - 連携キー：`calendar_events.record_id`（今回追加した列）に、ひも付くケース記録の id を入れて「1記録 ↔ 複数イベント」を表現。
- **逆連携（カレンダー → ケース記録）**
  - カレンダーのお休みを1個削除 → 残るイベントからケース記録の content と leave_date_start/end を**作り直す**（消した日が文から消える）。
  - カレンダーのお休みを全部削除 → ひも付くケース記録も**削除**。
  - カレンダーのお休みを編集（日付変更）→ ひも付く全イベントの日付から content を作り直す。
- **記録日（created_at / 投稿日）は絶対に動かさない。** 変わるのは「お休みの日」と記録の文章だけ。
- 休み連絡カテゴリのとき：内容欄（content）は**非表示**、お休みの理由欄は**textarea（複数行・必須表記なし）**。

### 変更したファイル
- `app.py`
  - `_format_leave_period(dates)` 追加：日付リスト→「6月2日・6月5日…」表記（連続は「〜」でまとめる）。
  - `_build_leave_content_multi(...)` 追加：複数日の content 生成。
  - 休み連絡保存（記録作成まわり）：`leave_dates`（カンマ区切り）を受け取り、複数日なら1記録に集約＋各日カレンダー登録＋全イベントに `record_id` セット。先頭イベントを `records.calendar_event_id` にも入れて後方互換。
  - `api_save_calendar_event`：編集時、編集対象に `record_id` があれば「ひも付く全イベントの日付」から content/start/end を作り直す分岐を追加。
  - `api_delete_calendar_event`：削除イベントに `record_id` があれば、残イベントで記録を作り直し or 残ゼロなら記録削除。
  - `/faq` ルート追加（`/help` と同じく `app.send_static_file('faq.html')`、`@login_required`）。
- `templates/input.html`
  - 休み連絡フォームに「飛び飛びのお休み日を追加」UI（日付＋追加ボタン＋タグ、hidden `leave_dates`）。
  - **スマホ対応：このUIは縦並び（日付欄を上に幅100%、ボタンを下に幅100%）。** 横並びにすると日付欄が潰れるので戻さないこと。
  - 休み連絡時に内容欄（`#content-section`）を非表示にする処理を `updateLeaveFormVisibility` に追加。
  - 理由欄を input → textarea 化、「（任意）」表記を削除。
  - フォーム先頭に「休みの連絡がケース記録とカレンダーに残る仕組みを見る」リンク（`/faq#q1` を新タブで開く）。
- `templates/calendar.html`
  - 以前に入れた「カレンダーから直接の複数日UI（ev-multi-toggle 等）」は**撤去済み**（入力起点はケース記録に一本化したため）。元の `saveEvent` に戻してある。再導入しないこと。
- `static/faq.html`（新規）
  - よくある質問（Q&A）ページ。休み連絡⇄カレンダー連携を**SVGの図2点**で解説（Q1：両方に残る仕組み／Q3：削除すると記録がどう変わるか）。ほか全13問（音声入力・写真動画・閲覧必須・利用者検索・記録修正・AI要約・スマホ利用など）。初心者向けにやさしい言葉＋開閉式。

### データベース変更（重要・両環境で実施済み）
```sql
ALTER TABLE calendar_events ADD COLUMN IF NOT EXISTS record_id bigint;
```
- dev（`tasukaru-dev` / otjevnmoycnvaxeltrtj）：**実施済み**
- 本番（`kaigo-ai-app` / abvglnkwtdeoaazyqwyd）：**実施済み**
- 既存データは無変更（空列が1つ増えただけ）。

### Git の状態
- 本番 `tasukaru` と dev `tasukaru-dev` は**同一コミット `0ce9212` に揃っている**（コードもDB構造も一致）。
- 直近の主なコミット：
  - `0ce9212` 飛び日追加UIをスマホ向けに縦並びへ修正
  - `88ef721` よくある質問(Q&A)ページ追加: /faqルート + 連携解説リンク
  - `62c7b64` 休み連絡UI調整: 理由欄textarea化・内容欄を休み連絡時のみ非表示
  - `bb59f61` 休み連絡 複数日(飛び日)対応: 1件集約+各日登録+逆連携(record_id)

---

## 3. dev での動作確認結果（すべて成功・確認済み）

Chrome連携で実機同様に確認済み：
1. 休み連絡選択で内容欄が非表示／理由欄が広いtextarea。
2. 飛び日3つ（6/2・6/5・6/10）→ ケース記録1件に「…はお休みと家族から連絡がありました。理由：通院のため」。
3. カレンダーに各日3個の「お休み」登録。
4. カレンダーから1日（6/5）削除 → ケース記録が「6月2日・6月10日」に自動修正（記録日は不変）。
5. カレンダーから全削除 → ケース記録も自動削除。
6. `/faq` ページ表示（図2点描画・開閉・もくじジャンプ動作）。
7. 記録入力の休み連絡フォームに FAQリンク表示。

---

## 4. 次にやること（残タスク・優先順）

### ★ 最優先：本番の最終動作確認（まだ未実施）
本番にコード反映＋DB列追加は完了済みだが、**本番URLでの実動作確認がまだ**。次のセッションで最初に行うこと：
1. 本番デプロイが反映されているか確認（revisions list か、本番URLをハードリロード）。
2. 本番（https://tasukaru-191764727533.asia-northeast1.run.app）で：
   - 記録入力 → 休み連絡 → 飛び日UIが**縦並びで正しく表示**されるか（スマホ幅でも確認）。
   - 複数日の休み連絡を**1件テスト保存** → ケース記録1件集約＋カレンダー各日登録を確認。
   - カレンダーから1日削除 → 記録が直るか。全削除 → 記録が消えるか。
   - **テストデータは確認後すぐ削除する**（本番なので残さない）。
3. 問題があればログ（ブラウザConsole / Networkの `/api/save_calendar_event`）を確認して修正。

> 補足：本番にコードが先に出てしまっていた期間があり、その間に現場が複数日休み連絡を保存しようとして失敗していないか要確認。現在は record_id 列を追加済みなので以降は正常動作する見込み。

### 今後の検討事項（やるかどうかは要相談）
- **双方向化（カレンダー起点でもケース記録を作る）**：今回見送り。やる場合は「TASUKARUケース記録連動」カレンダー分類を使って判定する案がある。ただし利用者特定UI・記録日・記録者・カテゴリ等の仕様判断が必要。現場から要望が出たら設計から相談。
- FAQページの導線追加（管理者MENUやヘルプ一覧からも開けるようにするか）。

---

## 5. 作業の進め方（このプロジェクトの流儀）

- ファイルのやり取り：ユーザーのローカルには直接アクセスできない。ユーザーが該当ファイルをチャットにアップロード → こちらでパッチ適用 → 完成版をダウンロード提供 → ユーザーが `cp` でリポジトリに戻す、という流れ。
  - 長いコードをターミナルにヒアドキュメントで貼らせると貼り付けミスが起きやすい。**完成版ファイルを渡す方式が確実。**
- 変更後は必ず `python3 -m py_compile app.py`（Python）や、JS/HTMLの構文チェックをしてから渡す。
- デプロイは `git push origin <branチ>` で自動。dev で確認 → 本番へマージ、の順を厳守。
- DB変更は dev で先に検証 → 本番、の順。本番操作は実行直前に必ず最終確認。

---

（このメモは次のセッションの開始時にそのまま渡せば、文脈を引き継いで作業を継続できます。）
