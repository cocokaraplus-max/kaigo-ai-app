# 引き継ぎ 2026-08-28（次のチャットへ）

このファイルを最初に読んでください。HIROさんに、もう一度説明させないためのものです。
前回（2026-08-27）の引き継ぎを引き継いだうえで、**今日ぶんを更新**しています。

---

## 0. まず守ること — セキュリティ

**★キー・トークン・パスワード・接続文字列を、チャットに出さないこと。**

- `.env`・環境変数・Supabaseのキー・APIキーを `cat` / `echo` / `printenv` しない
- ログやエラーを貼るときも、キーが混ざっていないか**貼る前に見る**
- どうしても触れる必要があるときは必ず伏せる
  例：`SUPABASE_KEY=（伏せ字）` / キーの末尾4文字だけ `…XXXX`
  ★このファイルにも、実物のキーは1つも書いていない
- スクリーンショットを撮るときも、キーが写る画面は避ける

**本番のSupabaseに書き込むSQLは、HIROさんに実行してもらう。** こちらから流さない。

**★本番（tasukaru）では読むだけ。** 確認のためにデータを1件も作らない・変えない。
画面を触ったら、必ず元の状態に戻してから終わる。

### 本番を「触らずに」確かめる技（今日たくさん使った。次も使える）

本番の画面で動作を確かめたいが、データは触りたくない。そういうときは：

| やりたいこと | 触らずに済ませる方法 |
|---|---|
| 配信されているコードが新しいか | `fetch('/画面')` して、印（marker）や関数名の**出現数を数える** |
| 画面の見た目を確かめたい | ニセのデータを変数に入れて、**描画関数を直接呼ぶ**（`renderResult(...)` / `buildReport(...)`） |
| 確認ダイアログを通したい | `window.confirm = () => true` に**一時的に差し替える**（★本物のダイアログはブラウザ操作を止めるので絶対に出さない） |
| APIの返事だけ見たい | **存在しない氏名**で叩く（0件更新なので当たらない）。★ただし新コードが載っていることを先に確かめてから |
| 設定値を読みたい | `localStorage.getItem(...)` は読むだけ。**書き換えない**。押して試したら**同じ回数押して戻す** |

---

## 1. ファイルの受け渡し方（★今日から変わった）

**`/Users/ZIMAX/dev` がフォルダ接続されていれば、こちらから直接 読み書きできる。**
（Claudeデスクトップの「フォルダを追加」で接続されている状態）

### 今日の実際の状況

- ✅ **読む・書く** … できた（ファイルを取り込む／書き戻す）
- ❌ **Mac上でコマンドを走らせる**（`device_bash`）… **起動しなかった**
  → `grep` や `sed` をMac側で走らせられないので、**ファイルを取り込んでから**こちらで調べた

### だから、こう進める

| したいこと | やり方 |
|---|---|
| ファイルを読む | こちらが直接取り込む。HIROさんの手間はゼロ |
| ファイルを直す | こちらが直接書き戻す。**変更内容は毎回 diff で見せる** |
| **git 操作** | **必ずHIROさんの手元**。こちらはコマンドを渡すだけ |
| 本番のSQL | **必ずHIROさんの手元**。読むだけのSQLでも自分で流さない |

### ★コマンドを渡すときは `&&` で1本につなぐ

今日、**`git add` の `g` が抜ける事故が2回**あった。バラバラに渡すと、
1行目が失敗しても2行目3行目が走ってしまい、`Everything up-to-date` で空振りする。

```bash
cd /Users/ZIMAX/dev/kaigo-ai-app && git add -- ファイル1 ファイル2 && git commit -m "..." && git push origin tasukaru-dev
```

`&&` なら途中で止まるので、事故が事故のまま見える。

### device_bash が使えるときは

Mac上でシェルが使えるなら、`grep` / `sed -i` / 短いpythonをその場で走らせたほうが速い。
ただし**1回45秒**で切れるので、長い仕事は分ける。

---

## 2. 相手のこと

**HIROさん（ZIMAXさん）。** TASUKARU の作者で、運営者で、たった一人の開発者。
通所介護（デイサービス）の事業所を運営しながら、自分で作っている。

- **専門の技術者ではない。** 用語をそのまま使わず、何が起きているかを日本語で説明する
- **判断は速い。** 選択肢と、それぞれの危なさを出せば、その場で決めてくれる
- **こちらの間違いを責めない。** だからこそ間違いは自分から先に言う
- **「〜だと思います」より「測ったらこうでした」を喜ぶ**
- **現場の目線が鋭い。** 今日も「印刷されないことを伝えないと現場が混乱する」と指摘された。
  こちらが技術的に正しいものを作っても、**使う人が不安になる作りは直す**

### 話し方

- 日本語。です・ます。**短く、具体的に**
- 専門用語を使うときは、その場で言い換える
- **★「たぶん」「〜のはずです」で終わらせない。確かめてから言う。**
  確かめていないなら**「これは確かめていません」と明記する**
- 表・箇条書きを使って、目で追えるようにする

---

## 3. 2つのアプリ（★混同しないこと）

|  | TASUKARU | FUKUSHI QUEST |
|---|---|---|
| 場所 | `/Users/ZIMAX/dev/kaigo-ai-app` | `/Users/ZIMAX/dev/fukushi-quest` |
| 何のため | 通所介護の日々の業務 | 2026/12/13 のイベント用 |
| 作り | Flask（app.py）＋Jinja2テンプレート | HTML/CSS/JSだけ |
| 公開先 | Google Cloud Run（asia-northeast1） | GitHub Pages |
| ブランチ | `tasukaru-dev` → `tasukaru` の2段 | `main` の1本 |
| 止まると | 職員が記録を書けない。**業務が止まる** | 当日以外は誰も困らない |

★この重さの違いが、進め方の違い。TASUKARU は慎重に。

---

## 4. TASUKARU の環境

### デプロイは自動

```
git push origin tasukaru-dev  → DEV が自動ビルド&デプロイ（3〜6分）
git push origin tasukaru      → 本番 が自動ビルド&デプロイ（3〜6分）
```

Cloud Build のトリガーが2つ稼働中。手動デプロイは不要。
HIROさんが「デプロイ完了」と言ったら反映されている。

### URL

```
DEV   https://tasukaru-dev-191764727533.asia-northeast1.run.app
本番  https://tasukaru-191764727533.asia-northeast1.run.app
```

### 主な画面のパス（★間違えやすい）

| 画面 | パス |
|---|---|
| 書類出力 | `/print_output`（**アンダーバー**。`/print-output` は404） |
| 印刷プレビュー | `/print_preview` |
| モニタリング | `/monitoring` |
| 評価（旧） | `/assessment` |
| 評価（新・入口2つ） | `/assessment2` |

### Supabase（キーは絶対に出さない）

|  | 事業所コード | 利用者数 |
|---|---|---|
| DEV | `DEMO001` | 53人 |
| 本番 | `cocokaraplus-5526` | **86人**（うち履歴あり79人・利用中止で介護度なし7人） |

★どちらのプロジェクトで流したSQLか、必ず見分けてから信じる。
列の数だけでは見分けがつかない。**利用者数と facility_code を同時に数える。**

### 本番へ出す手順

```bash
cd /Users/ZIMAX/dev/kaigo-ai-app
git switch tasukaru
git merge --no-ff tasukaru-dev -m "marker-name"     # ★英数字だけ
git push origin tasukaru
git switch tasukaru-dev        # ★戻るのを忘れない
```

★**本番マージのコミットメッセージは英数字だけにする。**
今日、日本語が端末に貼られる途中で壊れ、履歴に文字化けが残った（`e28e962`）。
動作に影響はないが、あとから読めない。DEV側の日本語コミットは化けていない。

★リポジトリ直下に `tasukaru` という**0バイトのファイル**がある。
git がブランチ名と取り違えて `fatal: ambiguous argument` を出すことがある。
`git switch` を使うか `--` で区切る。（消すのが残件）

### 時刻

Cloud Run は UTC。日付を扱うコードは必ず `tokyo_tz` を通す。
これで一度事故が起きている（セルフ評価の対象月がUTCで決まっていた）。

---

## 5. 守っている決まり（★破らないこと）

1. **DDLはコードより先。** 本番に列が無いのに新しいコードを出すと、職員が保存した瞬間に失敗する。しかも手がかりが出ない
2. **書き換えは、何度流しても同じ結果になるスクリプトで。**
   - `assert count == 1` で、置き換える場所が1か所であることを必ず確かめる
   - ★**「すでに当たっているか」の判定は「新しいコードが在るか」で見る。**
     「印が在るか」だと、**追記型の置き換え**（新が旧を含む形）で2回当たってしまう。今日これで失敗した
   - `.bak` を残す
3. **印（marker）を付ける。** `guide-v1` `careclass-no-overwrite-v1` のように。後から `grep` で追えるようにする
4. **理由をコードに書く。** 「なぜこうしたか」「なぜこうしてはいけないか」。このリポジトリは元からそう書かれている。合わせる
5. **外れた対策は消す。** 残すと「あれが効いている」と誤解される

---

## 6. 確認の作法（★これが品質の中身）

こちらの言う「直りました」は、**測った結果でなければならない。**

| 対象 | やること |
|---|---|
| Python | `python3 -m py_compile` ＋ `python3 -m pyflakes` |
| JavaScript | `node --check`（テンプレートの `{{ }}` は null に、`{% %}`・`{# #}` は空に置き換えてから） |
| Jinjaテンプレート | `jinja2.Environment().parse(中身)` で構文を通す |
| 実際に動かす | Claude-in-Chrome で本物の画面 |
| 見た目の数字 | 推測しない。ブラウザで測る |
| DBの列名 | 近くのコードから推測しない。実際に読んでいるコードを追う。**コメントも信じない** |

### ★pyflakes の before / after を比べるときの注意

行数だけ見ても意味がない。**行番号を落として、指摘の内容だけを比べる。**

```bash
python3 -m pyflakes app.py.bak > b.txt
python3 -m pyflakes app.py     > a.txt
sed 's/^[^ ]*:[0-9]*:[0-9]*: //' b.txt | sort | uniq -c | sort > mb.txt
sed 's/^[^ ]*:[0-9]*:[0-9]*: //' a.txt | sort | uniq -c | sort > ma.txt
diff mb.txt ma.txt        # 差分なし＝増やしていない
```

★app.py の pyflakes は元から **89件・undefined name 16件**。これが基準。増えていなければよい。

### ★★いちばん効いた技：コードを「切り出して実際に走らせる」

書き写して確かめると、書き写した時点でズレる。**本物のコードをそのまま取り出して動かす。**

```python
# app.py からループ本体を切り出し、ニセのAIで走らせる
src = open('app.py').read()
block = textwrap.dedent(src[src.index('        GEN_FAIL_MSG = ') : src.index('        # DBに保存')])
exec(block, {'model': FakeModel(fail_times=2), ...})
```

```python
# print_preview.html の行を描く部分を切り出し、Jinjaで実際に描かせる
frag = tpl[tpl.index('{% for cat_name, cat_text in filtered_cats %}') : ...]
Environment().from_string(frag).render(filtered_cats=[...], items={}, p={...})
```

これで「印刷に出るか／画面で赤字か」を**目で見て確かめられる**。推測がゼロになる。

### ★やりがちな間違い

**ソースの中の文字列の出現回数を数えて「直った」「壊れている」と判断すること。**
- 自分が書いたコメントの中の文字列まで数えてしまう
  （今日も、コメントに書いた `id="tog-no-record"` を自分の assert が拾って落ちた）
- `if`/`else` に分かれて書かれているだけのものを「id重複」と誤報した

→ コメントを外してから数える。実行して確かめる。

---

## 7. 今日作ったもの（4件・すべて本番稼働中）

### `careclass-no-overwrite-v1` — 介護度が化けるのを止めた

**旧 `/assessment` の「介護区分」ボタン**が `patient_profiles.care_level` を上書きしていた。
区分は 要介護／要支援／事業対象者 の**3択しかない**のに、そこから介護度を決めようとして
`{"要介護": "要介護1"}` と**当て推量で書いていた**。

- **要介護4の人が、区分を押し替えるだけで 要介護1 になる**
- 押し間違えて押し戻すと、さらに壊れる（**戻す操作が戻らない**）
- しかも `_record_care_level_history()` を呼ばないので**履歴にも残らない**

**直し方**：`/api/update_patient_care_level` を「何も書き込まない」に。呼び出し側の `fetch` も削除。
新しい `/assessment2` は最初から「care_level は書き換えない」設計だったので、それに合わせた。
ルート自体は残した（職員の端末に古いJSが残っていても404にしないため）。

★**介護度を実際に変えるのは `/patient-info`（`/api/admin/patient/save`）だけ。**
あちらは 要介護1〜5 を選べて、`care_level_history` も積む。

**本番データの被害調査 → 無傷だった。**
履歴のある79人は「今の介護度」と「履歴の最新」が完全一致。残り7人は履歴なしだが、
**TASUKARU本格稼働前に利用が終わった人たち**で、介護度を入れずに終わった記録（不具合ではない）。

★調べ方の限界も記録しておく：一度壊れたあと誰かが利用者情報で直していれば、履歴も一緒に直るので**跡が残らない**。
言えるのは「**今このデータは正しい**」まで。

### `mon-card-delete-v1` — モニタリング結果に「削除」

`/monitoring` の生成結果カードに「削除」を追加（編集・コピーの右）。
**カテゴリ別モードの全カードに出る**（中身のあるカードにも）。まとめて1本モードには出さない
（あのモードはカード1枚＝報告書1本なので、削除＝全消しになるため）。

**削除の意味づけ（HIROさんの決定）**
- 消すのは「**この月の報告書の中身**」だけ。カテゴリを無効にするのではない
- **［保存］を押して初めて確定**。押さなければ元のまま
- あとで記録が追加されて**再生成すれば、また出てくる**

### `no-record-toggle-fix-v1` — トグルのID不一致

`/print_output` の「**報告なし項目も印刷**」ボタンだけ、**IDが `tog-no-record`（ハイフン）**で、
設定キーは `no_record`（アンダーバー）。14個中これ1つだけ不一致だった。

画面は `'tog-' + キー名` でボタンを探すので、このボタンだけ：
- 押しても**見た目が変わらない**
- 再読み込みしても**ONだと分からない**
- でも**状態は localStorage に残る**

→ **一度うっかり押すと、誰にも気づかれないままONが続く。**
これが「先月、空カテゴリが印刷されてしまった気がする」の正体だった。

IDを `tog-no_record` に直し、印刷プレビューにも「ONです」の黄色い帯を出すようにした（帯は印刷されない）。

★**このスイッチは localStorage（端末ごと）。** 職員Aの端末ではON、職員Bでは OFF、が起こる。
今はボタンが正しく光るので、見れば分かる。

### `gen-fail-visible-v1` — 生成失敗が黙って消えるのを止めた

`app.py` の書類出力側の自動生成（`_auto_generate_monitoring`）で、
**AI生成に失敗したカテゴリを「今月このカテゴリの報告はありませんでした」として保存**していた。
`print_preview` はその文言を印刷対象から外すので、**記録があるのに書類から黙って消える**。誰も気づけない。

（モニタリング画面側の同じ処理は「（生成エラー: …）」と見えるようにしてあった。**2か所で扱いが割れていた。**）

**直し方**
1. **まず1回だけ作り直す**（通信の一時的な失敗はこれで大半が通る）
2. それでも駄目なら、見て分かる文章を残し、**ログにも出す**
3. その文章は**画面には赤字で残り、書類には1文字も出ない**

入る文章：

> **【この行は書類には印刷されません・画面だけの表示です】**AIの生成に失敗しました。記録は2件あります。モニタリング画面でもう一度生成してください。

★**「印刷されません」を先頭に置いたのはHIROさんの指摘。**
これが無いと、画面で見た職員が「これがケアマネさんに行くのか」と慌てる。

**3か所で伝える**
- 文章の先頭
- 印刷プレビューのカテゴリ欄に赤い札「**印刷されません**」
- モニタリング画面のカード下に「⚠ 生成に失敗しました ／ この文章は書類には印刷されません」

★**「AIの生成に失敗しました」という語が、行を見分ける目印。**
`app.py` 1か所・`print_preview.html` 3か所・`monitoring.html` 2か所で**揃っていないと動かない**。
文面を変えるときも、**この語は残すこと。** スクリプトが毎回この数を数えている。

---

## 8. 前回までに作ったもの（引き続き稼働中）

### `guide-v1` 画面の案内役

79画面あるのに手引書は22項目、という問題への答え。
`guide_integration.py` の `SCREENS`（43画面ぶんの文章）から、
① 入力欄の横の「?」 ② 右上の「?」のカード ③ 手引書に足すもとネタ を出す。

- ★テンプレート79枚には手を入れていない。触ったのは `base.html` 1枚だけ
- **文章を直すときは `guide_integration.py` だけを直す**
- キーはURLのパスまるごと（`/input` `/admin/jisseki`）。先頭1語にすると `/admin` の下が全部同じになる
- 探し方は「完全一致 → いちばん長い前方一致」
- `_NO_GUIDE`（`/self-eval/run`・`/self-eval/locked`）＝ 利用者にタブレットを渡す画面。案内を出してはいけない
- ★このモジュールは**読むだけ**。insert/update/delete/upsert/rpc を書かない

点検スクリプト `check_guide.py`：

```bash
python3 check_guide.py          # 点検
python3 check_guide.py --new    # 案内がまだ無い画面
python3 check_guide.py --quiet  # 問題があるときだけ（終了コード 0/1）
```

### `eval-two-entrances-v1` 評価の入口を担当ごとに分けた

`/assessment2`。評価担当者と機能訓練指導員で入口を分けた。
2人が同時に入るのでロックを入口ごとに持たせ、材料の欄も2つに分けた
（`source_data` ＝機能訓練指導員／`source_data_eval` ＝評価担当者）。
★旧 `/assessment` はそのまま残してある。古い `editing_by` も消さない。

### その他

- `dead-caserecords-v1` … `/case_records`・`/history` の500を解消（`/monitoring` へ転送）
- `shift-line-hit-v2` … カレンダーのマス、上半分＝勤務予定／下半分＝予定
- `drawer-tap-v2` … Androidで取っ手のタップが効かない件
- `static/tap.html` … 端末のタップを診断するページ（次も使える）

---

## 9. 学び

### 手元で再現できない不具合は、【実機から事実を取る道具】を作る（前回の学び）

Androidのタップは2回とも見当違いの直しをした。3回目に診断ページを作って記録を取ったら、
1回で原因が確定した（開いた直後の合成clickが、全画面マスクに当たって `close()` を呼んでいた）。

★**2回外したら、直し方ではなく調べ方を変えること。**

### ★今日の学び：症状の説明がつかないときは、コードを機械的に突き合わせる

「空カテゴリが印刷されてしまった気がする」の原因は、**ボタンIDの `_` と `-` の1文字違い**だった。
見つけ方は、**設定キー14個と、対応するボタンIDが実在するかを、機械に総当たりさせただけ**。

```python
for k in poItems_keys:
    print("OK" if ('tog-'+k) in ids else "★無い", k)
```

13個がOKで1個だけ「★無い」。**読んで探したら、まず見つからなかった。**

### ★今日の学び：正しい直しでも、現場が不安になる作りは直す

生成失敗の文章を「画面には残す／紙には出さない」と作った。技術的には正しい。
だがHIROさんの指摘：「**印刷には出ない事を伝えるメッセージも必要。出てしまうと思って現場は混乱する。**」

**動きが正しいことと、使う人が安心できることは別。** 動きを直したら、
**それを使う人がどう受け取るかまで確かめる。**

### その他

- **列がどのテーブルにあるかを、コメントで判断しない。** `evaluation_helper.py` には「patients.care_level」とあるが、実際は `patient_profiles`。ついでに今日確かめたが、**`patients.care_level` を読み書きするコードは app.py に1か所も無い**（`table("patients")` は32か所あるが、`care_level` と一緒に出る行はゼロ）
- **こちらの出したSQLの穴を、自分で先に言う。** 今日 `join lateral` の内部結合で「0件」を出したが、それは「食い違いなし」とも「履歴が無くて比べられなかった」とも読めた。**HIROさんに報告する前に気づけていなかった**
- 職員が見ている環境は Chrome とは限らない。UAを見て初めて `Line/26.13.0/IAB` と分かった
- 「直した」と言うなら、**届いたことまで確かめる**

---

## 10. 残っていること

### TASUKARU

**すぐできる**

- 案内文をHIROさんに読んでもらう → 直すのは `guide_integration.py` の文章だけ
- ~~介護区分が空の2人を埋める~~ → **★落としてよい。** 利用中止者で、介護度を入れずに終わった記録
- リポジトリ直下の0バイトファイル `tasukaru` を消す

**中くらい**

- `/timecard` に案内が届かない。`timecard.html` が `base.html` を継承していない
  → `templates/_guide.html` に切り出して両方から読み込む（B案）
- 案内がまだ無い画面3つ（`/admin/timecard/report`・`/patient-info/import`・`/photo/admin`）
- 月を切り替えても評価の入力が残る（引き継ぐ／消す の確認が要る）
- 同姓同名の利用者が評価でぶつかる（`user_name` をキーにしているため）
- `app.py` の pyflakes 未定義名 16件（`supabase` `my_name` `_fc` `_mn` ほか）
- 書式の選択（機能訓練加算なしの事業所は「今月の評価」1欄でよい）
- 評価画面の音声入力（`/api/evaluation/ingest_file` の録音機を移植する）

**今日、見つけたが手をつけていないこと**

- `print_preview` の赤い「印刷されません」の札は、**実物の画面で見ていない**。
  いま実際に失敗しているカテゴリが1つも無いため。テンプレートをJinjaで描かせて確認済みだが、
  **本番の画面で目視はしていない**
- 空カテゴリの判定文字列が2か所で揃っていない
  （`monitoring.html` は `報告はありませんでした` の部分一致、`print_preview.html` は全文一致）

### やらないと決めたこと

- `/care-manager-report`（`docs/CARE_MANAGER_REPORT_DESIGN.md` の③）
  → HIROさん判断：復活させない。いまの書類出力で印刷できればよい

### FUKUSHI QUEST（★期限あり。2026/12/13）

- 許諾確認4件（326さん／かすこんねぅ／ダンス部／プラタナス）
- ゲスト紹介の写真が透けた `326.png` のまま
- 協賛企業名・キッチンカー名・プラタナスのInstagram URL
- 会場用 `?q=` QRコード 7種類
- 本番前の本部リセット確認

---

## 11. 直近の状態

```
本番マージ    e28e962（2026-08-28）  gen-fail-visible-v1
              b776747               mon-card-delete-v1 / no-record-toggle-fix-v1
              5f80f52               careclass-no-overwrite-v1
              d181d27（2026-08-27）  前回まで

              ★戻すときは git revert -m 1 <該当コミット> → push

tasukaru-dev  a8e5025
```

**本番の確認（すべて読むだけで実施）**

- 旧 `/assessment` から介護度の上書きが消えていること
- モニタリングのカードに「削除」が出ること
- 生成失敗のカードが赤字＋「書類には印刷されません」になること
- 報告書シートに失敗の文章が1文字も混ざらないこと
- 「報告なし項目も印刷」のIDが直っていること

★`e28e962` のコミットメッセージは**文字化けしている**（日本語が貼り付けで壊れた）。
動作に影響なし。直すには force push が要るので**やらない**。印はコード中にあるので `grep` で追える。

---

**最初にやること：**
このファイルを読んだうえで、HIROさんに「**今日は何から始めますか**」と聞いてください。
説明を求めないこと。ここに書いていないことが必要になったら、そのとき聞けば十分です。
