# TASUKARU 開発 引き継ぎメモ（2026-06-05 セッション末）

次のチャットの冒頭で、まず `README_tasukaru_dev.md` を読み、続けてこのメモを読めば、現状・進め方・残タスクが分かるようにしてある。前回メモ `HANDOFF_2026-06-04.md` も背景として有効（§1の基本情報・作法は引き続き踏襲）。

---

## 0. まず最初に読むもの
- リポジトリ直下の `README_tasukaru_dev.md`（§1〜§7）。特に §7 バイタル機能。
- 前回メモ `HANDOFF_2026-06-04.md`（基本情報・作法・残タスクの細部）。
- このメモ（HANDOFF 2026-06-05）。今回の完了分・新たな教訓・残タスクの最新版。

---

## 1. プロジェクト基本情報（前回から不変）
- ローカルパス: `/Users/ZIMAX 1/dev/kaigo-ai-app`
- 構成: Flask + Supabase + Cloud Run
- DEVブランチ: `tasukaru-dev` / 本番ブランチ: `tasukaru`
- DEV URL: https://tasukaru-dev-191764727533.asia-northeast1.run.app
- 本番URL: https://tasukaru-191764727533.asia-northeast1.run.app
- GitHub: cocokaraplus-max/kaigo-ai-app / GCP: tasukaru-production（asia-northeast1）
- 主要ファイル: `templates/vitals.html`（バイタル測定・約4,000行超）, `app.py`, `templates/manual.html`（ガイド）, `templates/print_preview.html`, `templates/print_output.html`, `templates/daily_view.html`（ケース記録・本日の記録）
- 検証用利用者: 青木 利夫 / 倍子（よみ「ますこ」＝当て字検証用）。実データに 阿部武・池田ヨシ・石井三郎・石田ナミ・井上光子 など。

---

## 2. 作業の進め方（厳守ルール）

### 2-1. 役割分担
- アシスタントはローカルを直接操作できない。ユーザーがVSCodeのターミナルでコマンドを実行し、結果を貼り付ける形で進める。
- アシスタントは grep / sed の確認コマンドを投げ、ユーザーが出力を貼る。それを見てからパッチを作る。**推測でパッチを書かない**（既存機能を壊すため）。

### 2-2. パッチの作法
- 変更は必ず **冪等な Python スクリプト**（アンカー文字列ベース）で渡す。複数回実行しても結果が同じになるよう書く。
- アシスタントはサンドボックスで検証してからファイルを渡す:
  - Python構文: `py_compile`
  - 挿入するJS片の構文: `node --check`
  - 実ファイル該当箇所を再現したファイルにパッチを適用し、適用後の全体が構文OKか
  - **冪等性**（2回目の実行で全項目スキップになるか）
  - HTML編集時は **div balance**（`<div` 数と `</div>` 数の一致）も確認
- 冪等判定マーカーの罠: 冪等判定に使う文字列は、**実際に挿入される文字列の中に必ず含まれる**ものにする。今回 `/* below-gap-fix */`（マーカー）と挿入文 `/* below-gap-fix: ... */` が不一致で2回挿入される事故があった → マーカーを `below-gap-fix:` に修正して解決。
- アンカーの罠: 実ファイルの空白・空行・インデントは想定とずれることが多い。`sed -n 'X,Yp' file | cat -et`（macOSは `-A` 不可、`-et` を使う）で `$`（行末）と `^I`（タブ）を可視化してから正確なアンカーを取る。罫線文字（`──` 等）は環境差があるのでアンカーに含めない。
- JSのグローバルは `window.xxx = function...` 形式。関数定義済み判定は文字列出現でなく `window.fnName = function` の有無で行う。
- `patient_id` と表示側 `pid` は String で統一して比較。

### 2-3. ファイルの受け渡し
- アシスタントはパッチを `present_files` で提示 → ユーザーがダウンロード。
- ユーザーは VSCode のターミナルで `cp ~/Downloads/patch_xxx.py .` → `python3 patch_xxx.py`。
- 適用結果（標準出力）と確認用 grep の結果をユーザーがチャットに貼る。
- 実機確認は Chrome連携（DEVタブのみ）。本番タブは操作しない。

### 2-4. セキュリティ（厳守）
- シークレット値は一切出さない／文書に書かない／ログに残さない。環境変数は名前のみ記載し値はマスク。
- Cloud Run 環境変数（値は書かない）: `LINE_CHANNEL_ACCESS_TOKEN` / `LINE_CHANNEL_SECRET` / `LINE_ADMIN_USER_ID` / `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` / 多数の `STRIPE_PRICE_*`。Supabaseキーも同様。

### 2-5. 本番反映手順
```
git checkout tasukaru
git merge tasukaru-dev
git push origin tasukaru
git checkout tasukaru-dev      ← ★これを必ず最後まで実行（§2-6参照）
```
- マージ後の `xxxxxxx..yyyyyyy tasukaru -> tasukaru` の出力で、コミット範囲とコンフリクト有無を必ず確認。
- 本番反映後は数分でCloud Run本番ビルド→本番URL反映。ブラウザはハードリロード（Cmd+Shift+R）。

### 2-6. ★今回の教訓: ブランチ取り違えに注意（重要）
- **本番マージ後に `git checkout tasukaru-dev` を実行し忘れると、`tasukaru`（本番ローカル）に居続けてしまう。** その状態で次のパッチをコミットすると、本番ブランチに誤コミットが乗る（今回 `8c8acde` がこれで本番ローカルに乗った）。
- 幸い push 前だったのでリモートは無傷。以下の手順で安全に復旧した:
  1. `git checkout tasukaru-dev`
  2. `git cherry-pick <誤コミットhash>` （中身をdevへ移す）
  3. `git push origin tasukaru-dev`
  4. `git checkout tasukaru`
  5. `git reset --hard origin/tasukaru` （ローカル本番を誤コミット前へ戻す）
  6. `git checkout tasukaru-dev`
- 予防: コミット前に必ず `git branch --show-current` で `tasukaru-dev` にいることを確認する。本番マージの最後の `git checkout tasukaru-dev` は省略しない。

---

## 3. 今回（2026-06-05）完了し、本番反映済みのもの

本番反映済みコミット（dev上のハッシュ。複数回に分けて本番マージ済み）:
- `87b4306` 測定UI第2段: 利用者検索窓を追加（初版・一覧フィルタ方式）
- `57584b5` 測定UI第2段改修: 検索を候補ドロップダウン化（daily_view移植）＋スワイプ削除赤帯はみ出し修正
- `71430f9` 赤帯はみ出し恒久修正（スワイプ前は非表示）＋書類プレビューの編集ヒント文字色を可読化
- `8bc682b` ガイド更新: 体温一括の回数撤去→10分自動説明 / アラート・再検査の見分け方 / 利用者検索 / 10分判定セクション追加
- `770446b` ケース記録モーダルのスクロールロック解除漏れ修正（全モーダル閉鎖時に body.overflow を自動解除する番人）
- `e8bbb8f` 体力測定グラフのリサイズで凡例が崩れる問題を修正（Chart.js responsive化＋リサイズ時に再描画通知）
- `16f63a6` 特記事項下配置レイアウトの枠間の余分な隙間を詰める（min-height漏れ適用と gap/margin 二重を解消）

機能の要点:

1. **測定UI 第2段（検索ドロップダウン）** … `templates/vitals.html`。AM/PMタブ下に検索窓。名前・ふりがな・カルテ番号(`chart_number`)の部分一致で候補ドロップダウン表示、タップで `openMeasure` でその人の測定画面へ。`daily_view.html` の `searchPatients` を移植。関数: `window.searchMeasurePatients` / `window.selectMeasurePatient` / `window.clearMeasureSearch`。CSS: `.measure-search-results` 系。旧「一覧フィルタ方式」(`filterMeasureList`)は撤去済み。

2. **スワイプ削除の赤帯はみ出し修正** … `templates/vitals.html`。2列カードで `swipe-delete-bg`（赤帯）が静止状態で右にのぞく問題。CSSで `.swipe-delete-bg { opacity:0 }`、`.patient-vital-card.swiping .swipe-delete-bg { opacity:1 }` とし、`initSwipeDelete` の onMove で `.swiping` 付与・onEnd/モーダル閉じで除去。スワイプ前は赤帯非表示なので inner の高さに依存せず漏れない。マーカー: `measure-v2 swipe-fix` / `measure-v2 swipe-hide`。

3. **アラート / 再検査の仕様（ガイドに明記済み）**
   - アラート（赤バッジ `#ea4335`）= `checkAlert(v)` が `SETTINGS` の上下限を外れた値を検出（血圧上下・脈拍・体温は高/低両方、SpO2は低のみ）。閾値は設定タブで変更可。
   - 再検査（橙バッジ `#ff6f00`）= `recheck` フラグ。付き方2通り: ①異常値で自動推奨、②値が範囲内でも職員判断で手動チェック（`v-recheck-${pid}` / `ef-recheck-${pid}`）。

4. **ガイド(manual.html) バイタルページ更新** … 既存セクション `s-vitals-input`（929行〜）に「利用者をすばやく探す（検索）」「アラートと再検査の見分け方」を追加。体温一括(`s-bulk-temp`)の旧「回数セレクタ(1回目/2回目)」記述とモックを削除し10分自動グルーピング説明に差し替え。再検査アラーム(`s-vitals-recheck`)の手前に新セクション `s-vitals-window`「同じ時間帯の測定をまとめる（10分判定）」を追加。すべて phone-mock（HTML/CSS図解）で統一。マーカー: `guide-measure-search` / `guide-alert-recheck` / `guide-10min-window`。目次(toc)への10分判定リンクは未追加。

5. **ケース記録モーダルのスクロールロック解除漏れ修正** … `templates/daily_view.html`。`body.overflow='hidden'` を立てる3モーダル（`record-modal`=display:block / `cr-search-modal`=class is-open / `vas-edit-modal-overlay`=hidden属性）が重なって操作されると解除漏れが起き、画面全体が縦スクロール不能になる（しばらくで直る）症状。デバイス問題ではなくコードのスクロールロック競合。修正は既存の開閉ロジックを変えず、「3モーダルが1つも開いていなければ body.overflow を必ず解除する番人」を 500ms インターバルで追加（`window._scrollLockGuardTimer`）。マーカー: `scroll-lock-guard` / `_anyLockingModalOpen`。

6. **体力測定グラフのリサイズ凡例崩れ修正** … `templates/print_preview.html`。グラフ右端のつまみ(`.rep-resizable-handle`)で伸縮すると、握力・片脚立位の右(実線)/左(破線)凡例が潰れて読めなくなる症状。原因: `drawBalanceChart` の Chart.js options に responsive 指定が無く（3.x デフォルトで比率固定）、かつリサイズ時に Chart へ再描画通知をしていなかった。修正: `drawBalanceChart` の options に `responsive:true, maintainAspectRatio:false` を追加（他のグラフ生成箇所は触らない）。リサイズハンドラに `_resizeChartsIn(container)`（コンテナ内の全canvasを `Chart.getChart().resize()`）を追加し、mousemove/touchmove/確定時に呼ぶ。Chart.js は 3.9.1（CDN、`Chart.getChart` 使用可）。マーカー: `balance-chart-responsive` / `chart-resize-notify`。

7. **特記事項下配置レイアウトの隙間修正** … `templates/print_preview.html`。特記事項が下配置（`data-layout="below"`）のとき、特記事項枠の下に大きな空白。原因: `.rep-special-row { min-height:32mm }`（本来グラフ横配置の右カラム用）が below にも漏れ適用され、未入力でも32mm確保。さらにコンテナの `gap:4mm` と各行 `margin-bottom` の二重。修正: `data-layout="below"` 限定で min-height 解除・gap 2mm統一・margin二重解消・特記事項本文は min-height:12mm 確保。Chrome連携でDEV実機を計測（container 295px→197px）し見た目確認のうえ反映。マーカー: `below-gap-fix:`。

---

## 4. 残タスク（次にやること）

### 4-1. 測定UI 第3段
- 進捗バー（○/○名）。
- 済/未の色分け（緑=測定済 等）。
- カード下段の表示切替（バイタル要約 ⇔ ふりがな＋カルテ番号、トグル）。

### 4-2. 書類出力プレビューの細部
- **満足度の「None」表示**: `print_preview.html` 1656/1660行 `ev.get('satisfaction', '未記入')` は値がNoneのとき「None」と表示してしまう（`.get`のデフォルトはキー欠落時のみ）。`ev.get('satisfaction') or '未記入'` に直すと未入力でも「未記入」と出る。**優先度低・見送り中**（本番でエラー報告なし＝現場は入力運用、未入力ダミーで顕在化しただけ）。やるなら小修正。
- **写真の横並び**: パターンA/Bの `img-col` 内で複数写真が縦積み（C は親に flex あり横並び）。グラフrespoinsive化(§3-6)で連動して改善したかは**実機未確認**。要DEV確認。希望は「どの配置でも崩れず横並び」。

### 4-3. ガイドの仕上げ
- 目次(toc)に10分判定セクション(`s-vitals-window`)へのリンクを追加（任意）。
- 測定UI第3段が固まったらガイドにも反映。

### 4-4. 任意の調整・様子見
- 無音しきい値（`BULK_VOICE_RMS_THRESHOLD = 0.05` @ vitals.html）の現場フィードバック反映。数値1つ変更（無音が通るなら上げる／小声が弾かれるなら下げる）。実測: 無音 最大RMS≈0.0218 / 発話 0.0955〜0.2083。
- 10分枠の延伸上限（叩き台6時間）。
- 複数測定の印のデザイン（やや控えめ）。
- 「まこ」等のあいまい照合を mid→none に倒すか。

### 4-5. バイタル以外の既存残タスク（README §4・§5 参照）
- 決済（Stripe）・LINE連携の残タスク（料金プラン、Webアプリ承認処理、契約管理画面、期限通知など）。詳細は README §5-4。
- 掲示板「さらに読み込む」ボタン、`utils.pycode` の削除 など（優先度中）。

---

## 5. 技術メモ・落とし穴
- ブラウザキャッシュで古いHTMLが出る → 実機確認は必ずハードリロード（Cmd+Shift+R）。
- **Chrome連携での実機計測が有効**: ログインはユーザーが行い、アシスタントは `javascript_tool` で `getComputedStyle` / `getBoundingClientRect` を使い余白・高さを計測 → 一時的な `<style id="_experiment">` を注入して見た目を試す（リロードで戻る・安全）→ 確定後に冪等パッチ化、という流れが今回の隙間修正で有効だった。認証情報の入力・送信系操作はアシスタントは行わない。
- スクロールロック系のバグは「複数モーダルが各自 body.overflow を上書き」して競合するのが典型。個別の解除漏れを潰すより「全モーダル閉鎖を監視して必ず解除する番人」を1つ置くのが堅実（既存ロジック不変更で回収できる）。
- Chart.js でコンテナをCSSリサイズしても中身は追従しない。`responsive:true, maintainAspectRatio:false` ＋ リサイズ時の `chart.resize()` 通知が必要。`Chart.getChart(canvas)`（v3.4+）で既存インスタンス取得。
- `vitals` の行特定キーは `(facility_code, patient_id, measured_date)`。`measured_at` はUTC保存。10分枠判定は `window.VITAL_WINDOW_MS = 10*60*1000` と `groupVitalWindows`（vitals.html 3178行付近）で `measured_at` 絶対時刻ベース。複数測定は「本日の記録」で `●` バッジ＋`openVitalHistoryModal`。
- 体温一括の確信度: 緑=即登録 / 黄=氏名ドロップダウン確定で昇格 / 赤=未検出で抹消。累積に入るのは緑＋確定黄のみ。回数セレクタは撤去済み（10分判定が自動決定）。
- パッチ作業ディレクトリ（アシスタント側サンドボックス）: `/home/claude/patch`、出力は `/mnt/user-data/outputs/`。

---

## 6. 次セッションの最初の一手（推奨）
1. `README_tasukaru_dev.md` と本メモ（＋前回 HANDOFF）を読む。
2. 作業ツリー確認: `git status --short && git branch --show-current`（`tasukaru-dev` にいること、未コミットが無いこと）。
3. 着手するタスクを §4 から選ぶ。実機確認が要るもの（写真横並び §4-2）は Chrome連携でDEVを開いて現状を見てから。
4. パッチは grep/sed で現物確認 → 冪等パッチ作成 → サンドボックス検証（py_compile / node --check / 適用後構文 / 冪等性 / div balance）→ DEV反映 → 実機確認 → 本番反映。
5. **本番反映後は必ず `git checkout tasukaru-dev` で dev に戻る**（§2-6）。
