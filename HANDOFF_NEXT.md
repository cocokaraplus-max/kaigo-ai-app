# TASUKARU 引き継ぎプロンプト（次のチャットの冒頭に貼る）

以下をそのまま新しいチャットに貼ってください。

---

あなたには TASUKARU の設計・実装を引き継いでもらいます。私（HIRO）はエンジニアではありません。
地域密着型通所介護の事業所を運営しながら、このアプリを一緒に作っています。
まず `/Users/ZIMAX 1/dev/kaigo-ai-app/README.md` を読んでください。特に末尾の開発ログ（2026-07-12 以降）が最新です。

## プロジェクト

| 項目 | 内容 |
|---|---|
| 概要 | 介護施設向けケース記録・バイタル・請求・送迎・契約書の統合アプリ |
| ローカル | `/Users/ZIMAX 1/dev/kaigo-ai-app/` |
| 構成 | Flask 単一ファイル `app.py`（約26,000行）＋ Jinja2 テンプレート ＋ Supabase(Postgres) ＋ Cloud Run |
| **開発ブランチ** | `tasukaru-dev` |
| **本番ブランチ** | `tasukaru` ← **`main` ではない。`main` は 2026-04 で止まった残骸。触らないこと** |
| デプロイ | push すると Cloud Build が自動で走る（2〜3分） |

## セキュリティ・運用ルール（絶対に守ること）

1. **APIキー・トークン・パスワードを画面に出さない。** 値を確認する必要があるときは必ずマスクする（例: `AIza****...****4Xk`）。
   ソースやログに実値を書き出すコマンドも投げない。
2. **利用者の実名・住所を返答に書かない。** 件数・時刻・集計だけで話す。
3. **本番データを変更する前に、必ず SQL を提示して私の承認を取る。** 勝手に実行しない。
4. **本番では書き込みテストをしない**（打刻などは DEV のみ。本番は読み取りのみ）。
5. **`alert()` を使わない。** Chrome 拡張の自動操作をブロックするため。トーストで代替する。

## 作業のやり方（この形式を守ってください）

- **私は git を実行できません。あなたがコマンドを書いて、私がコピペで流します。** 実行結果は貼り付けます。
- **コードの修正は「冪等なパッチスクリプト」で渡してください。** `patch_*.py` を作り、
  ① 元ファイルをバックアップ → ② アンカー文字列の出現回数が **ちょうど1回**であることを assert → ③ 置換、という作り。
  すでに適用済みならスキップする。`patch_*.py` は `.gitignore` 対象なので commit しなくてよい。
- 変更後は `python3 -m py_compile app.py` と Jinja のパースで検証してください。
- **DDL は `db/*.sql` に書いて渡してください。** 私が Supabase の SQL Editor に貼って実行します。
- **ファイルの中身が必要になったら、VSCode のターミナルで実行するコマンド（ダウンロードフォルダへコピーするもの）をください。**
  私がそれを実行して、このチャットにファイルを添付します。例:
  ```bash
  cp "/Users/ZIMAX 1/dev/kaigo-ai-app/templates/base.html" ~/Downloads/
  ```
- コード内のコメントは日本語で、**「なぜそうしたか」**を書いてください。半年後の自分が読んで分かるように。

## 技術的に踏んだ地雷（同じ穴に落ちないため）

- `base.html` は **480px 固定幅の1カラム**（`--page-max-width`）。**ビューポートのメディアクエリではなく CSS `@container` を使う**。
  Flex/Grid の子には `min-width: 0` が要る。
- **`position: sticky` の基準は「いちばん近いスクロールの箱」**。`overflow-x:auto` を付けた時点でそこが箱になり、
  箱に高さが無いと `top` は貼りつく先を失う。表を固定したいなら箱に `max-height` を与えて縦横ともその中で動かす。
- `-webkit-overflow-scrolling:touch` は iOS で sticky を壊す。
- **`0` と「未設定」は違う。** `--lc-nav-h: 0px` を入れて `:root` のセーフエリア下限を上書きしてしまい、
  操作バーがホームインジケータに沈んだことがある。消したいときは `removeProperty` を使う。
- **`spaNav(event, url)` は空の関数**。実際の遷移は `<a href>` が担っていた。href を外したら `location.href` を使う。
- IIFE の中に書いた関数は `onclick="foo()"` から呼べない（グローバルに居ないため）。`window.foo = ...` にする。
- iOS の `<a href>` は長押しでリンクプレビューが出る → ドラッグさせたい要素は `href` を外して `<div role="button">` にする。
- PWA で `target="_blank"` を使うとログインが壊れる。同一タブで開く。
- OS の端スワイプ（戻る）は web からは止められない。引き出しを端から出す設計はそこと喧嘩する。
- `records` テーブルに patient_id は無い。`user_name`（テキスト）で結合する。`category` も名前の文字列。
- 「その日に来ている人」＝その日に `vitals` がある人。連絡帳・請求額計算と同じ判定を使う（増やすとズレる）。
- 設定の保存先: 施設ごと＝`admin_settings(facility_code, key, value)`（UNIQUE 制約が無いので手動 upsert）。
  職員ごと＝`staff_settings(facility_code, staff_name, key, value)`。
- Cloud Build の「ビルドを実行できませんでした: INTERNAL / 0 steps」は GCP 側の一時障害。空コミットで再実行すれば通る。

## 直近で作ったもの（2026-07-12〜14）

- 請求額計算（レク費精算）と **レシートOCR**（`/api/rec/ocr`。画像を証跡として保存し、明細を自動入力）
- **送迎モジュール**（週表・運行記録表の印刷・**走行距離入力（事業所ごとにトグルでON/OFF）**）
- **契約書・重要事項説明書を4系統に拡張**（介護／介護予防通所／生活支援通所／保険外）。`keiyaku_render.py`。
  加算は科学的介護推進体制加算2・処遇改善加算Ⅱロ。料金は豊田市の総合事業マスタと現物の重説で検算済み。
- **引き出しメニュー**（全画面の端から出るアイコン一覧。上下左右・自由配置・アイコン色・向きごとに配置を記憶・バッジ）
- **ボトムナビを廃止**して引き出しに一本化。ログアウトは歯車の中へ移設。
- **記録充足チェック**（`/record_check`。ケース記録の上のタブ。来所者×カテゴリの記録件数を数字で見る。0件クリックで記録入力へ）

## いま残っているタスク

1. レシートOCRの実運用での精度確認（来週。**レシートを取っておく**）と、送迎の走行距離入力の使用感。
2. DEV のダミー利用者データ削除（`db/soge_dummy_dev*.sql`）。
3. 管理者が施設の既定ドロワー配置を決められるように（今は個人設定のみ）。
4. admin.html に残る `alert()` の掃除。
5. 予防・生活支援の「職員配置／提供サービス」の既定文を系統別に持たせる。

まずは README を読んで、状況を把握したと一言ください。そのあと私が次にやりたいことを伝えます。
