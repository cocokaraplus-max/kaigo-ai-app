# TASUKARU 開発 引き継ぎメモ（2026-06-04 セッション末）

次のチャットの冒頭で、まず `README_tasukaru_dev.md` を読み、続けてこのメモを読めば、現状・進め方・残タスクが分かるようにしてある。

---

## 0. まず最初に読むもの
- リポジトリ直下の `README_tasukaru_dev.md`（§1〜§7）。特に **§7 バイタル機能** が今回の作業範囲。
- このメモ（HANDOFF）。README に書ききれない「進め方の作法」と「中断中タスクの細部」を補う。

---

## 1. プロジェクト基本情報
- ローカルパス: `/Users/ZIMAX 1/dev/kaigo-ai-app`
- 構成: Flask + Supabase + Cloud Run
- DEVブランチ: `tasukaru-dev` / 本番ブランチ: `tasukaru`
- DEV URL: https://tasukaru-dev-191764727533.asia-northeast1.run.app
- 本番URL: https://tasukaru-191764727533.asia-northeast1.run.app
- GitHub: cocokaraplus-max/kaigo-ai-app / GCP: tasukaru-production（asia-northeast1）
- 主要ファイル: `templates/vitals.html`（バイタル測定ページ・約4,000行超）, `app.py`（バックエンド本体）, `templates/manual.html`（ガイド）, `templates/print_preview.html`, `templates/print_output.html`
- 検証用利用者: 青木 利夫 / 倍子（よみ「ますこ」＝当て字の検証用）。実データに 阿部武・池田ヨシ・石井三郎・石川トメ・石田ナミ・井上光子 など。

---

## 2. 作業の進め方（厳守ルール）

### 2-1. 役割分担
- アシスタントはローカルを直接操作できない。**ユーザーがVSCodeのターミナルでコマンドを実行し、結果を貼り付ける**形で進める。
- アシスタントは「コードを確認するための grep / sed コマンド」を投げ、ユーザーが出力を貼る。それを見てからパッチを作る。**推測でパッチを書かない**（既存機能を壊すため）。

### 2-2. パッチの作法
- 変更は必ず **冪等な Python スクリプト**（アンカー文字列ベース）で渡す。複数回実行しても結果が同じになるように書く。
- アシスタントはサンドボックスで以下を検証してからファイルを渡す:
  - Python構文: `py_compile`
  - 挿入するJS片の構文: `node --check`
  - 実ファイル該当箇所を再現したファイルにパッチを適用し、適用後の全体が構文OKか
  - **冪等性**（2回目の実行で全項目スキップになるか）
- 冪等判定の罠（過去に2度踏んだ）: 新規挿入するHTMLの onclick 内に関数名の文字列が入ると、「関数定義済みか」チェックがその文字列に釣られて誤スキップする。**判定は必ず `window.fnName = function` の有無で行う**（文字列出現で判定しない）。
- JSのTDZ対策: グローバルは `window.xxx = function...` 形式で定義する。
- フロントの `patient_id` と表示側 `pid` は型がずれることがあるので **String で統一**して比較する。

### 2-3. ファイルの受け渡し（重要）
- アシスタントはパッチを `present_files` で提示する → ユーザーがダウンロード。
- ユーザーは **VSCodeのターミナルにコマンドを投げてダウンロード/適用**する。典型例:
  ```
  cp ~/Downloads/patch_xxx.py .
  python3 patch_xxx.py
  ```
- 適用結果（標準出力）と、確認用 grep の結果を**ユーザーがチャットに貼る**。アシスタントはそれを見て次へ進む。
- 大きめのコード確認も、ユーザーが `grep -n` / `sed -n` をターミナルで実行 → 出力を添付、という流れ。
- ブラウザでの実機確認は **Chrome連携（DEVタブのみ）**。本番タブは操作しない。マイク録音などはユーザーが操作し、アシスタントは画面/ネットワークを見る。

### 2-4. セキュリティ（厳守）
- **シークレット値は一切出さない／文書に書かない／ログに残さない**。環境変数は「名前のみ」記載し、値はマスク（例: `LINE_CHANNEL_ACCESS_TOKEN = ****`）。
- 鍵・トークン・パスワード類が画面やコードに現れたら、引用・記録時に**スクランブル（伏せ字）をかけて抽出**する。
- Cloud Run に設定済みの環境変数（**値は書かない**）: `LINE_CHANNEL_ACCESS_TOKEN` / `LINE_CHANNEL_SECRET` / `LINE_ADMIN_USER_ID` / `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` / 多数の `STRIPE_PRICE_*`。
- Supabase のキーや接続情報も同様に出さない。

### 2-5. 本番反映手順（§2準拠）
```
git checkout tasukaru
git merge tasukaru-dev
git push origin tasukaru
git checkout tasukaru-dev
```
- マージ後の `xxxxxxx..yyyyyyy tasukaru -> tasukaru` の出力で、コミット範囲とコンフリクト有無を必ず確認。
- 本番反映後は Cloud Run の本番ビルドが走る。数分後に本番URLへ反映。ブラウザは**ハードリロード（Cmd+Shift+R）**。
- 散乱パッチ対策で `.gitignore` に `patch_*.py` 等と `patch.py` を追加済み。

---

## 3. 今回（2026-06-04）完了し、本番反映済みのもの

本番反映は2回に分割:
- 1回目 commit `17b422a`: 読み仮名照合+確信度UI / 10分グルーピング / 測定UI第1段 / メモ機能
- 2回目 `17b422a..9ef3f89`: 無音バグ対応一式（音量監視・累積保持・回数撤去・しきい値0.05）+ README更新

機能の要点（詳細は README §7）:
1. **体温一括の読み仮名照合＋確信度UI** … 当て字「倍子（ますこ）」対応。緑(high自動)/黄(mid要確認)/赤(none対象外)。サーバ `/api/vital_bulk_temp`、`_voice_match_temp`。
2. **10分枠グルーピング＋複数測定の印＋履歴モーダル** … 「本日の記録」。`groupVitalWindows`、`openVitalHistoryModal`。表示層のみ・非破壊。
3. **測定UI 第1段（案B 骨組み）** … 一覧（2列カード）⇔測定画面の2状態。`window._measureView`、`openMeasure`/`backToMeasureList`。
4. **メモ機能** … 入力欄フル幅化＋音声ボタン右隣。日次にメモアイコン＋`openVitalNoteModal`（`_vitalNoteCache`）。**`vitals.note` カラムを dev/prod 両方に追加済み**。
5. **無音バグ対応（誤登録の根絶）** … 録音中に Web Audio API で音量(RMS)監視し、無音なら送信しない。`startBulkVoiceMonitor`/`stopBulkVoiceMonitor`、`window._bulkVoiceDetected`。サーバ側もプロンプト強化＋空結果検出＋2KB未満弾き。
   - しきい値 `BULK_VOICE_RMS_THRESHOLD = 0.05`、`BULK_VOICE_MIN_HITS = 8`。実測値: 無音 最大RMS≈0.0218 / 発話 0.0955〜0.2083。
   - **現場の声を聞いて調整する前提**。`templates/vitals.html` の `BULK_VOICE_RMS_THRESHOLD` の数値1つを変えるだけ（無音が通るなら上げる／小声が弾かれるなら下げる）。
6. **累積保持＋クリアボタン＋回数セレクタ撤去** … 解析のたびにクリアせず緑を累積（同一pid上書き）、黄は確定で昇格、赤は抹消。`renderBulkTempResults()`。回次は10分グルーピングが自動決定するため回数セレクタ（旧 `bulk-temp-order`）は撤去。

---

## 4. 残タスク（次にやること）

### 4-1. 測定UI 第2段（最優先・着手中で中断）
- 測定タブの一覧（2列カード）上部に **検索窓** と **全員/午前/午後タブ** を配置する。
- 検索: 名前・ふりがな・カルテ番号の部分一致。既存ロジックを流用する:
  - フロント: `filterAddPatientList`（利用者追加モーダルの検索）@ `templates/vitals.html`
  - サーバ: `_patient_matches_query` @ `app.py`
- 午前/午後: 既存の `currentAmpm` 切替を使う（測定タブの `renderPatientList` は既に `currentAmpm` で targets を絞っている）。
- 差し込み位置: 第1段で作り替えた `renderPatientList` の「一覧（2列カード）」描画の手前にUI、`targets` に検索フィルタを追加する形。
- **中断時に投げていた確認コマンド（ユーザーがまだ未返信）**:
  ```
  sed -n '/window.filterAddPatientList = function/,/^};/p' templates/vitals.html
  grep -n "renderPatientList\|_measureView\|patient-vitals-list\|targets.map\|currentAmpm\|grid-template-columns:repeat(2" templates/vitals.html
  ```
  → 次セッションはまずこの2つの出力をもらってから第2段パッチを作る。

### 4-2. 測定UI 第3段
- 進捗バー（○/○名）。
- 済/未の色分け（緑=測定済/黄など）。
- カード下段の表示切替（バイタル要約 ⇔ ふりがな＋カルテ番号、トグルできる）。

### 4-3. ガイド更新
- `templates/manual.html` のバイタル関連記述を、上記の新機能（読み仮名照合・10分グルーピング・2列測定UI・メモ・体温一括の累積/無音対応）に合わせて更新。
- 当初方針は「全機能が固まってから」。測定UIが第3段まで終わったら着手が自然。

### 4-4. 任意の調整・様子見
- 無音しきい値（`BULK_VOICE_RMS_THRESHOLD`）の現場フィードバック反映。
- 10分枠の延伸上限（叩き台6時間）。
- 複数測定の印のデザイン（現状やや控えめ）。
- 「まこ」等のあいまい照合を mid→none に倒すかどうか。

### 4-5. バイタル以外の既存残タスク（README §4・§5 参照）
- 決済（Stripe）・LINE連携まわりの残タスク（料金プラン、Webアプリの承認処理、契約管理画面、期限通知など）。詳細は README §5-4。
- 掲示板「さらに読み込む」ボタン、`utils.pycode` の削除 など（README §5-4 優先度中）。

---

## 5. 技術メモ・落とし穴
- ブラウザキャッシュで古いHTMLが出ることがある → 実機確認は必ずハードリロード。
- Chrome連携の `read_network_requests` は urlContains フィルタ／レスポンスボディ取得が期待通り効かないことがある。無音レスポンスのJSON直接取得は失敗したので、画面スクショ＋一時RMSデバッグ表示で代替した（デバッグ表示は確定パッチで除去済み）。
- 体温一括の確信度フロー: 緑＝即登録予定 / 黄＝氏名ドロップダウンで確定して初めて登録予定へ昇格 / 赤＝未検出で抹消。累積リストに入るのは「緑＋確定した黄」だけ。
- `vitals` の行特定キーは `(facility_code, patient_id, measured_date)`。`measured_at` はUTC保存。10分枠判定は `measured_at`（絶対時刻）で行う。
- パッチ作業ディレクトリ（アシスタント側サンドボックス）: `/home/claude/patch`、出力は `/mnt/user-data/outputs/`。

---

## 6. 次セッションの最初の一手（推奨）
1. `README_tasukaru_dev.md` と本メモを読む。
2. 作業ツリー確認: `git status --short && git branch --show-current`（`tasukaru-dev` にいること、余計な未コミットが無いこと）。
3. 測定UI第2段に着手するため、4-1 の2つの確認コマンドの出力をユーザーからもらう。
4. それを見て、検索窓＋午前午後タブの冪等パッチを作成 → サンドボックス検証 → DEV反映 → 実機確認 → 問題なければ本番反映。
